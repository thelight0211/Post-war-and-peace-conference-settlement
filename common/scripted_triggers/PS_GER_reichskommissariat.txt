	is_controlled_by_GER_or_subject = {
			custom_trigger_tooltip = {
			tooltip = is_controlled_by_GER_or_subject
				OR = {
					is_fully_controlled_by = GER
					controller = { is_subject_of = GER }
				}
		}
	}
	is_claim_or_core_of_ROOT = {
		OR = {
			is_claimed_by = ROOT
			is_core_of = ROOT
		}
	}
	is_colony_or_reickskommissariat = {
		NOT = {
			any_owned_state = {
				is_core_of = PREV
			}
		}
	}
