return_core_category = {

	return_cores_to_ally = {
		icon = generic_agreement
		days_re_enable = 5
		priority = 999

		visible = {
			NOT = { has_country_flag = bureaucratic_cooldown_short }
			is_in_faction = yes
			NOT = { 
				any_other_country = {
					has_country_flag = KR_returning_land 
				}
			}
			FROM = {
				is_in_faction_with = ROOT
				exists = yes
			}
			any_owned_state = {
				is_fully_controlled_by = ROOT
				is_core_of = FROM
				NOT = {
					OR = {
						is_claimed_by = ROOT
						is_core_of = ROOT
						has_state_flag = KR_keeping_stateROOT
					}
				}
				NOT = {
					has_state_flag = Land_locked_state
				}
				OR = {
					is_coastal = yes
					any_neighbor_state = {
						is_owned_by = FROM
						NOT = {
							has_state_flag = Land_locked_state
						}
					}
				}
				NOT = {
					any_country_with_core = {
						is_ally_with = ROOT 
						exists = yes
						NOT = { tag = FROM }
						NOT = { tag = ROOT }
						capital_scope = {
							is_core_of = FROM
						}
						NOT = {
							FROM = {
								capital_scope = {
									is_core_of = PREV.PREV
								}
							}
						}
					}
				}

				# Exclude states where ROOT is a German subject and the state isn't ROOT's capital core
				NOT = {
					ROOT = {
						is_colony_or_reickskommissariat = yes
					}
				}

				# ---- TERRITORIAL EXCEPTIONS ----
				# States excluded from automatic return under specific conditions

				# Universal exclusions (any ROOT, any FROM)
				NOT = { state = 802 }
				NOT = { state = 103 }
				NOT = { state = 104 }
				NOT = { state = 70 }
				NOT = { state = 71 }
				NOT = { state = 664 }
				NOT = { state = 73 }
				NOT = { state = 77 }
				NOT = { state = 184 }
				NOT = { state = 735 }		# Savoy
				NOT = { state = 1 }			# Corsica
				NOT = { state = 102 }
				NOT = { state = 119 }
				NOT = { state = 39 }
				NOT = { state = 28 }
				NOT = { state = 45 }		# Vojvodina

				# Country-specific exclusions
				NOT = {
					OR = {
						AND = { ROOT = { tag = ITA } OR = { state = 163 state = 846 } }
						AND = { ROOT = { tag = GER } OR = { state = 85 state = 807 state = 188 state = 86 state = 762 } }
						AND = { ROOT = { tag = ENG } OR = { state = 331 state = 332 state = 659 } }
						AND = { ROOT = { tag = FRA } OR = { state = 320 } }
						AND = { ROOT = { tag = POR } OR = { state = 321 } }
					}
				}

				# Road to 56 conditional exclusions
				NOT = {
					OR = {
						AND = {
							NOT = { has_global_flag = road_to_56_is_active }
							OR = { state = 446 state = 912 state = 972 state = 975 }
						}
						AND = {
							has_global_flag = road_to_56_is_active
							OR = { state = 995 state = 911 state = 964 state = 1078 state = 1086 state = 336 }
						}
					}
				}

				# ENG road_to_56 exclusion
				NOT = {
					AND = {
						ROOT = { tag = ENG }
						OR = {
							AND = { NOT = { has_global_flag = road_to_56_is_active } state = 1021 }
							AND = { has_global_flag = road_to_56_is_active state = 336 }
						}
					}
				}

				# Conditional exclusions (ROOT must own specific states)
				NOT = {
					OR = {
						AND = {
							ROOT = { owns_state = 68 owns_state = 63 }
							OR = { state = 63 state = 68 }
						}
						AND = {
							ROOT = { owns_state = 5 owns_state = 763 }
							OR = { state = 5 state = 763 }
						}
						AND = {
							ROOT = { owns_state = 66 owns_state = 67 }
							OR = { state = 66 state = 67 }
						}
						AND = {
							ROOT = { owns_state = 82 owns_state = 83 owns_state = 84 owns_state = 76 }
							OR = { state = 82 state = 83 state = 84 state = 76 }
						}
						AND = {
							ROOT = {
								OR = {
									AND = { NOT = { has_global_flag = road_to_56_is_active } owns_state = 972 }
									AND = { has_global_flag = road_to_56_is_active owns_state = 1078 }
								}
								owns_state = 69
								owns_state = 74
							}
							OR = {
								AND = { NOT = { has_global_flag = road_to_56_is_active } state = 972 }
								AND = { has_global_flag = road_to_56_is_active state = 1078 }
								state = 69
								state = 74
							}
						}
					}
				}
			}
		}

		target_root_trigger = {
			is_in_faction = yes
		}

		target_array = allies

		target_trigger = {
			FROM = {
				is_in_faction_with = ROOT
				exists = yes
			}
		}

		cost = 0

		fire_only_once = no
		ai_will_do = {
			factor = 100
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision return_cores_to_ally target: [From.GetName]"
			set_country_flag = KR_returning_land
			FROM = { save_event_target_as = return_to }
			country_event = { id = kr.political.75 days = 1 }
		}
	}

	return_cores_to_subject = {
		icon = generic_agreement
		days_re_enable = 5
		priority = 999

		visible = {
			NOT = { has_country_flag = bureaucratic_cooldown_short }
			num_subjects > 0
			NOT = { 
				any_other_country = {
					has_country_flag = KR_returning_land 
				}
			}
			FROM = {
				is_subject_of = ROOT
				NOT = { is_in_faction_with = ROOT }
				exists = yes
			}
			any_owned_state = {
				is_fully_controlled_by = ROOT
				is_core_of = FROM
				NOT = {
					OR = {
						is_claimed_by = ROOT
						is_core_of = ROOT
						has_state_flag = KR_keeping_stateROOT
					}
				}
				NOT = {
					has_state_flag = Land_locked_state
				}
				OR = {
					is_coastal = yes
					any_neighbor_state = {
						is_owned_by = FROM
						NOT = {
							has_state_flag = Land_locked_state
						}
					}
				}
				NOT = {
					any_country_with_core = {
						is_ally_with = ROOT 
						exists = yes
						NOT = { tag = FROM }
						NOT = { tag = ROOT }
						capital_scope = {
							is_core_of = FROM
						}
						NOT = {
							FROM = {
								capital_scope = {
									is_core_of = PREV.PREV
								}
							}
						}
					}
				}

				# ---- TERRITORIAL EXCEPTIONS ----
				# (identical to return_cores_to_ally)

				# Universal exclusions (any ROOT, any FROM)
				NOT = { state = 802 }
				NOT = { state = 103 }
				NOT = { state = 104 }
				NOT = { state = 70 }
				NOT = { state = 71 }
				NOT = { state = 664 }
				NOT = { state = 73 }
				NOT = { state = 77 }
				NOT = { state = 184 }
				NOT = { state = 735 }		# Savoy
				NOT = { state = 1 }			# Corsica
				NOT = { state = 102 }
				NOT = { state = 119 }
				NOT = { state = 39 }
				NOT = { state = 28 }
				NOT = { state = 45 }		# Vojvodina

				# Country-specific exclusions
				NOT = {
					OR = {
						AND = { ROOT = { tag = ITA } OR = { state = 163 state = 846 } }
						AND = { ROOT = { tag = GER } OR = { state = 85 state = 807 state = 188 state = 86 state = 762 } }
						AND = { ROOT = { tag = ENG } OR = { state = 331 state = 332 state = 659 } }
						AND = { ROOT = { tag = FRA } OR = { state = 320 } }
						AND = { ROOT = { tag = POR } OR = { state = 321 } }
					}
				}

				# Road to 56 conditional exclusions
				NOT = {
					OR = {
						AND = {
							NOT = { has_global_flag = road_to_56_is_active }
							OR = { state = 446 state = 912 state = 972 state = 975 }
						}
						AND = {
							has_global_flag = road_to_56_is_active
							OR = { state = 995 state = 911 state = 964 state = 1078 state = 1086 state = 336 }
						}
					}
				}

				# ENG road_to_56 exclusion
				NOT = {
					AND = {
						ROOT = { tag = ENG }
						OR = {
							AND = { NOT = { has_global_flag = road_to_56_is_active } state = 1021 }
							AND = { has_global_flag = road_to_56_is_active state = 336 }
						}
					}
				}

				# Conditional exclusions (ROOT must own specific states)
				NOT = {
					OR = {
						AND = {
							ROOT = { owns_state = 68 owns_state = 63 }
							OR = { state = 63 state = 68 }
						}
						AND = {
							ROOT = { owns_state = 5 owns_state = 763 }
							OR = { state = 5 state = 763 }
						}
						AND = {
							ROOT = { owns_state = 66 owns_state = 67 }
							OR = { state = 66 state = 67 }
						}
						AND = {
							ROOT = { owns_state = 82 owns_state = 83 owns_state = 84 owns_state = 76 }
							OR = { state = 82 state = 83 state = 84 state = 76 }
						}
						AND = {
							ROOT = {
								OR = {
									AND = { NOT = { has_global_flag = road_to_56_is_active } owns_state = 972 }
									AND = { has_global_flag = road_to_56_is_active owns_state = 1078 }
								}
								owns_state = 69
								owns_state = 74
							}
							OR = {
								AND = { NOT = { has_global_flag = road_to_56_is_active } state = 972 }
								AND = { has_global_flag = road_to_56_is_active state = 1078 }
								state = 69
								state = 74
							}
						}
					}
				}
			}
		}

		target_root_trigger = {
			num_subjects > 0
		}

		target_array = subjects

		target_trigger = {
			FROM = {
				is_subject_of = ROOT
				NOT = { is_in_faction_with = ROOT }
				exists = yes
			}
		}

		cost = 0

		fire_only_once = no
		ai_will_do = {
			factor = 100
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision return_cores_to_subject target: [From.GetName]"
			set_country_flag = KR_returning_land
			FROM = { save_event_target_as = return_to }
			country_event = { id = kr.political.75 days = 1 }
		}
	}

	return_cores_to_master = {
		icon = generic_agreement
		priority = 999
		
		visible = { NOT = { has_country_flag = bureaucratic_cooldown_short } }
		
		target_root_trigger = {
			num_subjects > 0
		}
		target_array = subjects
		
		target_trigger = {
			FROM = {
				is_subject_of = ROOT
				any_owned_state = {
					is_fully_controlled_by = FROM
					OR = {
						is_core_of = ROOT
						is_claimed_by = ROOT
					}
					NOT = {
						has_state_flag = Land_locked_state
					}
					OR = {
						is_coastal = yes
						has_state_flag = connected_via_con
						any_neighbor_state = {
							is_owned_by = ROOT
							NOT = {
								has_state_flag = Land_locked_state
							}
						}
					}
				}
				NOT = {
					OR = {
						all_owned_state = {
							OR = {
								is_core_of = ROOT
								is_claimed_by = ROOT
							}
						}
						capital_scope = {
							is_core_of = ROOT
						}
					}
				}
			}	
		}

		fire_only_once = no

		ai_will_do = {
			factor = 100
		}

		complete_effect = {
			hidden_effect = {
				country_event = puppets_give_cores.1
				723 = { owner = { set_country_flag = refreshing_claims }}
				random_country = {
					limit = { NOT = { has_country_flag = refreshing_claims } }
					transfer_state = 723
				}
				random_country = {
					limit = { has_country_flag = refreshing_claims }
					transfer_state = 723
					clr_country_flag = refreshing_claims
				}
			}
		}
	}
}