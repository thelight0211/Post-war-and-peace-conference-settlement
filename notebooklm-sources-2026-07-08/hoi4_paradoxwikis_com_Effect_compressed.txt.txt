hoi4_paradoxwikis_com__Effect_compressed.txt
============================================

Paradox Wikis Hearts of Iron 4 Wiki Search Hearts of Iron 4 Log in

Page Discussion Read Edit Edit source View history

Effect Effects (also known as Commands) are used in order to affect the game dynamically from within a specific scope. They are a one-time change to the current condition of the game, without the ability to have a lasting

This is a community maintained wiki. If you spot a mistake, please effect. Instead, modifiers are used to have a continuous, everlasting effect on the game's condition that can be represented with a number. Effect blocks cannot be used to apply modifiers directly, however they can add help with fixing it. something that can apply modifiers, most commonly with add_ideas.

Effects are used throughout the game in numerous scopes, most commonly edited effect blocks are national focus rewards, event options and decision effects.

Note that certain effects may take a value from a variable, i.e. add_manpower = var:my_var  This is noted by <variable> in an effect's parameters. See Variables for information on the variable effects.

The list of effects may be outdated. A complete, but unsorted, list of effects can be found in /Hearts of Iron IV/documentation/effects_documentation.html or /Hearts of Iron IV/documentation/effects_documentation.md. Hearts of Iron 4 Wiki Recent changes Contents [hide] Random page 1 Scopes Paradox Wikis 1.1 Effect scopes

- Main portal 1.2 Effects with scopes

- Wikis discord 1.3 Dual scopes

- Contact us 2 Any scope

- Style guidelines 2.1 General

2.2 Border wars HoI4 links

2.3 Variables

- Forums

- Game discord 2.4 Arrays

- Mods 3 Country scope

- Store 3.1 General

- YouTube 3.2 States

3.3 Mana Tools

3.4 Politics What links here 3.5 Balance of power Related changes Special pages 3.6 Diplomacy

Printable version 3.7 Faction Permanent link 3.8 Autonomy Page information 3.9 Governments in exile

3.10 War 3.11 Resources 3.12 Buildings 3.13 National focuses 3.14 Decisions 3.15 Missions 3.16 Technologies 3.17 Ideas 3.18 Units 3.19 Equipment 3.20 Military 3.21 Doctrine 3.22 Intelligence 3.23 Characters

3.23.1 Unit leaders 3.23.2 Country leaders 3.23.3 Advisors 3.23.4 Scientists

3.24 MIOs 3.25 Special Projects 3.26 Career profile 3.27 History 3.28 Variable

4 State scope 4.1 General 4.2 Buildings 4.3 Resistance and compliance 4.4 Raids

5 Character scope 5.1 General 5.2 Unit leaders 5.3 Country leaders 5.4 Combat 5.5 Operatives

6 Division scope 7 MIO scope 8 Contract scope 9 Raid scope 10 Special Project scope 11 Other scopes 12 Flow control

12.1 If statements 12.2 Random effects 12.3 Tooltip manipulation

13 Meta effects 14 Scripted effects

14.1 Useful scripted effects

Scopes ​[ edit | edit source ]

Main article: Scopes

Scopes serve as special effect types that modify the entity that serves as the context for the effects being executed, such as GER = { add_political_power = 150 }  adding 150 political power to  Germany.

Effect scopes ​[ edit | edit source ]

These can only be used as effects; trying to use them as triggers will result in nothing happening.

Effect scopes: [Collapse]

Version Name Usage Target type Example Description

Added Executes children effects on every country that meets the limit, including those that do not

every_possible_country Always usable Country every_possible_country = { ... } 1.11 exist.

every_country Always usable Country every_country = { … } Executes contained effects on every country that meets the limit. 1.0

random_country Always usable Country random_country = { … } Executes contained effects on a random country that meets the limit. 1.0

Executes contained effects on every country that meets the limit and is not the same country every_other_country Within country scope only Country every_other_country = { … } 1.0

as the one this is contained in.

Executes contained effects on a random country that meets the limit and is not the same random_other_country Within country scope only Country random_other_country = { … } 1.0

country as the one this is contained in.

every_country_with_original_tag = {

every_country_with_original_tag Always usable Country #required 1.9

original tag. …                  #effects to run

}

random_country_with_original_tag = {

random_country_with_original_tag Always usable Country #required

original tag. …                  #effects to run

}

Executes contained effects on every country that meets the limit and borders the country every_neighbor_country Within country scope only Country every_neighbor_country = { … } 1.0

this is contained in.

Executes contained effects on a random country that meets the limit and borders the random_neighbor_country Within country scope only Country random_neighbor_country = { … } 1.0

country this is contained in.

Executes contained effects on every country that meets the limit and has any core states every_occupied_country Within country scope only Country every_occupied_country = { … } 1.9

controlled by the country this is contained in.

Executes contained effects on a random country that meets the limit and has any core random_occupied_country Within country scope only Country random_occupied_country = { … } 1.9

states controlled by the country this is contained in.

Executes children effects on every Allied Country different from the one in scope (or every_allied_country Within country scope only Country every_allied_country = { … } 1.15

`random_select_amount` of random country if specified) that fulfills the `limit` trigger.

Executes children effects on a random Allied Country different from the one in scope that random_allied_country Within country scope only Country random_allied_country = { … } 1.15

fulfills the `limit` trigger.

Executes contained effects on every country that meets the limit and is at war with the every_enemy_country Within country scope only Country every_enemy_country = { … } 1.0

country this is contained in.

Executes contained effects on a random country that meets the limit and is at war with the random_enemy_country Within country scope only Country random_enemy_country = { … } 1.0

country this is contained in.

Executes contained effects on every country that meets the limit and is a subject of the every_subject_country Within country scope only Country every_subject_country = { … } 1.11

country this is contained in.

Executes contained effects on a random country that meets the limit and is a subject of the random_subject_country Within country scope only Country random_subject_country = { … } 1.11

country this is contained in.

Executes children effects on every faction member of the country's faction in scope, if every_faction_member Within country scope only Country every_faction_member = { … } 1.17

country does not have a faction it will only work on itself.

every_state Always usable State every_state = { … } Executes contained effects on every state that meets the limit. 1.0

random_state = {

random_state Always usable State     prioritize = { 123 321 } #optional Executes contained effects on a random state that meets the limit. 1.0 …    #effects to run

}

Executes contained effects on every state that meets the limit and neighbours the state this every_neighbor_state Within state scope only State every_neighbor_state = { … } 1.0

is contained in.

Executes contained effects on a random state that meets the limit and neighbours the state random_neighbor_state Within state scope only State random_neighbor_state = { … } 1.0

this is contained in. Does not support prioritizing.

Executes contained effects on every state that meets the limit and is owned by the country every_owned_state Within country scope only State every_owned_state = { … } 1.0

this is contained in.

random_owned_state = {

fects on a random state that meets the limit and is owned by the random_owned_state Within country scope only State     prioritize = { 123 321 } #optional Executes contained ef

1.0 …    #effects to run country this is contained in. }

Executes contained effects on every state that meets the limit and is a core of the country every_core_state Within country scope only State every_core_state = { … } 1.11

this is contained in.

random_core_state = {

Executes contained effects on a random state that meets the limit and is a core of the random_core_state Within country scope only State     prioritize = { 123 321 } #optional 1.11

}

Executes contained effects on every state that meets the limit and is controlled by the every_controlled_state Within country scope only State every_controlled_state = { … } 1.9

country this is contained in.

random_controlled_state = {

tional Executes contained effects on a random state that meets the limit and is controlled by the random_controlled_state Within country scope only State     prioritize = { 123 321 } #op 1.9

}

random_owned_controlled_state = {

it and is owned and random_owned_controlled_state Within country scope only State     prioritize = { 123 321 } #optional Executes contained effects on a random state that meets the lim

1.3 …    #effects to run controlled by the country this is contained in. }

Executes contained effects on every unit leader (corps commanders, field marshals, every_unit_leader Within country scope only Unit Leader every_unit_leader = { … } 1.5

admirals) that meets the limit and is recruited by the country this is contained in.

Executes contained effects on a random unit leader (corps commanders, field marshals, random_unit_leader Within country scope only Unit Leader random_unit_leader = { … } 1.5

admirals) that meets the limit and is recruited by the country this is contained in.

Executes contained effects on every army leader that meets the limit and is recruited by the every_army_leader Within country scope only Unit Leader every_unit_leader = { … } 1.5

country this is contained in.

Executes contained effects on a random army leader that meets the limit and is recruited by random_army_leader Within country scope only Unit Leader random_army_leader = { … } 1.5

the country this is contained in.

Executes contained effects on every army leader that meets the limit. Preferable to use global_every_army_leader Always usable Unit Leader global_every_army_leader = { … } 1.5

every_army_leader unless necessary to use global_every_army_leader.

Executes contained effects on every navy leader that meets the limit and is recruited by the every_navy_leader Within country scope only Unit Leader every_navy_leader = { … } 1.5

country this is contained in.

Executes contained effects on a random navy leader that meets the limit and is recruited by random_navy_leader Within country scope only Unit Leader random_navy_leader = { … } 1.5

the country this is contained in.

Within country scope or operations Executes contained effects on every operative that meets the limit and is recruited by the every_operative Operative every_operative = { … } 1.9

only country this is contained in. Within country scope or operations Executes contained effects on a random operative that meets the limit and is recruited by

random_operative Operative random_operative = { … } 1.9 only the country this is contained in.

Executes contained effects on every character that meets the limit and is recruited by the every_character Within country scope only Character every_character = { … } 1.11

country this is contained in.

Executes contained effects on a random character that meets the limit and is recruited by random_character Within country scope only Character random_character = { … } 1.11

the country this is contained in.

Executes contained effects on every division that meets the limit and is owned by the every_country_division Within country scope only Division every_country_division = { … } 1.12

current country.

Executes contained effects on a random division that meets the limit and is owned by the random_country_division Within country scope only Division random_country_division = { … } 1.12

current country.

Executes contained effects on every division that meets the limit and is located within the every_state_division Within state scope only Division every_state_division = { … } 1.12

current state.

Executes contained effects on a random division that meets the limit and is located within random_state_division Within state scope only Division random_state_division = { … } 1.12

the current state.

every_military_industrial_organization = { every_military_industrial_organization Within country scope only MIO Executes contained effects on every MIO within the current country that meets the limit. 1.13

… }

random_military_industrial_organization = { random_military_industrial_organization Within country scope only MIO Executes contained effects on a random MIO within the current country that meets the limit. 1.13

… }

Executes contained effects on every purchase contract within the current country that meets every_purchase_contract Within country scope only Purchase contract every_purchase_contract = { … } 1.13

the limit.

Executes contained effects on a random purchase contract within the current country that random_purchase_contract Within country scope only Purchase contract random_purchase_contract = { … } 1.13

meets the limit.

Executes children effects on every scientist (or "random_select_amount" of random every_scientist Within country scope only Character every_scientist = { … } 1.15

character if specified) of the country in scope, that fulfills the "limit" trigger.

random_scientist Within country scope only Character random_scientist = { … } Executes children effects on random scientists that fulfills the "limit" trigger. 1.15

Executes children effects on every active scientist (or "random_select_amount" of random every_active_scientist Within country scope only Character every_active_scientist = { … } 1.15

character if specified) of the country in scope, that fulfills the "limit" trigger.title.

random_active_scientist Within country scope only Character random_active_scientist = { … } Executes children effects on random scientists that fulfills the "limit" trigger. 1.15

party_leader = { Executes the effects on the party leader with the specified ideology type. Must contain a

party_leader Within country scope only Character         has_ideology = liberalism has_ideology  in the limit that refers to a specific ideology type (e.g. Despotic), not a 1.11

}

every_collection_element = {

be a collection name or an inline

definition of a collection

Applies arbitrary effects to all elements of a collection. To learn more about collections, see every_collection_element Always usable Collection/Any         limit = { 1.17

execution to a subset of elements

}

NOTE: Some of these scopes may have no countries/states that match the criteria.

Effects with scopes ​[ edit | edit source ]

Effects that change the scope include the following:

start_civil_war, which changes it to the rebelling dynamic country. create_dynamic_country, which changes it to the newly-created dynamic country.

Dual scopes ​[ edit | edit source ]

The following scopes can be used either as effect or trigger scopes; some can also be used as the right side of some effects and triggers as a target. If usage as a target is possible, it's marked within the table.

Several dual scopes may have a scope that varies depending on where it's used, such as variables, which can be set to anything.

Dual scopes: [Collapse]

Usable Version

Name Usage Target type Example Description as Added

target The country defined by the tag or tag alias. Tag aliases are defined in /Hearts of Iron IV/common/country_tag_aliases, as a way to refer to a specific country (such

TAG Always usable Country scope SOV = { country_event = my_event.1 } ✓ 1.0 as a side in a civil war) in addition to its actual tag. If the country with the exact tag doesn't exist, but a dynamic country originating from the specified tag does, the scope will refer to the dynamic country.

<state_id> Always usable State scope 123 = { transfer_state_to = SCO } The state defined by this id. ✓ 1.0

not within Character ENG_theodore_makhno = { set_nationality = On game versions prior to 1.12.8, the character must be already recruited by the country this is scoped <character> Character scope ✓ 1.11

scope UKR } from.

The MIO identified by that ID as defined within the Within country scope

mio:<MIO> MIO scope mio:AST_cockatoo_doe_organization = { … } /Hearts of Iron IV/common/military_industrial_organization/organizations/*.txt ✓ 1.13 only

file.

sp: Within country scope The special project identified by that ID as defined within the Special project scope sp:sp_land_flamethrower_tank = { … } ✓ 1.15

<special_project> only /Hearts of Iron IV/common/special_projects/projects/*.txt file.

ENG = {

ROOT Always usable Depends on usage                 type = annex_everything ✓ 1.0 distinguish between the default scope and ROOT, such as certain scripted GUI contexts or certain on

} #GER declares war on ENG (if there is

no scope before ENG)

Targets the current scope where it's used. For example, when used in every_state, it will refer to the state that's currently being evaluated. Primarily useful for variables (as in the example, where omitting it

THIS Always usable Depends on usage set_temp_variable = { target_country = wouldn't work) or for built-in localisation commands, where some scope must be specified. More rarely, ✓ 1.0 THIS }

this may help with scope manipulation when using PREV. Since omitting it makes no difference in how the code gets interpreted, there is little to no usage outside of these cases.

FRA = {

Targets the scope that the current scope is contained in. Can have additional applications where the

PREV Always usable Depends on usage                 target = PREV chained indefinitely as PREV.PREV. Commonly results in broken-looking tooltips: what's shown to the ✓ 1.0

} #Germany declares war on random_country

declare_war_on = { Can be chained indefinitely as FROM.FROM. Used to target various hardcoded scopes inherent to the

FROM Always usable Depends on usage } In events, this refers to the country that sent the event (i.e. if the event was fired using an effect, then it's ✓ 1.0 FROM = { the ROOT scope where it was fired). load_oob = defend_ourselves In targeted decisions or diplomacy scripted triggers, this refers to the scope that is targeted. }

Within country scope overlord Country scope overlord = { … } The overlord of the country if it is a subject. Subject to the 'invalid event target' error. X 1.3

only

Within country scope faction_leader Country scope faction_leader = { add_to_faction = FROM } Faction leader of the faction the country is a part of. Subject to the 'invalid event target' error. X 1.10.1

only

In state scope, the country that owns the state. In combatant scope, the country that owns the divisions. In Within state, character,

owner Country scope owner = { add_ideas = owns_this_state } character scope, the country that has recruited the character. Subject to the 'invalid event target' error X 1.0 or combatant scope only

when used for a state.

controller = {

controller Within state scope only Country scope             type = take_state_focus The controller of the current state. Subject to the 'invalid event target' error. X 1.0 generator = { 123 }

}

Within country scope The state where the capital of the current country is located in. Subject to the 'invalid event target' error in capital_scope State scope capital_scope = { … } X 1.0

only rare cases.

event_target: Saved event target or global event target, with no space after the colon. Subject to the 'invalid event target' Always usable Depends on usage event_target:my_event_target = { … } ✓ 1.0

<event_target_key> error.

var:my_variable = { … } Variable set to a scope. var:<variable> Always usable Depends on usage add_to_faction = my_variable  or ✓ 1.5

When used as a target rather than a scope, the var:  can be omitted in most cases. add_to_faction = var:my_variable

Any scope ​[ edit | edit source ]

Can be used in country, state or character scopes.

General ​[ edit | edit source ]

General any-scoped effects: [Collapse]

Name Parameters Examples Description

modifier = <modifier_string>

The name of the Modifier. scope = <country> add_dynamic_modifier = {

If you specify it, your dynamic modifier will be scoped     modifier = example_dynamic_modifier Adds a dynamic modifier to the specified scope (the default scope is ROOT). A add_dynamic_modifier     scope = GER

to this scope. Optional. It will be updated daily, unless forced to update early by force_update_dynamic_modifier effect. days = 14

days = x } s The modifiers will be removed after x days have passed. Optional.

modifier = <modifier_string> remove_dynamic_modifier remove_dynamic_modifier = { modifier = Removes a dynamic modifier from the current scope

The name of the Modifier. sabotaged_ressources }

A <bool>

force_update_dynamic_modifier force_update_dynamic_modifier = yes Forces an update to the effects given by variables within dynamic modifiers. v Boolean.

c

modifier = add_state_resistance_compliance_modifier  =

{ Adds either a resistance or compliance modifier to a state. Can only use modifiers from the <resistance_compliance_modifier> Modifier

add_state_resistance_compliance_modifier        modifier = dynamic_modifier_name /Hearts of Iron IV/common/resistance_modifiers.txt/compliance_modifiers.txt to apply.

state = 738 that are marked as is_dynamic = yes state= <state> Affected state. }

modifier = remove_state_resistance_compliance_modifier

= { Removes either a resistance or compliance modifier from a state. Can only use modifiers from the <resistance_compliance_modifier> Modifier

remove_state_resistance_compliance_modifier        modifier = dynamic_modifier_name /Hearts of Iron IV/common/resistance_modifiers.txt/compliance_modifiers.txt to remove.

state = 738 that are marked as is_dynamic = yes state= <state> Affected state. }

An unique string to identify the global flag with.

OR set_global_flag = my_flag

flag = <flag>

The flag to set. set_global_flag set_global_flag = {

days = <int> Defines a global flag. Sets the flag to last for the specified amount of days.     flag = my_flag '

Optional.     value = 1

value = <int> }

The new value of the flag on the scale from -2 147 483 648 to 2 147 483 647.

T

s <song title from .asset>

play_song play_song = "general_peace_1" Plays an audio track A music file located in the music folder and .asset

T S

The unique string of a global flag to clear.

flag = <flag>

The flag to modify.

value = <value> modify_global_flag = {

The value to add to the flag. Defaults to 0. modify_global_flag     flag = my_flag Adds an integer value to a flag. T

days = <int>     value = 3

The amount of days that the flag should last for }

before being cleared. Optional, defaults to permanent.

custom_effect_tooltip = my_tooltip_tt

A localized string to display in the tooltip. custom_effect_tooltip = { Displays a localized key in the effect tooltip. A localization_key = my_loc

}

tooltip = <string>

A localized string to display in the tooltip. custom_override_tooltip= {

custom_override_tooltip not_tooltip = <string>     not_tooltip = my_tt_NOT

inside this block. A A localized string to display in the tooltip for NOT     <effects>

block. Optional. }

effect_tooltip = {

effect_tooltip <string>         target = FRA Displays the effects in the tooltip without executing them. }

}

<string> Displays a string in the user directory's /Hearts of Iron IV/logs/game.log file when A log log = "myVariable: [?myVariable]"

An string to in the game.log executed, as well as showing up in the console if it is open when the logging effect was executed.

capital_scope = { <string>

save_event_target_as     save_event_target_as = my_state Saves the current scope as a key. Is cleared once execution ends (i.e. end of event). An unique string to identify the event target with.

}

<string> random_other_country = {

save_global_event_target_as An unique string to identify the global event target     save_global_event_target_as = Saves the current scope as a key. Persists after execution until cleared via effect. my_country

with. }

The unique string of the global event target to clear.

yes clear_global_event_targets clear_global_event_targets = yes Clears all global event targets.

Boolean.

T

A sound reference from an .asset file. T s

A seed integer.

set_province_name = {

id = <id>

The id of the province to be changed. }

set_province_name Changes the specified province/victory point's name to the specified name. name = <string>

The name to change the province to. set_province_name = { id = 325 name = "New

Name" }

The id of the province to reset.

province = <id>

Province where to damage units.

state = <id>

State where to damage units. region = <id>

Strategic region where to damage units. limit = { <triggers> }

Will only delete units if the triggers within are met for the country that owns the units. damage_units = {

damage = <fraction>

The percentage of damage done to units.     region = 5

org_damage = <fraction>     limit = { has_country_flag = TAG_test }

The percentage of damage done to units to     damage = 0.5

damage_units     org_damage = 0.5 Damages units in the specified area. organisation in particular.

str_damage = <fraction>     ratio = yes

The percentage of damage done to units to strength     template = "template_name"

in particular.     army = no

ratio = <yes>

} Will damage a ratio damage to total organisation/strength of unit if set. template = <string>

If specified, requires the template name to match. army = <bool>

Will damage the army units. navy = <bool>

Will damage the navy units.

entity = <gfx_entry>

The entity to spawn, defined within /Hearts of Iron IV/gfx/entities/*.asset

files.

id = int

A number ID which can be referred to by other effects. Optional. var = <variable>

If provided, the id of the entity will be stored using this variable. Optional. x = <int> create_entity = {

The X position of the entity.     entity = entity_name

y = <int>     id = 123

The Y position of the entity.     var = var_name

z = <int>

The Z position of the entity.

create_entity     z = 3 Creates an entity. d province = <int>     province = 123

The province the middle of which to use as the     state = 42 d rotation = 1.2 d

entity's position. scale = 10.0

state = <int>     min_zoom = 100.0

The state the middle of which to use as the entity's     visible = scripted_trigger_name

position. }

rotation = <decimal>

The rotation of the entity in radians. scale = <decimal>

The size of the entity. min_zoom = <decimal>

Minimum zoom level needed to be able to see the entity. visible = <scripted_trigger>

The scripted trigger that must be met for a country for it to see the entity.

The ID of the entity to destroy.

id = <ID>

The ID of the entity to modify.

ratio = <int>

Distance between starting position and target position where the entity is to be placed.

set_entity_movement = { rotation = <int>

The rotation to apply after the positioning.     start = {

start and target arguments:         x = 42

x = <int>

set_entity_movement The X position of the point. c } Sets the position and rotation of an entity using two coordinates.

y = <int>     target = {

The Y position of the point.

z = <int>     ratio = 0.5

The Z position of the point.     rotation = 1.2

} province = <int>

The province the middle of which to use as the point. state = <int>

The state the middle of which to use as the point.

set_entity_position = { id = <id>

id = 123 x = <int>

x = 42

y = <int> set_entity_position   y = 21 Sets the position of an existing entity

z = <int>   z = 3 c province = 123

province = <int> state = 42

state = <int> }

id = <ID>

The ID of the entity to modify. set_entity_rotation = {

set_entity_rotation     id = 123 Sets the currently-facing angle of an existing entity. c rotation = <decimal>     rotation = 0.23

a The new angle in radians. }

id = <ID>

The ID of the entity to modify. set_entity_scale = {

set_entity_scale   id = 123 Sets the size of an existing entity. scale = <decimal>   scale = 5.0

The scale to change the entity to. }

id = <int>

The ID of the entity to modify. set_entity_animation = {

set_entity_animation     id = 123 Sets the animation of a specified entity. animation = <animation_type>     animation = "shoot_lasers" t

The animation entry to apply. }

build_railway = {

level = <int>

Defaults to 1             add = 2

build_only_on_allied = <bool>         }

No by default, if yes and in a country scope, it will

only build on allied territories for the country scoped.     path = { 42 10 20 30 40 84 }

fallback = <bool>     start_province = 42

Defaults to no, if yes each option will try to fallback to     target_province = 84

the next available one. }

build_railway path = { <list of provinces> } Adds a railway level between two provinces or along a predefined path. start_province = <int>

build_railway = { target_province = <int>     level = 1

start_state = <int>     build_only_on_allied = yes

target_state = <int>     controller_priority = {

If using start state/target state, the game will pick the         base = 1

provinces with the best supply available. If using

state province/target province, the game will link             add = 2

those two provinces.         }

}

The name of the option. mtg_usa_civil_war_fascists.1.a

seller = <country>

The seller in the contract.

buyer = <country>

The buyer in the contract. create_purchase_contract = {

civilian_factories = <int> seller = ROOT

The amount of civilian factories required by the     buyer = FROM

contract.     civilian_factories = 2

create_purchase_contract     equipment = { Creates a purchase contract with the specified parameters. A equipment = { ... }

The equipment that the contract is for. In particular,

contains these attributes:     }

} type = <archetype>

The archetype of the equipment. amount = <int>

The amount of the specified equipment.

Border wars ​[ edit | edit source ]

These effects refer to the border wars that simulate combat on a border between two countries, with provinces where it takes place being highlighted in white. For the state-based border wars represented with orange stripes on states, see set_border_war in the state scope.

Border war-related any-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added change_state_after_war = <bool>

Whether the state changes hands after the war. start_border_war = {

Attacker or Defender scope     change_state_after_war = no

state = <id> / <variable>     attacker = {

The state the side is fighting on.         state = 527

num_provinces = <id>

The number of provinces used in the state.         on_lose = japan_border_conflict.3

on_win = <id>         on_cancel = japan_border_conflict.4

The event to fire for the side on a win.         modifier = 0.1 Starts a border war for the specified attacker and start_border_war         dig_in_factor = 0

on_lose = <id> defender. The participating countries are the owners   1.5 terrain_factor = 0

The event to fire for the side on a loss.     } of the specified states. on_cancel = <id>     defender = {

The event to fire for the side on a draw.         state = 408

modifier = <decimal>

The modifier on combat. Defaults to 0.

dig_in_factor = <decimal>         on_cancel = japan_border_conflict.4

The modifier applied to dig-in bonuses. Defaults to 1.     }

} terrain_factor = <decimal>

The modifier applied to terrain bonuses. Defaults to 1.

attacker = <id> / <variable>

The attacker state.

defender = <id> / <variable> set_border_war_data = {

The defender state. attacker = 527 Sets the bonuses or penalties for the attacker and

attacker_modifier = <id> / <variable> set_border_war_data     defender = 408 defender in an on-going border war. Used after   1.5

The modifier applied to attacker strength.     defender_modifier = 0.15

defender_modifier = <id> / <variable>

} The modifier applied to attacker strength. combat_width = <id> / <variable>

The combat width used in the border war battle.

attacker = <id> / <variable>

The attacker state. cancel_border_war = {

defender = <id> / <variable>

cancel_border_war     defender = 408 Cancels an on-going border war without a winner.   1.5 The defender state.

dont_fire_events = <bool> }

Stops the events from start_border_war from firing.

attacker = <id> / <variable>

The attacker state.

defender = <id> / <variable> finalize_border_war = {

The defender state.     attacker_win = yes

finalize_border_war     attacker = 527 Ends an on-going border war.   1.5 attacker_win = <bool>

Makes the attacker the winner. }

defender_win = <bool>

Makes the defender the winner.

Variables ​[ edit | edit source ] This section is transcluded from Data structures § Operators

The following is a list of variable-related effects and triggers. Variable-modifying effects have an equivalent for temporary variables, with temp_variable  being used instead of variable , and these temporary variable operators are also valid triggers, as described above. Every operator can be used with variables that do not exist, assuming a value of 0 unless a null-coalescing operator is used.

Variable-related arguments: [Collapse] Name Parameters Examples Description Notes

set_variable = { var = <variable>     var = my_variable

The variable to modify or create.     value = 100

value = <decimal>/<variable> Sets a variable's value to the specified

set_variable } set_variable = { <variable> = The value to set the variable to. amount, creating it if not defined.

<value> } . tooltip = localisation_key Localisation used by

the operation. Optional. set_temp_variable = { temp_var =

ROOT.overlord }

var = <variable>

The variable to modify or create. set_variable_to_random = {

min = <decimal>     var = random_num Sets a variable's value to the specified Shortened version exists with

The minimum possible value, defaults to 0.     max = 11 amount, creating it if not defined. The integer = yes set_variable_to_random =

set_variable_to_random max = <decimal> result will be greater than or equal than }

<variable> , setting it to a decimal between The maximum possible value, defaults to 1. the minimum and strictly less than the

0 and 1. Can be used in triggers. integer = <bool> maximum.

Sets if the variable must be an integer or if it can be set_temp_variable_to_random = my_var

decimal. Defaults to false.

<variable> Clears the value from the memory clear_variable clear_variable = my_variable Can only be used on regular variables.

Variable to clear. entirely.

add_to_variable = { var = <variable>     var = my_variable

The variable to add to.     value = 100

value = <decimal>/<variable>

add_to_variable } specified amount, creating it if not add_to_variable = { <variable> = The value to add to the variable.

defined. <value> } . tooltip = localisation_key Localisation used by

the operation. Optional. add_to_temp_variable = { temp_var =

num_owned_states }

subtract_from_variable = { var = <variable>     var = my_variable

The variable to subtract from.     value = 100 Shortened version exists with tooltip = sub_100_from_var_tt Decreases a variable's value by the

value = <decimal>/<variable> subtract_from_variable = { subtract_from_variable } specified amount, creating it if not

The value to subtract from the variable. <variable> = <value> } . Equivalent to defined.

tooltip = localisation_key Localisation used by adding a negative amount. the operation. Optional. subtract_from_temp_variable = { temp_var =

num_owned_states }

multiply_variable = { var = <variable>     var = my_variable

The variable to multiply.     value = 100

value = <decimal>/<variable> Multiplies a variable's value by the

multiply_variable } multiply_variable = { <variable> The value to multiply the variable by. specified amount.

= <value> } . tooltip = localisation_key Localisation used by

the operation. Optional. multiply_temp_variable = { temp_var =

num_owned_states }

divide_variable = { var = <variable>     var = my_variable

The variable to divide.     value = 100

value = <decimal>/<variable> Divides a variable's value by the specified

divide_variable } divide_variable = { <variable> = The value to divide the variable by. amount.

<value> } . tooltip = localisation_key Localisation used by

the operation. Optional. divide_temp_variable = { temp_var =

num_owned_states }

modulo_variable = { var = <variable>     var = my_variable

The variable to modulo.     value = 50

value = <decimal>/<variable>

modulo_variable } remainder of Euclidean division of the modulo_variable = { <variable> = The value to modulo the variable by.

variable by the specified value. <value> } . tooltip = localisation_key Localisation used by

the operation. Optional. modulo_temp_variable = { temp_var =

num_controlled_states }

round_variable = my_variable If exactly between two integers (Such as 1.5), <variable> Rounds the variable towards the closest

round_variable the option with lager absolute val gets chosen The variable to round. integer value.

( if -1.5,will be -2 ). round_temp_variable = temp

clamp_variable = {

var = <variable>     var = my_var Either min or max can be omitted, in which

The variable to clamp.     min = 0

} Clamps the variable to ensure its value is case it'll not be checked. Does nothing if the min = <decimal>/<variable> between the two specified numbers, variable is already in the range between min

clamp_variable The minimum value of the variable after the clamp. raising to the minimum if smaller or and max. This only changes the current max = <decimal>/<variable> clamp_temp_variable = { lowering to the maximum if larger. value of the variable, it can still go beyond

The maximum value of the variable after the clamp.     var = my_var the minimum or the maximum after the clamp. min = 0

}

var = <variable>

The variable to modify or create. career_profile_set_temp_playthrough_variable

career_profile_set_temp_playthrough_variable = { Sets a temporary variable to a value or ???

value = <decimal>/<variable>   sum = rocket_sites_built_1936 another variable. The value to set the variable to. }

var = <variable>

The variable to modify or create. career_profile_set_temp_variable = {

variable to a value or career_profile_set_temp_variable   var = num_dogs Sets a temporary

??? value = <decimal>/<variable>   value = num_dogs_in_career_profile another variable.

The value to set the variable to. }

Arrays ​[ edit | edit source ] See also: Arrays

Effects for modifying arrays: [Collapse] Name Parameters Examples Description Notes

array = <array>

The array to modify. add_to_array = {

value = <decimal>/<variable>     value = THIS.id Adds an element to the array either at the specified Shortened version exists with add_to_array = {

add_to_array The variable to add. } index, defaulting to the end otherwise. <array> = <value> } .

index = <integer>

The index to place the variable on in the array. Optional, defaults to the end of the array. add_to_temp_array = { temp_states = THIS }

array = <array> remove_from_array = {

The array to modify.     array = global.my_countries

value = <decimal>/<variable> } Removes an element from the array with the remove_from_array = { <array> =

remove_from_array The variable to remove. Optional. specified value or index. <value> } . If neither value nor index are index = <integer> specified, then the last element is deleted.

The index to remove the variable from in the array. Optional. remove_from_temp_array = { temp_states = THIS

}

clear_array = global.my_countries

The array to clear. clear_temp_array = temp_states

array = <array> resize_array = {

The array to modify.     array = global.countries_by_states

value = <decimal>/<variable>

resize_array The variable to add to the array if the size is larger than the array's

} the end if necessary. <array> = <size> } . current size. Optional, defaults to 0. size = <integer>

The amount of elements inside of the array after the resizing. resize_temp_array = { temp_states = 20 }

array = <array>

The array to modify. find_highest_in_array = {

value = <variable>     array = global.countries_by_states Finds the largest value in the array and assigns its find_highest_in_array The temporary variable where the largest value will get stored.     value = temp_largest_country Either value or index are optional to specify.

value and index to a temporary variable. index = <variable>     index = temp_country_index

The temporary variable where the index of the largest value will }

get stored.

array = <array>

The array to modify. find_lowest_in_array = {

value = <variable>     array = global.countries_by_states Finds the smallest value in the array and assigns its find_lowest_in_array The temporary variable where the smallest value will get stored.     value = temp_largest_country Either value or index are optional to specify.

value and index to a temporary variable. index = <variable>     index = temp_country_index

The temporary variable where the index of the smallest value will }

get stored.

Country scope ​[ edit | edit source ]

The effects here must be used within a country scope.

General ​[ edit | edit source ]

General country-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added <flag>

An unique string to identify the country flag with.

OR set_country_flag = my_flag

flag = <flag> No tooltip is shown. The flag in this effect is used in the

The flag to set. meaning of 'boolean flag', used to store information. In

set_country_flag days = <int> set_country_flag = { Defines a country flag. 1.0 Sets the flag to last for the specified amount of days.     flag = my_flag order to change the flag that represents the country,

Optional.     value = 1

value = <int> }

The new value of the flag on the scale from -2 147 483 648 to 2 147 483 647.

The unique string of a country flag to clear.

flag = <flag>

The flag to modify.

value = <value> modify_country_flag = {

modify_country_flag The value to add to the flag. Defaults to 0.     flag = my_flag Adds an integer value to a flag. The flag must be already set. 1.3 value = 3

days = <int> }

The amount of days that the flag should last for before being cleared. Optional, defaults to permanent.

id = <event>

The event to fire.

days = <int> / <variable>

Fires the event in the specified number of days. country_event = {

Optional.     id = my_event.1

hours = <int> / <variable>

Fires the event in the specified number of hours. Fires the specified event for the current

country_event     random_days = 10 mean_time_to_happen  for scheduling events. 1.0 Optional. } country.

Shortened variant exists if the event's ID is used instead random_hours = <int> / <variable>

of arguments. Adds a random number (between 0 and random_hours, inclusive) of hours to the scheduled fire time. Optional. country_event = my_event.1

random_days = <int> / <variable>

Adds a random number (between 0 and random_days, inclusive) of days to the scheduled fire time. Optional.

id = <event>

The event to fire.

days = <int> / <variable>

Fires the event in the specified number of days. news_event = { The news event uses a different interface to the country Optional.     id = my_event.1 event.

hours = <int> / <variable>

Fires the event in the specified number of hours. Fires the specified news event for the current

news_event     random_days = 10 random  can be a performance light alternative to 1.0 Optional. } country.

mean_time_to_happen  for scheduling events. random_hours = <int> / <variable>

Shortened variant exists if the event's ID is used instead Adds a random number (between 0 and random_hours,

of arguments. inclusive) of hours to the scheduled fire time. Optional. news_event = my_event.1

random_days = <int> / <variable>

Adds a random number (between 0 and random_days, inclusive) of days to the scheduled fire time. Optional.

<string> Makes the current scope use the specified set_cosmetic_tag set_cosmetic_tag = SAF_SOV_communism   1.3

The cosmetic tag to switch to. cosmetic tag, changing name and flag.

<bool> Makes the current scope drop the current drop_cosmetic_tag drop_cosmetic_tag = yes   1.3

Boolean. cosmetic tag they are using.

Boolean. set_rule = { Toggles the special game rules for the current set_rule     desc = TAG_my_rule_description

desc = <localisation key> scope. Note: each rule can only be toggled a Game rule list [Expand] 1.0 can_create_factions = yes

The localisation used as the description for why the rule } few times before a reload is required. is set.

ideology = <ideology group>

Ideology group of the party. set_party_rule = {

desc = <localisation key>     ideology = democratic Toggles the special game rules for the current set_party_rule A description used for the rule. Optional, defaults to     desc = TAG_my_rule_description 1.12

scope's political party. being the same as default.     can_create_factions = yes

} <rule> = <bool>

Rule's new value.

target = <country>

Target of the rule. Currently can_access_market  and

usage_desc = <localisation key> add_relation_rule_override = { can_send_volunteers  are supported. In case of

A description used as the reason for the rule applying. target = SOV Toggles the special game rules for the current overlap, restricting actions is preferred (e.g.

Optional. add_relation_rule_override     usage_desc = TAG_my_rule_description scope in diplomacy towards the specified can_send_volunteers = no  or 1.13

trigger = <scripted trigger>     trigger = my_scripted_trigger country only, if the trigger is met. can_not_declare_war = yes  are preferred over

A scripted trigger deciding when the override should be     can_access_market = yes

} the alternatives). In the scripted trigger, ROOT  is the active. Optional, defaults to always true.

country with the override and FROM  is the target. <rule> = <bool>

Rule's new value.

target = <country>

Target of the rule.

usage_desc = <localisation key> remove_relation_rule_override = {

A description used as the reason for the rule applying.     target = SOV Removes the toggle added with remove_relation_rule_override Optional.     usage_desc = TAG_my_rule_description 1.13

add_relation_rule_override. trigger = <scripted trigger>     can_access_market = yes

A scripted trigger for identifying the relation rule. }

<rule> = <bool>

Rule's new value.

The sound effect must be properly defined in <string> Plays the specified sound once only for the

scoped_sound_effect scoped_sound_effect = "boom" /Hearts of Iron IV/sound/ More info can be 1.6 A sound reference from an .asset file. current country.

found in the Sound modding article.

The song must be defined in a music station in order to <song title from .asset> Plays an audio track for the specified country work. More information can be found in the Music

scoped_play_song scoped_play_song = "general_peace_1" 1.9.3 A music file located in the music folder and .asset only. modding page. If you wish to simply play a sound, the

scoped_sound_effect effect should be used instead.

<id> Moves the camera position over the specified goto_province goto_province = 325 1.0

The id of the province go to. province.

goto_state = 1

<state> / <variable> Moves the camera position over the specified goto_state 1.0

The id of the state go to. state. goto_state = var:some_state

change_tag_from = ROOT The country the player becomes needs to be the Switches the player to the current scope from

<country> / <variable> scope in which the command is used. For example, change_tag_from the target scope. Nothing happens if the target 1.0

The country to change from. ABC = { change_tag_from = XYZ }  will make scope is controlled by AI.

change_tag_from = var:from.country the player controlling XYZ play as ABC instead.

Reserves the dynamic country, making sure Usually used in combination with

reserve_dynamic_country <bool> reserve_dynamic_country = yes that it does not get recycled for civil war even if 1.9 create_dynamic_country.

it does not exist.

limit = { ... } force_update_map_mode = {

Triggers required for the map mode to refresh.     limit = { Forcefully refreshes the specified mapmode for force_update_map_mode Optional.         is_ai = no Map modes are defined in

the player, rather than waiting for a daily 1.11 } /Hearts of Iron IV/common/map_modes/*.txt

mapmode = <id>     mapmode = my_map_mode update. The ID of the custom map mode. }

type = <type>

The type of strategy. add_ai_strategy = {

id = <country>

add_ai_strategy     id = GER Sets an AI strategy for the current scope. See AI Modding for more details. 1.0 What country the strategy is against.

value = <int> }

The weighting added by the strategy.

The reserve_dynamic_country effect can be used if the original_tag = <tag>

dynamic country does not yet exist in order to ensure The original tag to be used by the country.

that it does not get overwritten by other creations of create_dynamic_country = {

copy_tag = <tag> original_tag = POL dynamic countries. If this is not done, the dynamic

If specified, copies stuff from this tag rather than the create_dynamic_country     copy_tag = SOV Creates a new dynamic country, akin to ones country will immediately stop existing if no states are 1.9

original tag.     add_political_power = 100 used in civil wars. transferred in the same scope. transfer_state = 123

Effects that will be executed on the new dynamic as a dynamic country's core: if that's unneeded, the

country. cores would need to be removed after creation.

States ​[ edit | edit source ]

These effects in particular are country-scoped effects that are related to states rather than effects within the state scope.

State-related country-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added

<state> / <variable> Adds a core for the current scope to the specified add_state_core add_state_core = 345   1.0

The state to add core to. state.

<state> / <variable> Removes the core of the current scope from the remove_state_core remove_state_core = 345   1.0

The state to remove core from. specified state.

set_capital = {state = 345} state = <state> / <variable>

Syntax has been changed in 1.11. The state to make capital.

Makes the specified state the current scope's capital It was "set_capital = 345" set_capital remember_old_capital = no set_capital = { 1.0

state. Old capital is remembered, if not Whether the old capital gets "remembered", making the country   state = 345

specified otherwise. change to it in case the current capital is lost.   remember_old_capital = no

}

<state> / <variable> Adds a claim for the current scope on the specified add_state_claim add_state_claim = 345   1.0

The state to add a claim to. state.

<state> / <variable> Removes a claim of the current scope from the remove_state_claim remove_state_claim = 345   1.0

The state to remove the claim from. specified state.

This can fail to carry over the control, so it's recommended to instead use

<state> / <variable> Makes the current scope the owner of the specified set_state_owner set_state_owner = 345 transfer_state unless transferring the 1.0

The state to change ownership of. state. ownership without transferring over the control.

<state> / <variable> Makes the current scope the controller of the set_state_controller set_state_controller = 345 1.0

The state to change controller of. specified state.

Adds a contested owner to a state. The effect can <state> / <variable>

add_contested_owner add_contested_owner = 42 be used either from a country or a state scope and Can also be used in state scope. 1.15 State to contest.

accepts the other as parameter.

Removes a contested owner to a state. The effect <state> / <variable>

remove_contested_owner remove_contested_owner = 42 can be used either from a country or a state scope Can also be used in state scope. 1.15 State to stop contest.

and accepts the other as parameter.

<state> / <variable> Makes the current scope the owner and controller of transfer_state_to exists as a state- transfer_state transfer_state = 345 1.0

The state to change owner and controller of. the specified state. scoped variant.

A peace conference or the controller <id> Changes the controller of the specified province to being at peace will reset the control of

set_province_controller set_province_controller = 2999 1.0 The province to change controller of. the current scope. the province to the owner unless the

controller is at war with the owner.

Mana ​[ edit | edit source ]

Mana in this usage means political power, stability, war support, and other values in the topbar. Fuel is, instead, in the resources section, while convoys can be added/removed with add_equipment_to_stockpile.

Mana-related country-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added

add_political_power = 100

<int> / <variable> Adds the specified amount of political power to the add_political_power   1.0

The amount to add. current scope. add_political_power = var:my_var

<int> / <variable> Sets the specified amount of political power for the set_political_power set_political_power = 100   1.0

The amount to add. current scope.

<int> / <variable> Adds to the current stability value for the current add_stability add_stability = 0.1 Stability values are between 0 and 1. 1.5

The amount to add. scope.

<int> / <variable> set_stability set_stability = 0.5 Sets the current stability value for the current scope. Stability values are between 0 and 1. 1.5

The amount to add.

<int> / <variable> Adds to the current war support value for the current War Support values are between 0 and add_war_support add_war_support = 0.1 1.5

The amount to add. scope. 1.

<int> / <variable> Sets the current war support value for the current War Support values are between 0 and set_war_support set_war_support = 0.5 1.5

The amount to set. scope. 1.

<int> / <variable> Adds the specified amount of command power to the add_command_power add_command_power = 100   1.5

The amount to add. current scope.

add_manpower = 100000

<int> / <variable> Adds the specified amount of manpower to the add_manpower   1.0

The amount to add. current scope. add_manpower = var:my_var

<float> / <variable> Adds the specified amount of army experience to the army_experience army_experience = 10   1.0

The amount to add. current scope.

<float> / <variable> Adds the specified amount of navy experience to the navy_experience navy_experience = 10   1.0

The amount to add. current scope.

<float> / <variable> Adds the specified amount of air experience to the air_experience air_experience = 10   1.0

The amount to add. current scope.

Politics ​[ edit | edit source ]

Political country-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added ruling_party = <ideology>

The party to set.

elections_allowed = <bool>

Whether elections are allowed. Optional. set_politics = {

last_election = <date>     ruling_party = democratic Before 1.7, included parties = {

When the last election was. Optional.     elections_allowed = no 1.0 set_politics     last_election = "1935.12.17" Sets the political status of the country, including the ... }  for assigning party

election_frequency = <int> (updated election_frequency = 48 ruling party and elections. popularities, which has been moved to

How often in months an election occurs. Optional.     long_name = TAG_party_long 1.7) set_popularities

long_name = <string>     name = TAG_party

The long name of the country's new ruling party, appearing when }

hovering over it. Optional. name = <string>

The name of the country's new ruling party. Optional.

set_popularities = {   democratic = 50

<ideology> = <int>/<variable> 0, set_popularities neutrality = 15 Sets the political party popularities for the current The popularities must add up to 10

1.7 The popularity to set. fascism = 30 scope. otherwise the command will have no

communism = 5 effect. }

Values used are 0 to 1. ideology = <ideology/tag>

The party to change. If using a tag, uses that tag's ruling party. add_popularity = {

add_popularity     ideology = fascism Adjusts the popularity for the specified party in the You can use ideology = ROOT to 1.0

popularity = <int> / <variable>     popularity = -0.5 current scope. The amount of popularity to change. } increase the popularity of the currently

ruling party.

ideology = <ideology>

The party to change. set_political_party = {

set_political_party     ideology = fascism Sets the popularity for the specified political party in   1.0

popularity = <int>     popularity = 50 the current scope. The amount of popularity to set. }

ideology = <ideology>

The party to change. set_party_name = { The name appears in the country

long_name = <string> set_party_name     long_name = Changes the name of the specified political party for politics/diplomacy view, the long name

1.0 The new full name for the party. GER_neutrality_party_kaiserreich_long the current scope. appears in the tooltip when hovering

name = <string> over the party.

} The new short name for the party.

<country> Executes the events in the on_new_term_election hold_election hold_election = ROOT 1.0

The country to hold an election for. on action for the current scope.

Balance of power ​[ edit | edit source ]

Balance of power is defined in /Hearts of Iron IV/common/bop/*.txt files.

Balance of power-related country-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added id = <BoP ID>

Balance of power to set/modify.

left_side = <BoP side ID>

The left side of the BoP. right_side = <BoP side ID> set_power_balance = { Necessary for a balance of power to appear.

The right side of the BoP.     id = my_bop Sets a new balance of power or edits the For the default state, initial_value , set_power_balance     left_side = my_bop_left_side 1.12

set_default = <bool> existing one. left_side , and right_side  directly right_side = my_bop_right_side

Resets the BoP to the initial state defined in the file. } inside of the BoP are read. Optional, defaults to false. set_value = <decimal>

The new value of the BoP. Optional, defaults to not changing the value.

remove_power_balance = { id = <BoP ID>

remove_power_balance     id = my_bop Removes the balance of power in entirety. 1.12 Balance of power to modify.

}

id = <BoP ID>

Balance of power to modify. add_power_balance_value = {

value = <decimal> Pushes the balance of power towards one

add_power_balance_value     value = -0.1 1.12 The value to add. side.

tooltip_side = <BoP side ID> }

The side to show in the tooltip. Optional.

id = <BoP ID>

Balance of power to modify. add_power_balance_modifier = {

add_power_balance_modifier     id = my_bop Applies a balance of power modifier. 1.12 modifier = <static modifier>     modifier = my_static_modifier

The static modifier to apply. }

id = <BoP ID>

Balance of power to modify. remove_power_balance_modifier = {

remove_power_balance_modifier     id = my_bop Cancels a balance of power modifier. 1.12 modifier = <static modifier>     modifier = my_static_modifier

The static modifier to apply. }

remove_all_power_balance_modifiers = { id = <BoP ID>

remove_all_power_balance_modifiers     id = my_bop Cancels all balance of power modifiers. 1.12 Balance of power to modify.

}

id = <BoP ID>

Balance of power to modify. set_power_balance_gfx = {

side = <BoP side ID> Changes the appearance of one of the sides

set_power_balance_gfx     side = my_bop_side /Hearts of Iron IV/interface/*.gfx 1.12 The side whose GFX to change. within the balance of power.

gfx = <sprite> }

The sprite to change the GFX to.

Diplomacy ​[ edit | edit source ]

Diplomatic country-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added

Boolean.

The effect does nothing if the country exists. All states that are cored by the specified country will be given to it. If the current country has a core on a state transferred to the released country,

<country> Releases the specified non-existent country as a free nation within release release = GER the core will be lost. If looking to make a subject into an 1.0

The target country. the current country's owned states. independent nation, use set_autonomy. States that are owned but not controlled will be transferred to the released country, but won't be controlled by it.

The effect does nothing if the country exists. All states that are <country> Releases the specified non-existent country as a free nation within cored by the specified country will be given to it. If the current

release_on_controlled release_on_controlled = GER 1.9.1 The target country. the current country's controlled states. country has a core on a state transferred to the released country,

the core will be lost.

The effect does nothing if the country exists. All states that are cored by the specified country will be given to it. If the current

<country> Releases the specified non-existent country as a puppet of the release_puppet release_puppet = GER country has a core on a state transferred to the released country, 1.0

The target country. current scope within the current country's owned states. the core will be lost. States that are owned but not controlled will be transferred to the released country, but won't be controlled by it.

The effect does nothing if the country exists. All states that are <country> Releases the specified non-existent country as a puppet of the cored by the specified country will be given to it. If the current

release_puppet_on_controlled release_puppet_on_controlled = GER 1.9.1 The target country. current scope within the current country's controlled states. country has a core on a state transferred to the released country,

the core will be lost. target = <country>

/ <variable>

The subject country. The effect does nothing if the country exists. All states that are autonomy_state = release_autonomy = { cored by the specified country will be given to it. If the current <type>     target = VIN Releases the specified non-existent country as a subject of the country has a core on a state transferred to the released country,

release_autonomy The type of autonomy     autonomy_state = specified autonomy of the current scope within the current country's the core will be lost. States that are owned but not controlled will be 1.3 autonomy_puppet

state to set.     freedom_level = 0.5 owned states. transferred to the released country, but won't be controlled by it. freedom_level = } The autonomy states are found in <float> /Hearts of Iron IV/common/autonomous_states/*.txt. The new freedom level value. Optional.

The target country.

The target country.

recall_attache The target country with recall_attache = GER Recalls the current scope's attaché from the specified country.   1.5 an attache.

country =

<country> Possible relations: The target country to alter non_aggression_pact the relationship with diplomatic_relation = { guarantee ROOT.     country = SOV Used to define a diplomatic relation between the current scope and puppet

diplomatic_relation     relation = guarantee 1.0 relation = <type> target scope country. military_access

The relation to change. } docking_rights active = <bool> embargo

Whether the relation is OFFER_AIR_BASE_ACCESS started or broken.

target = The current scope gains the specified opinion modifier towards the <country> target scope. Can also be used to modify trade relations by adding The target country. add_opinion_modifier = { 'trade = yes' in the opinion <modifier> in

Opinion modifiers are found in add_opinion_modifier     target = GER

modifier = /Hearts of Iron IV/common/opinion_modifiers/*.txt. 1.0**modifier = faction_traitor /Hearts of Iron IV/common/opinion_modifiers/*.txt.

<modifier> } If used with a trade opinion_modifier the behaviour is reversed, The opinion modifier to meaning that the target gains the trade opinion towards the current add. scope.

target =

The target country. remove_opinion_modifier = {

The current scope loses the specified opinion modifier towards the Opinion modifiers are found in remove_opinion_modifier     target = GER

modifier = 1.0 modifier = faction_traitor target scope. /Hearts of Iron IV/common/opinion_modifiers/*.txt.

<modifier> }

The opinion modifier to remove.

target =

The target country. reverse_add_opinion_modifier = { Opinion modifiers are found in reverse_add_opinion_modifier     target = GER The target scope gains the specified opinion modifier towards the

modifier = /Hearts of Iron IV/common/opinion_modifiers/*.txt. 1.0 modifier = faction_traitor current scope.

<modifier> } Useful for when you don't know what the current scope will be. The opinion modifier to add.

target =

The target country. add_relation_modifier = { Relation modifiers are found in target = SWE The current scope gains the specified relation modifier towards the /Hearts of Iron IV/common/modifiers/*.txt files, used

add_relation_modifier modifier =     modifier = 1.4 target scope. to apply a targeted modifier with a non-static target. To change the

<modifier> HUN_dynastic_ties_license lomatic opinion of a country, see add_opinion_modifier.

The relation modifier to } dip

add.

target =

The target country. remove_relation_modifier = { Relation modifiers are found in target = SWE The current scope loses the specified relation modifier for towards /Hearts of Iron IV/common/modifiers/*.txt, used to

remove_relation_modifier modifier =     modifier = 1.4 the target scope. apply a targeted modifier with a non-static target. To change the

<modifier> HUN_dynastic_ties_license diplomatic opinion of a country, see remove_opinion_modifier.

The relation modifier to }

remove.

target =

The target country. add_collaboration = {

add_collaboration     target = TAG Adds collaboration in TAG with the scoped country. 1.9 value = <0-1>     value = 0.3

How much collaboration }

to add.

target =

The target country. set_collaboration = {

set_collaboration     target = TAG Sets the collaboration in TAG with the scoped country. 1.9 value = <0-1>     value = 0.3

How much collaboration }

will be set.

<tag> Recalls volunteers sent to the specified country back to the current recall_volunteers_from recall_volunteers_from = SPR 1.9

The target country. country.

USA = {

GER = {

foreign_civilian_oversight

}

} PREV will be the country for whom the occupation law will be changed. If PREV is not a country, nothing changes. If PREV is the

The new occupation law # Changes USA's occupation law for GER. same country, changes the default occupation law. If PREV is

set_occupation_law enacted by the previous Sets the occupation law of the country. different, default_law resets the country-specific law to the global 1.12 scope or default, otherwise it resets the default law to the occupation law

USA = { with starting_law = yes  in definition.

default_law .   USA = {

default_law

}

}

# Changes the USA's default occupation law to the

default.

USA = {

GER = {

set_occupation_law_where_available

= foreign_civilian_oversight

}

}

The new occupation law possible. set, tries again at every smaller sub-set: if default is impossible,

set_occupation_law_where_available enacted by the previous Sets the occupation law of the country. 1.12 tries every single individual occupied country; if the country's law is

scope or USA = { impossible to change, tries every single state within the country.

default_law . USA = {

set_occupation_law_where_available

= default_law

}

}

# Changes the USA's default occupation law to the

default where possible.

The target country.

As of 1.14.7, this effect ignores country scoping and always applies <tag>

break_embargo break_embargo = ITA Stops embargoing the target country. to the ROOT, instead the diplomatic_relation effect can be used to 1.12 The target country.

break the embargoes of other countries.

The target country.

Faction ​[ edit | edit source ]

Faction-related country-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added

Creates a faction with the specified name for the <loc_key> OBSOLETE, use

create_faction create_faction = MY_FACTION_NAME current scope. The current scope and any 1.0 The name of the faction. create_faction_from_template.

subjects automatically join the faction.

Faction template id. create_faction_from_template =

OR faction_template_GER_mitteleuropa_alliance

template = <string>

The template of the faction. create_faction_from_template = { Create a faction from a template allows for

create_faction_from_template name = <loc_key>    template = 1.17 optional customization of name, icon and color.

The name of the faction. faction_template_defensive_democratic

icon = <sprite>    name = AUS_alpine_federation

The icon of the faction.    icon = GFX_faction_logo_generic_2

color = { 100 100 150 } color = <int>

}

The color of the faction in RGB format.

add_to_faction The TAG of the nation to add to the faction of the current add_to_faction = GER 1.0 scope.

scope.

Boolean.

<bool> Removes the current scope from the faction they leave_faction leave_faction = yes 1.5

Boolean. are part of.

<scope> Removes the specified scope from the faction remove_from_faction remove_from_faction = GER 1.0

The target country. led by the current scope.

set_faction_name Sets a faction name as the loc name. set_faction_name = SOME_LOC_KEY Changes faction names. 1.6

set_faction_leader <bool> Boolean. set_faction_leader = yes Sets the current country as the faction leader. 1.0

Sets the current country as the faction set_faction_spymaster <bool> Boolean. set_faction_spymaster = yes 1.9

spymaster.

Faction rule id.

<string> Changes current country's faction manifest, the set_faction_manifest set_faction_manifest = faction_manifest_id 1.17

Faction manifest id. previous manifest is removed.

The goal of the faction. faction_goal_an_armored_fist

The goal of the faction. faction_goal_secure_the_oil_supply

category = <string>

The category of the faction goal. add_faction_goal_slot = {

to the faction for a specific add_faction_goal_slot     category  = short_term Adds extra goal slots

1.17 value = <int> / <variable>     value = 1 category.

A value of the faction goal slot. }

<float> / <variable> Adds influence to the country based on the given add_faction_influence_ratio add_faction_influence_ratio = 0.075 1.17

The amount to add. ratio of the faction’s total influence.

<int> / <variable> add_faction_influence_score add_faction_influence_score = 5 Adds influence to the country in the faction. 1.17

The amount to add.

<int> / <variable> Adds Faction Initiative points to the current add_faction_initiative add_faction_initiative = 1 1.17

The amount to add. country’s faction.

<int> / <variable> add_faction_power_projection add_faction_power_projection = 100 Adds power projection to the faction. 1.17

The amount to add.

<string> Set either a member upgrade for the specified set_faction_upgrade set_faction_upgrade = token 1.17

Faction upgrade id. tag.

set_faction_member_upgrade_min = { upgrade = <string> Set a faction's minimal requirements for an

set_faction_member_upgrade_min     upgrade = TOKEN_TO_FACTION_MEMBER_UPGRADE 1.17 Faction upgrade id. faction member upgrade group.

}

<bool> Sets wheter the current countries faction can set_faction_military_unlocked set_faction_military_unlocked = yes 1.17

Boolean. make changes to the faction research section.

<bool> Sets wheter the current countries faction can set_faction_research_unlocked set_faction_research_unlocked = yes 1.17

Boolean. make changes to the faction research section.

Autonomy ​[ edit | edit source ]

Autonomy-related country-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added <country>

The target country.

OR puppet = GER The autonomous state picked is one which contains default =

target = <country> yes  and where allowed = { ... }  is fulfilled within the

The target country. Makes the specified country a subject of the /Hearts of Iron IV/commmon/autonomous_states/

puppet end_wars = <bool> puppet = { 1.0 current scope. definition, rather than necessarily being autonomy_puppet.

Whether the target country will peace out in all of its     target = ITA Results in a crash-to-desktop if the game is unable to find any

non-civil wars it's participating in. Defaults to true.     end_wars = no

} such autonomous states. end_civil_wars = <bool>

Whether the target country will peace out in all of its civil wars it's participating in. Defaults to true.

<country> Removes the subject status between the end_puppet end_puppet = GER Must be used within the overlord's scope. 1.0

The target country. target and the current scope.

value = <float> add_autonomy_ratio = {

The freedom score to add.     value = 0.1 Adds a freedom score ratio modifier to the add_autonomy_ratio     localization = Used in the subject's scope. 1.3

localization = <string> current scope. AST_adopt_westminster

The localization key for the modifier. }

value = <float>

The freedom score to add. add_autonomy_score = {

add_autonomy_score     value = 10 Adds an exact freedom score modifier to Used in the subject's scope. 1.3

localization = <string>     localization = EXAMPLE the current scope. The localization key for the modifier. }

target = <country> / <variable>

The subject country. The autonomy_free state will free the subject, however this effect autonomous_state = <type> has to be executed within the scope of the target country's

set_autonomy = { The type of autonomy state to set. current overlord for this to have effect. The autonomy states are

freedom_level = <float> found in set_autonomy     autonomous_state = autonomy_free Sets the autonomy level for the specified

1.3 The new freedom level value. Optional.     end_wars = no country, including independence. /Hearts of Iron IV/common/autonomous_states/*.txt

end_wars = <yes/no> files. Although end_wars is an optional argument defaulting to no,

} Will end any wars the subject is involved in. omitting it results in the country's occupied states returning to its end_civil_wars = <yes/no> control, stranding enemy units.

Will end any civil wars the subject is subject to

Governments in exile ​[ edit | edit source ]

Government in exile-related country-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added

add_legitimacy Adds legitimacy to a government in exile. add_legitimacy = 10 Adds legitimacy.   1.6

set_legitimacy Sets the legitimacy of governments in exile. set_legitimacy = 10 Sets legitimacy.   1.6

Must be fired from ROOT, the country

Makes a country a government in exile in a set country, with a set become_exiled_in = { target = <Host tag> that should be exiled, or a TAG become_exiled_in legitimacy = <0-100> (starting legitimacy, Creates a government in exile. specification must be used. This effect 1.6

starting legitimacy. optional) } would not automatically force a country

to capitulate.

end_exile Ends a government in exile. end_exile = yes Ends a government in exile.   1.6

War ​[ edit | edit source ]

War-related country-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added

The amount to change by.

threat = <int>

The amount to change by. add_named_threat = {

add_named_threat     threat = 5 Adjusts the level of World Tension and adds   1.0

name = <string>     name = GER_rhineland an entry in the World Tension tooltip. The localization string. }

target = <country>

Which country to annex. annex_country = {

Without transfering troops, the annexed country's annex_country     target = GER Annex the specified country for the current

1.0 transfer_troops = yes     transfer_troops = yes scope. divisions' equipment is lost.

Whether to transfer the troops of the annexed country. }

targeted_alliance = <country>

The country to assist. add_to_war = {

enemy = <country> Forces the current scope to join the war of

add_to_war     enemy = HUN   1.0 The country attacking the ally. the specified ally against the specified enemy.

hostility_reason = <string> }

Localization for the reason for joining. Optional.

target = <country> / <variable>

The country to attack. Wargoals are found in declare_war_on = {

type = <wargoal> /Hearts of Iron IV/common/wargoals/*.txt. declare_war_on     target = GER Makes the current scope declare war on the

1.0 The wargoal to declare with.     type = annex_everything specified country with the specified wargoal. See also add_civil_war_target in order to assign a war

} generator = { <state id> } between different countries to be a civil war.

The states to supply the wargoal (i.e. take_state_focus).

<country> / <variable>

The scope to white peace. white_peace = GER

OR Makes the current scope white peace the

white_peace tag = <country> / <variable> white_peace = {   1.0 specified scope.

The scope to white peace.     tag = GER

message = <localisation key>     message = my_peace_tt

The reason for peace showing up in the pop-up. }

tag = <country> / <variable>

The scope to peace with. Current scope is the winner, target and its subjects are

score_factor = <decimal> / <variable> the losers. Can only be used if at war with the target. A The fraction of the total score awarded to the winners score_factor of 0.0 is equivalent to a whitepeace. compared to regular victory.

winner_scope  and loser_scope  have the message = <localisation key>

start_peace_conference = { following possible values: The reason for peace showing up in the pop-up. Optional.     tag = GER Makes the current scope start a peace

ALL : all countries at war with the other side. start_peace_conference winner_scope = <scope type>     score_factor = 0.4 conference with the specified scope on the 1.12

FACTION : all countries in the same faction as the Which countries should be present in the conference on the     message = my_peace_tt other side.

current scope or under its overlordship. winner side alongside the current scope. Optional, defaults to }

LIMITED_FACTION. LIMITED_FACTION : includes faction members if and only if the country is a faction leader, and

loser_scope = <scope type>

Which countries should be present in the conference on the includes subjects of the country.

loser side alongside the target country. Optional, defaults to LIMITED : includes only subjects of the country.

LIMITED_FACTION.

target = <country>

The scope to truce with. set_truce = {

set_truce     target = GER Makes the current scope truce with the   1.0

days = <int>     days = 90 specified scope. The duration of the truce. }

create_wargoal = { target = <country> / <variable>

The country to target.

type = <wargoal> }

The wargoal to generate. Grants the current scope a wargoal against Wargoal type can be found in

create_wargoal generator = { <state id> } 1.0 /Hearts of Iron IV/common/wargoals/*.txt

The states to supply the wargoal (i.e. take_state_focus). create_wargoal = { the specified country. type = take_state_focus

expire = 365     target = PREV

The amount of days that the wargoal will last before expiring.     generator = { 123 321 }

If unset or set to 0, will never expire.     expire = 90

}

target = <country> / <variable>

The country to target. remove_wargoal = {

Removes wargoals from the current scope to Wargoal type can be found in remove_wargoal     type = all 1.10.2

type = <wargoal>     target = ROOT the specified country. /Hearts of Iron IV/common/wargoals/*.txt

The wargoal to remove. "all" will remove all wargoals. }

ideology = <ideology>

The ideology of the breakaway country. start_civil_war = {

ruling_party = <ideology> ruling_party = communism

Changes the ideology of the original, player-led country, if # Original country's ideology changes

set. Optional. to communism

size = <float>     ideology = ROOT

The size of the breakaway country and the fraction of the     # Breakaway gets old ideology of ROOT

original stockpile and military units it will receive by default.

Optional, defaults to 0.5.     states = {

army_ratio = <float>         282 533 536 555 529 530 528

The size of the land army that the breakaway country gets.     }

Optional, defaults to being the same as size.     keep_unit_leaders = {

navy_ratio = <float>     }

The size of the naval forces that the breakaway country gets.     keep_political_leader = yes

Optional, defaults to being the same as size.     keep_political_party_members = yes

} states = all  would include every single state air_ratio = <float>

controlled by the country. If the country's current The size of the airforce that the breakaway country gets.

capital state is set as one of the states that the Optional, defaults to being the same as size.

start_civil_war = { revolt can gain, it won't fire. set_capital can be used capital = <state>     ideology = democratic to change the capital beforehand, with

The capital state of the breakaway country. Optional.     size = 0.1 on_civil_war_end being used to set it back to the

states = { <state> }     states = all default after the civil war ends.

The states included in the breakway country. Optional,     states_filter = {

defaults to random states based off size. all  will result in Elections will always be disallowed for the breakaway.

all states that meet the filter going to the breakaway. If the ruling_party  attribute is used, the original }

states_filter = { <triggers> }     set_country_flag = country will have its elections disallowed. In the base Starts a civil war for the current scope with

start_civil_war A trigger block checked for the state that must be met to be TAG_my_country_tag_alias_trigger game files, an on action is set up to ensure that 1.0 # Sets a country flag that gets used the specified parameters.

transferred to the breakaway. Optional. elections get allowed if the democratic side wins the in a country tag alias.

keep_unit_leaders = { <unit leader id> } civil war. }

List of unit leaders to be kept by their legacy_id. Optional. A civil war started via this effect cannot have more than keep_unit_leaders_trigger = { <triggers> } (See country tag aliases) two sides and the effect cannot be used in history or

Trigger block checked for every unit leader that forces them bookmark's effect = { ... }. For adding more sides or to be kept if they meet the triggers. The default scope is the starting one before the game's start, this can be unit leader, ROOT is the country receiving the unit leader, start_civil_war = {

while FROM is the original owner of the unit leader. Optional.     size = 0.1 originating from a dynamic country created via keep_scientists_trigger = { <triggers> }     army_ratio = 0.5 create_dynamic_country) as a civil war via

Trigger for scientist to remain with the original country.     navy_ratio = 0 add_civil_war_target. air_ratio = 1

keep_political_leader = <bool> keep_unit_leaders_trigger = {

Controls if the promoted party leader (i.e. the one that'd take has_trait = my_trait_name

power if the country were to be switched to that ideology     }

group) of the revolting ideology group will be kept by the     keep_all_characters = yes

country or join the revolt, yes resulting in the former.     PREV = {  # Original country

Optional, defaults to false.

Character keep_political_party_members = <bool>             set_nationality = PREV.PREV

Controls if non-promoted party leaders of the revolting             # Transfers to breakaway

ideology group will be kept by the country or join the revolt,         }

yes resulting in the former. Optional, defaults to false.     }

keep_all_characters = yes TAG_airforce_leader

If true, the revolter will have no characters from the original }

country transferred to them. Optional, defaults to false. <effects> (See usage for PREV and PREV.PREV)

An effect block executed for the breakaway country.

Sets that the war between ROOT and TAG is a civil war, resulting in the victory being the ROOT and TAG must already be at war with each

add_civil_war_target <country>  - The country to set as the target. add_civil_war_target = TAG 1.9 annexation of the other side and setting world other for the effect to take place. tension limits on intervention.

The ongoing war must already be marked as a civil Removes the status of the war as a civil war

remove_civil_war_target <country>  - The country to set as the target. remove_civil_war_target = TAG war, whether it was initiated by start_civil_war or 1.12.13 between the pair of countries.

add_civil_war_target was used to mark it as one.

target = <country>

The country which should receive the units from the current scope.

size = <float>

The size of the breakaway country and the fraction of the original stockpile and military units it will receive by default. Optional, defaults to 0.5.

transfer_units_fraction= { army_ratio = <float>

target = SPD The size of the land army that the breakaway country gets. size = 0.5

Optional, defaults to being the same as size. stockpile_ratio = 0.8

navy_ratio = <float> army_ratio = 0.8 Transfers a fraction of the military to a target, transfer_units_fraction The size of the naval forces that the breakaway country gets. navy_ratio = 0.5 including units (either type: land, navy, or air), 1.9

air_ratio = 0.5 Optional, defaults to being the same as size. keep_unit_leaders_trigger = { equipment, and unit leaders. air_ratio = <float> has_trait =

The size of the airforce that the breakaway country gets. trait_SPA_nationalist_sympathies

Optional, defaults to being the same as size. }

} keep_unit_leaders = { <unit leader id> }

List of unit leaders to be kept by their legacy_id. Optional. keep_unit_leaders_trigger = { <triggers> }

Trigger block checked for every unit leader that forces them to be kept if they meet the triggers. The default scope is the unit leader, ROOT is the country receiving the unit leader, while FROM is the original owner of the unit leader. Optional.

Adds specified number of nukes to the add_nuclear_bombs Adds nuclear bomb to TAG's stockpile. add_nuclear_bombs = 100 Needs the Nuke tech to use. 1.6

country's stockpile

province = <ID>

The specific province to nuke. launch_nuke = {

state = <ID>

} Nukes the specified province or a province in The state to nuke.

the needed state. If a state is set rather than controller = <TAG>

the specific province, first prioritises the If set to use a nuke, then requires at least one nuclear launch_nuke Prioritises provinces controlled by this country. launch_nuke = { 1.6

country set in controller , then prioritises bomb in the stockpile. use_nuke = <boolean>     state = 42

the countries at war with the current scope, Whether a nuke should be deducted from the country's     controller = GER

and then countries that are neutral. stockpile. Defaults to false.     use_nuke = yes

nuke_type = <nuke_type> nuke_type = nuclear_bomb

type of nuke to use (e.g. nuclear_bomb, }

thermonuclear_bomb etc.)

Resources ​[ edit | edit source ]

Resource-related country-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added type = <resource>

The resource to add.

amount = <int> add_resource = {

The amount of resource to add.     type = oil Adds the specified resource in the specified amount add_resource     amount = 50 Can also be used in state scope. 1.0

state = <id> Which state to add the resource to. Variables to the specified state. state = 88

can be used. }

show_state_in_tooltip = <bool>

Whether the state should be shown in the tooltip. Defaults to true.

resource = <resource>

The resource to import. create_import = {

create_import amount = <int>     amount = 100   1.0

specified resource and from the specified exporter. The amount of resource to import.     exporter = GER

} exporter = <id> Which country exports the resource.

receiver = <tag> give_resource_rights = { receiver = ENG state

The country that would get the resource rights. = 291 }

state = <state> The resource rights will only be Gives all the resources of a state to the target

give_resource_rights The state where the resource rights are located. give_resource_rights = { provided as long as the current country 1.6 country

resources = { <resource> <...> <resource> }     receiver = POL controls the state with resource rights. The resources to which give resource rights to. Optional, defaults     state = 321

to all.     resources = { oil }

}

The state to remove current country's resource rights from.

The fuel amount

Fuel amount.

The needed ratio of fuel.

Buildings ​[ edit | edit source ]

Building-related country-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added type = <building>

The building to add. Adds an off-map (offmap) building for the current add_offsite_building add_offsite_building = { type = arms_factory scope that produces its effects without being   1.5

level = <level> / <variable> level = 1 } present in a state.

The maximum level to add.

building = <building>

The building to modify. modify_building_resources = {

resource = <resource> Modifies the resource output of the specified

modify_building_resources     resource = oil   1.5 The resource to add. building for the current scope.

amount = <amount> }

The amount of resource to add.

type = <building>

The building to damage. damage_building = {

state = <id> / <variable> type = infrastructure

The state to target.   state = 123

tags = <building_tag>   damage = 1 The health of buildings is determined The buildings with this tag to damage. }

by the value attribute in a building's tags = { <building_tag> }

damage_building Damages a building in a targeted state or province. definition. This is multiplied by their 1.3 The buildings with these tags to damage. level to get their total health.

damage_building = { repair_speed_modifier = <float>

tags = dam_building Can also be used in state scope. Repair will be x% slower until building is fully repaired   damage = 1

damage = <float>   repair_speed_modifier = -0.8

The amount of damage to inflict.   province = 3488

} province = <id> / <variable>

The province to target for provincal buildings.

National focuses ​[ edit | edit source ]

National focus-related country-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added <focus tree>

The national focus tree to load.

OR load_focus_tree =

tree = <focus tree ID> china_communist_focus

The national focus tree to load. Focuses that aren't present in the newly-loaded tree will not be kept as

keep_completed = <bool> Loads a new focus tree for the current scope, load_focus_tree

Whether focuses shared between the load_focus_tree = { completed for has_completed_focus checks or when loading the old tree 1.5 retaining any shared focuses if set.

tree = british_focus back. old and new trees should stay

keep_completed = yes completed. Defaults to false.   copy_completed_from = ENG

copy_completed_from = <tag> }

Copy completed focus from an existing country.

Bypasses the specified focus for the current scope <focus>

unlock_national_focus unlock_national_focus = my_focus (marks as complete without firing   1.0 The focus to unlock.

complete_effect  of the focus).

The focus to complete.

OR complete_national_focus = my_focus

focus = <focus>

The focus to complete. use_side_message = <bool> complete_national_focus = { Completes the specified focus for the current In 1.15 block version was added, 'originator_name' can be TAG, character,

complete_national_focus 1.0 Create popup notification in the   focus = scope. state, or any other localization key.

GER_autonomous_organization_todt bottom right that includes

use_side_message = yes

originator_name  instead of   originator_name = GER_fritz_todt

normal focus popup. }

originator_name = <string>

Used for tooltip only.

focus = <focus>

uncomplete_children = uncomplete_national_focus = { Removes a focus from list of completed focus, and

<bool>   focus = GER_oppose_hitler potentially all focuses requiring it as a prerequisite. uncomplete_national_focus Defaults "no". Optional.   uncomplete_children = yes 1.11

If the focus has one, the 'on_uncomplete' effect will refund_political_power =   refund_political_power = no

} be executed on each uncompleted focus. <bool>

Defaults "no". Optional.

If put within a focus' completion reward, the focus will not be marked as complete at the time the effect is executed, leading to has_completed_focus  checks specifying that focus in particular to be

Refreshes the focus tree for the specified country, marked as false. <bool>

mark_focus_tree_layout_dirty mark_focus_tree_layout_dirty = yes restarting the checks in allow_branch  and 1.9 Boolean. This can be bypassed by putting an effect within a hidden event fired

position offsets for focuses. immediately within the focus or by reloading the same focus tree with load_focus_tree  set to keep completed focuses, marking the focus as

complete, before using the effect.

Activates the shine effect on the focus with the Tooltips are only shown in debug mode. <focus>

activate_shine_on_focus activate_shine_on_focus = my_focus given id. Focuses that are completed cannot have 1.15 The focus to activate a shine effect on. Can be used to simulate work on more than one focus at a time.

an activated shine effect.

<focus> Deactivate the shine effect on the focus with the deactivate_shine_on_focus The focus to deactivate a shine effect deactivate_shine_on_focus = my_focus given id. The current focus cannot have it's shine Tooltips are only shown in debug mode. 1.15

on. effect removed.

reduce_focus_completion_cost = {

focus = focus_id

cost = 35 focus = <focus>

} The focus to reduce cost time. Reduce the cost needed to complete a specific

reduce_focus_completion_cost focus. The cost accepts script constants. The 1.17 cost = <int> / <variable>

focus can be a uniform list or a single token. Time to reduce (in days). reduce_focus_completion_cost = {

focus = {focus_id_1 focus_id_2}

cost = 35

}

Decisions ​[ edit | edit source ]

Decision-related country-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added

Activates the specified decision for <decision> Decisions are found in

activate_decision activate_decision = my_decision the current scope, ignoring triggers 1.0 The decision to activate. /Hearts of Iron IV/common/decisions/*.txt

for the decision.

target = <country> Decisions are found in The country to target. activate_targeted_decision = { Activates the specified targeted

/Hearts of Iron IV/common/decisions/*.txtOnly works on activate_targeted_decision     target = GER decision for the specified target for 1.5

decision = <decision>     decision = my_decision missions; regular decisions targeted this way become visible but do not the current scope.

The decision to activate. } activate.

remove_targeted_decision = {

1.5 The decision to remove.     decision = my_decision decision for the current scope. /Hearts of Iron IV/common/decisions/*.txt

}

The decision to display.

<show_effect_tooltip> Displays a special tooltip for the Decisions are found in

unlock_decision_tooltip unlock_decision_tooltip = { Show decision effects (default is no) specified decision in the effect 1.5

tooltip.

<show_modifiers>

Show decision modifiers. (default is no) }

Displays a special tooltip for the <category> Decision categories are found in

unlock_decision_category_tooltip unlock_decision_category_tooltip = specified decision category in the 1.5 The decision category to display. my_category /Hearts of Iron IV/common/decisions/catergories/*.txt

effect tooltip.

decision = <decision>

The decision to add days to. add_days_remove  = { Adds the number of days to the add_days_remove     decision = decision_here Decisions are found in

timer created by a decision's 1.9 days = <int> / <variable>     days = 30 /Hearts of Iron IV/common/decisions/*.txt

days_remove. The number of days to add to the decision. }

Allows to remove specified decision without remove_decision remove_decision = GER_MEPO Removes a decision.   1.6

running remove_effect.

If the decision is on cooldown, it <decision>

remove_decision_on_cooldown remove_decision_on_cooldown = gets removed, in order to reactivate 1.11 The decision that is to be removed. TAG_my_decision

or remove completely.

Missions ​[ edit | edit source ]

Mission-related country-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added

<mission> Activates the specified mission for the current Missions are found in activate_mission activate_mission = my_mission 1.5

The mission to activate. scope, ignoring any triggers for the decision. /Hearts of Iron IV/common/decisions/*.txt

<mission> Displays a special tooltip for the specified Missions are found in activate_mission_tooltip activate_mission_tooltip = my_mission 1.5

The mission to display. mission in the effect tooltip. /Hearts of Iron IV/common/decisions/*.txt

<mission> Removes the specified mission for the current Missions are found in remove_mission remove_mission = my_mission 1.5

The mission to remove. scope. /Hearts of Iron IV/common/decisions/*.txt

mission = <mission>

The mission to add days to. add_days_mission_timeout = {

add_days_mission_timeout     mission = my_mission Adds the number of days to the specified Missions are found in 1.9

days = <int> / <variable>     days = 20 mission. /Hearts of Iron IV/common/decisions/*.txt

The number of days to add to the mission. }

Technologies ​[ edit | edit source ]

Technology-related country-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added

Adjusts the number of research slots <int>

add_research_slot add_research_slot = 1 the current scope has. Can remove   1.0 The number of slots to add or remove.

slots with negatives.

<int> Sets the number of research slots the set_research_slots set_research_slots = 4   1.0

The number of slots to set. current scope has.

bonus = <float>

The bonus to technology given, default 0.

uses = <int>

The amount of times the bonus can be used, default 1. ahead_reduction = <float> Research bonus categories are defined in

The cost reduction if ahead of time, default 0. add_tech_bonus = {

Grants a research bonus to the current

add_tech_bonus category = <string> uses = 1 files, while technologies are defined in 1.0

Which technology category the bonus applies to. scope with the specified parameters. category = radar_tech /Hearts of Iron IV/common/technologies/*.txt

Multiple can be defined. } files. technology = <string>

Which technology the bonus applies to. Multiple can be defined.

name = <string>

Tooltip shown in research tabs, optional.

A value of 1 sets the technology. A value of 0 removes the technology, but if it is a researchable technology, the duration it takes to research isn't reset, meaning it can be researched in 1 day. Technologies that are mutually exclusive with other

<technology> = <int> technologies can not be removed by this effect. Technologies The technology to add. set_technology = { Grants the specified technology to the

set_technology are defined in suicide_craft = 1 1.0

popup = no current scope. } /Hearts of Iron IV/common/technologies/*.txt

To not show the popup after adding technology files.

To show the effects of the technology use custom_effect_tooltip = tech_effect|

<technology_token>

<string> Adds the current scope to the specified  Technology sharing groups are found in Hearts of Iron add_to_tech_sharing_group add_to_tech_sharing_group = us_research 1.3

The group to add the current scope to. technology sharing group. IV\common\technology_sharing*.txt

1.3 The group to remove the current scope from. us_research specified technology sharing group. IV\common\technology_sharing*.txt

id = <string>

The group to modify. modify_tech_sharing_bonus = {

modify_tech_sharing_bonus     id = us_research Modifies the specified technology  Technology sharing groups are found in Hearts of Iron 1.3

bonus = <float>     bonus = 0.5 sharing group. IV\common\technology_sharing*.txt

The new bonus. }

Makes the current country's researched inherit_technology <tag>  The country to inherit technology from. inherit_technology = CAN technologies be copied from the Useful when making a country independent. 1.6

specified country.

<bool> Forces the refresh of the hidden mark_technology_tree_layout_dirty mark_technology_tree_layout_dirty = yes 1.15

Boolean. technologies for the scoped country.

Ideas ​[ edit | edit source ]

This includes national spirits, laws, designers, and advisors. (using the idea_token)

Idea-related country-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added

add_ideas = my_idea

<idea> Can be used as a scope to add add_ideas Adds the specified ideas to the current scope. 1.0

The idea to add. add_ideas = { multiple at once. my_idea_1

}

idea = <idea>

The idea to add.

days = <int> / <variable> Either one of days , months , or

The number of days to add the idea for. add_timed_idea = { years  is mandatory. The tooltip will

months = <int> / <variable> add_timed_idea     idea = my_idea Adds the specified ideas to the current scope for the

use the exact same phrasing in 1.0 The number of months to add the idea for. A month is equal to 30     days = 180 specified number of days.

years/months/days as used in the days. }

attributes. years = <int> / <variable>

The number of years to add the idea for. A year is equal to 365 days.

idea = <idea>

The idea to modify.

days = <int> / <variable> Positives add to the time, negatives The number of days to modify the idea by. shorten it. Either one of days ,

modify_timed_idea = {

months = <int> / <variable> months , or years  is mandatory. modify_timed_idea     idea = my_idea Extends or shortens the duration of the timed idea

1.0 The number of months to modify the idea by. A month is equal to     days = 60 by the specified amount. The tooltip will use the exact same 30 days. } phrasing in years/months/days as used years = <int> / <variable> in the attributes.

The number of years to modify the idea by. A year is equal to 365 days.

If the ideas have the same name in the add_idea = <idea> localisation, it will show up as

The idea to add. swap_ideas = { he idea rather than swap_ideas     remove_idea = my_idea_1 Switches two ideas with a tooltip displaying any modifying t

swapping them. 1.3 remove_idea = <idea>     add_idea = my_idea_2 modifier differences between them.

The idea to remove. } The add will occur before the removal of the old idea.

remove_ideas = my_idea

<idea> Can be used as a scope to remove remove_ideas 1.0

The idea to remove. remove_ideas = { Removes the specified idea from the current scope. multiple at once.

}

<trait> Removes all ideas for the current scope that use the remove_ideas_with_trait remove_ideas_with_trait =   1.0

The trait to target. motorized_equipment_manufacturer specified trait.

<idea> Displays the specified idea in the tooltip for the show_ideas_tooltip show_ideas_tooltip = my_idea   1.0

The idea to display. current effect scope. Does not add the idea.

Units ​[ edit | edit source ]

Unit-related country-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added Loads the specified order of battle for the current scope,

Orders of battle are stored within applying the effects

<oob> /Hearts of Iron IV/history/units/*.txt. load_oob load_oob = "GER_default" within. The filename 1.0

The filename of the order of battle to load, without the .txt extension. Primarily used to spawn divisions at specified with the .txt

locations. extension omitted is used as the effect's target.

name

The name of the division.

regiments = {

}

support = {

}

The composition of the division. Sub-units are defined in /Hearts of Iron IV/common/units/*.txt files. division_names_group = <group>

The division names group that the template will use, deciding on the division_template = {

automatically-generated names of any new divisions built using that     name = "Test"

template. Optional, assigns one automatically if omitted. These are     is_locked = yes

defined within     division_cap = 3

/Hearts of Iron IV/common/units/names_divisions/*.txt

files.

is_locked = <bool>     regiments = { The x and y attributes represent the rows and

specified division division_template Whether the division is locked to modification and deletion. Optional.         infantry = { x = 0 y = 0 } columns in the division designer and start from 0. No 1.0

template to the current infantry = { x = 0 y = 1 }

force_allow_recruiting = <bool> tooltip is shown. infantry = { x = 0 y = 2 } scope.

Whether the locked template can have units deployed using it without infantry = { x = 0 y = 3 }

allowing editing. Optional, only has an effect in locked templates.     }

division_cap = <int>     support = {

The maximum amount of divisions that this template may have; requires         military_police = { x = 0 y = 0 }

the template to be locked. Optional.     }

} priority = <int>

The priority the template receives in receiving supplies. Goes from 0 to 2. Optional, 1 by default. template_counter = <int>

The icon used by the division as an integer. Optional, defaults to the icon of the most common sub-unit within. The icons are defined as sprites within any /Hearts of Iron IV/interface/*.gfx file (By default subuniticons.gfx ) with the pattern of GFX_div_templ_<int>*large  and**GFX_div_templ*<int>_small . override_model = <entity>

Enforces the entity used by the units using this template to be the specified one. Optional.

create_colonial_division_template = {

subject = RAJ

division_template = {

subject = <country>     name = "Infantry Division"

Country tag for an overlords subject.     division_names_group = RAJ_INF_01 Create a colonial create_colonial_division_template     ... division template for In country scope of overlord, E.g. ROOT = ENG. 1.15

division_template = { ... }     regiments = { overlord/owner.

The regular effect to create a division template.       infantry = { x = 0 y = 0 }

}

}

template_name = <string>

The template to change. Optional if used in division scope. add_units_to_division_template = {

regiments = {     regiments = { <unit> = <column> brigades to first

} available slots of Columns go left-to-right starting with 0. Can also be

add_units_to_division_template         infantry = 2 1.0 support = {     } specified columns to used in division scope. <unit> = <column>     support = { the template (if }         military_police = 0

possible). }

The units to add to the template. Sub-units are defined in }

/Hearts of Iron IV/common/units/*.txt files.

Toggles the locked division_template = <string> set_division_template_lock = { status on a division

The name of the division template.     division_template = "Infantry template for the set_division_template_lock Division"   1.5

is_locked = <bool> current scope, which is_locked = yes

Whether the division is locked or not. } prevents editing or deletion.

Boolean. country_lock_all_division_template = yes

OR Locks all division Used to prevent training, disbanding, and editing

country_lock_all_division_template is_locked = <bool> country_lock_all_division_template = { templates for the 1.9 units.

Boolean.   is_locked = yes current scope. desc = <loc_key>   desc = loc_key

Tooltip. }

Changes whether it's division_template = <string> possible to recruit

Template to modify. set_division_force_allow_recruiting = {

set_division_force_allow_recruiting     division_template = "My locked divisions of a locked   1.12

force_allow_recruiting = <bool> template" template without Whether to allow or disallow recruiting. Defaults to true if unset. } unlocking the

template.

division_template = <string> set_division_template_cap = { Sets the cap of a

The name of the division template. division_template = "Swiss

set_division_template_cap Citizen Militia" division template. The 1.12

division_cap = <int> division_cap = template has to be The division cap. SWI_militia_division_cap locked first.

}

Clears the cap on the clear_division_template_cap = {

division_template = <string> clear_division_template_cap division_template = "Swiss template, allowing it to

1.12 The name of the division template. Citizen Militia" have an unlimited

} amount of divisions.

delete_unit_template_and_units = { Deletes the specified

division_template = <string> division template and delete_unit_template_and_units Division"   1.5

The name of the division template.     disband = yes #will refund equipment all units using it for the and manpower current scope. }

delete_unit = {

state = <number id>     disband = yes #will refund equipment

The id number of the state the unit must be in. and manpower

}

division_template = <string>

The template the units must use to be deleted. Deletes all units that No tooltip is generated. delete_units can be used if delete_unit 1.5

id = <int> delete_unit = { meet the filters. deleting all units of a specific template. The id given to the unit if created via the     division_template = "Infantry

create_unit  effect. Division"

disband = <bool> }

If true, will refund equipment and manpower.

delete_unit = {} # Will delete all units

division_template = <string> delete_units = {

The template the units must use to be deleted.     division_template = "Infantry Deletes all units with a Generates a tooltip, unlike delete_unit. Mandatory to delete_units Division" 1.9

disband = <bool> certain template. specify a division_template. disband = yes

If true, will refund equipment and manpower. }

equipment = <type>

Equipment type used by the railway gun. create_railway_gun = {

name = <string>     equipment = railway_gun_equipment_1

create_railway_gun The name used by the railway gun. Optional. name = TAG_new_railway_gun Creates a railway gun. 1.11 location = <province> location = 12406

Location where the railway gun is created. Assumes the capital by }

default.

Teleports all railway <bool>

teleport_railway_guns_to_deploy_province teleport_railway_guns_to_deploy_province guns to the province 1.11

Boolean. = yes where they get deployed.

add_unit_bonus = {

category_light_infantry = {

Adds permanent

}

subunit and subunit add_unit_bonus <subunit> = { ... } ???

cavalry = { category bonuses for soft_attack = 0.05 country. hard_attack = 0.05

}

}

Equipment ​[ edit | edit source ]

Equipment-related country-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added

Reduces the overall This should not be used in civil wars to simulate stockpile splitting. <float> / <variable>

set_equipment_fraction set_equipment_fraction = 0.5 equipment stockpile by start_civil_war  automatically divides stockpiles according to the 1.0 The fraction of equipment to remove.

the specified fraction. respective size.

type = <equipment>

The equipment to add. Either types and archetypes are add_equipment_to_stockpile = {

accepted.

amount = <int> / <variable>     producer = GER Edits the equipment The amount to add. } stockpile of the current With negative numbers, optionally specifying a producer will ensure only

add_equipment_to_stockpile producer = <country> / <variable> scope, adds or removes equipment with that producer gets removed. The equipment must be 1.0 Defines who produced the equipment. Optional, defaults to equipment of a specified unlocked by the producer for the effect to succeed.

add_equipment_to_stockpile = { the current scope.     type = medium_tank_chassis_1 type or archetype. variant_name = <string>     amount = 100

The equipment variant to add. Mandatory if a variant needs     variant_name = "Panzer III"

to be created to produce the equipment, optional otherwise. }

type = <equipment>

The equipment to add. Can be archetype. Sends the specified send_equipment = {

amount = <int> / <variable> Cannot remove equipment into negatives, in which case equipment will not

send_equipment     amount = 100 the specified target, 1.0 The amount to add. be received by the target in entirety.

target = <country> / <variable> } from the current scope.

Which country receives the equipment.

value = <0-1> Sends the specified How much equipment to send. send_equipment_fraction = { fraction of equipment to

send_equipment_fraction     value = 0.3 the specified target,   1.9 target = <country> / <variable>     target = GER

removing said equipment Which country receives the equipment. }

from the current scope.

target = <country>

Which country receives the license.

cost_factor = <float>

Modifies the production cost. create_production_license = {

Equipment scope     target = HUN Grants the specified

type = <equipment>         type = fighter_equipment_1 country a license to

create_production_license The equipment the country is licensed to produce. Must be         version = 0 produce the specified   1.4 an non-archetype equipment.         new_prioritised = no equipment from the

version = <int> current scope.

The version indicates which variant should be licensed. The }

default is 0, meaning the base variant. new_prioritised = <boolean>

Whether new equipment is prioritised or not. Yes by default.

add_equipment_subsidy = {

cic = <int>

support_equipment The amount of economic capacity required by the subsidy.     seller_tags = { BHR }

} equipment_type = <archetype>

Creates an equipment seller_tags  and seller_trigger  are mutually exclusive. In the The equipment archetype that the subsidy is for.

add_equipment_subsidy subsidy on the scripted trigger, ROOT  is the country with the subsidy and FROM  is the 1.13 seller_tags = { <countries }

add_equipment_subsidy = { international market. seller. Countries that can have the subsidy.

seller_trigger = <scripted trigger>     equipment_type =

The trigger deciding which countries can have the subsidy. infantry_equipment

my_scripted_trigger

}

Modifies the economic <int> The economic capacity will be capped to 0 if the total after the effect is

add_cic add_cic = 300 capacity bank on the 1.13 The amount of economic capacity to add. negative.

international market.

name = <string>

The name of the variant.

type = <equipment>

The equipment type the variant is of. parent_version = <int>

create_equipment_variant = { Ordering for multiple variants of the same equipment. 0 is the     name = "Vetehinen Class"

oldest, 1 is the second-oldest, etc. Optional, 0 by default.     type = ship_hull_submarine_1

show_position = <bool>

Dynamic equipment version numbering. If disabled removes     modules = {

the suffix from the equipment name. Numbering linked to         fixed_ship_torpedo_slot =

parent_version. Optional, yes by default. ship_torpedo_sub_1

obsolete = <bool>

sub_ship_engine_1 Whether the equipment variant is flagged as obsolete within

the GUI and for AI. Optional, no by default. ship_mine_layer_sub

mark_older_equipment_obsolete = <bool>     }

Marks all older (non-chassis) equipment variants as obsolete }

as long as the following matches: Archetype, niche, mission set (for planes). Optional, defaults to false.

create_equipment_variant = { name_group = <name group>

The name group used for equipment. Stored in     type = fighter_equipment_0

/Hearts of Iron IV/gfx/army_icons/army_icons.txt. /Hearts of Iron IV/common/units/names_ships.     obsolete = yes

Optional, can only be defined for ships.     upgrades = { Upgrades are defined within plane_gun_upgrade = 1 Creates the specified

/Hearts of Iron IV/common/units/equipment/upgrades/*.txt. create_equipment_variant role_icon_index = <int>/auto

Index of the role icon that will be used, as an integer. If set to Equipment types, including module slots for them, are defined within

"auto", will pick automatically. If set to 0, will be unset. } /Hearts of Iron IV/common/units/equipment/*.txt.**Optional, only can be defined for ships. Equipment modules are defined within**model = ** /Hearts of Iron IV/common/units/equipment/modules/*.txt.

Model that will be used by the equipment on the world map. create_equipment_variant = {

Optional.

icon = <sprite>     parent_version = 1

The icon that will be used by equipment. Stored as a     modules = {

spriteType within         main_armament_slot =

tank_heavy_machine_gun /Hearts of Iron IV/interface/*.gfx. Optional.

design_team = mio:<MIO>     upgrades = {

The military industrial organisation that should be set as the         tank_nsb_engine_upgrade = 2

designer of the equipment. Optional.     }

allow_without_tech = <bool>

"GFX_ENG_basic_light_tank_medium" If set, bypasses the requirement that the equipment that the

variant is for must be unlocked through research. Optional, ENG_MKIV_light_tank_entity

defaults to false.     design_team =

Upgrade scope mio:ENG_vauxhall_organization

} <upgrade> = <amount>

The upgrades configuration for the variant. Module scope <slot> = <module>

The modules configuration for the variant.

amount = <int>

The amount to produce before automatically stopping. Optional.

requested_factories = <int>

The number of factories to assigned initially. Optional. progress = <float>

The initial production progress. Optional. efficiency = <float>

The initial production efficiency. Optional. add_equipment_production = {

name = <string>     equipment = { Starts a production line

The name that'll be used for the equipment, such as with         type = light_cruiser_2

add_equipment_production ships. Optional.     } for the specified   1.0

industrial_manufacturer = mio:<MIO>     progress = 0.95 scope.

The military industrial organisation that's set as the     amount = 1

equipment's designer. }

Equipment scope type = <equipment>

The name of the equipment to produce. creator = <country>

The country which is producing the equipment. Used if root scope isn't producer. Optional. version_name = <string> The name of the variant to

produce. Optional.

name = <loc_key>

Name. add_design_template_bonus = {

name = air_equipment uses = <int>

uses = 1 Add free bonus design The value for uses  and cost_factor  can either be an absolute value The amount of times the discount can be used.

add_design_template_bonus   cost_factor = 0.75 discount to given types or a script constant. Can use several equipment types, where 1 is 1.15 cost_factor = <float>   equipment = small_plane_airframe

with a set of uses. mandatory. Discount.   equipment = medium_plane_airframe

equipment = large_plane_airframe equipment = <equipment>

} Can be equipment type and archetype.

add_equipment_bonus = {

project = FROM

bonus = {

project = <> Adds the specified

Optional, special project scope for using special project equipment bonuses to

name. If not set, the name will be used. the country. As instant = yes

add_equipment_bonus     } description the given loc name = <loc_key> 1.15

Name. small_plane_naval_bomber_airframe = special project will be bonus = { ... } {

used. Same usage as in Bonus.       air_range = 0.1

}

}

Changes current version type = <equipment> number for a given

Equipment type. set_equipment_version_number = { equipment type to N. The Set "Variant max version" to specified version. set_equipment_version_number   type = small_plane_airframe_1 next equipment variant 1.16

version = <int>   version = 4 Provides no tooltip. created from that type will

Version to set. }

have version number N+1.

Military ​[ edit | edit source ]

Military-related country-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added

type = <ship>

The type of ship to destroy. destroy_ships = {

destroy_ships     type = destroyer Destroys the specified type and amount of ships   1.5

count = <int> or all     count = all controlled by the current scope. The amount to destroy. }

transfer_navy = { target = <country> Transfers the current scope navy to the specified

transfer_navy     target = GER   1.5 The target country. country.

}

type = <ship>

The type of ship to transfer.

target = <country> transfer_ship = {

The target country. prefer_name = "HMS Achilles"

prefer_name = <string> transfer_ship     type = light_cruiser Transfers the specified type of ship from the

  1.4 Name of ship in origin navy that will preferably be transferred     target = NZL current scope to the specified country. to target navy. Optional.     exclude_refitting = no

} exclude_refitting = <bool>

Determines whether ships that are being refitted will be transferred. Optional.

type = <ship>

The type of ship to create. FRA = {

equipment_variant = <string> create_ship = {

The equipment variant to use. type = ship_hull_submarine_1 Create a ship from another country and assign it

creator = <country> create_ship         equipment_variant = "S Class" to the reserve fleet. If not set, it will be the scoped   1.9

The country that created this ship. Optional.         creator = ENG

name = <string>

Name of the ship. Optional.

} amount = <int>

The amount of ships to create. Optional, defaults to 1.

add_mines Add mines to a strategic region for the current country. add_mines = { region = 42 amount = 100 } Add mines to a strategic region.   1.6

name = <string>

The name of the ace.

surname = <string> add_ace = {

The surname of the ace.     name = "Amelia"

callsign = <string> Ace types found in

add_ace     callsign = "Revenant" Adds an ace for the current scope. 1.0 The callsign of the ace. /Hearts of Iron IV/common/aces/*.txt.

type = <type>     is_female = yes

The ace type. }

is_female = <bool>

The gender of the ace.

<string> Unlocks the specified combat tactic for the unlock_tactic unlock_tactic = tactic_masterful_blitz 1.17

Tactic to unlock. country.

Doctrine ​[ edit | edit source ]

Doctrine-related country-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added name = <loc_key>

Optional tooltip showing why the doctrine has reduced cost in the doctrine menu.

cost_reduction = <float> add_doctrine_cost_reduction = { For a general doctrine cost reduction, Percentage of cost reduced. cost_reduction = 0.5 see "

add_doctrine_cost_reduction uses = 2 Adds a limited use cost reduction for doctrines. 1.11 uses = <int> <land/air/naval>_doctrine_cost_factor"

category = land_doctrine Number of times the cost reduction can be used. } in Modifiers. category = <doctrine category>

Which doctrine category the cost reduction will apply to. (Ex: land_doctrine , air_doctrine .)

amount = <int>

Amount of mastery to add.

folder = <string> You can use flexible filters to have this

Optional - will filter by tracks in the specified folder. add_mastery = { effect apply to all tracks that match the grand_doctrine = <string>     amount = 100 specified folder, grand doctrine,

Optional - will filter by tracks in folders with the specified grand     # FILTERS: subdoctrine or specific track. If a

doctrine.     folder = land

add_mastery     grand_doctrine = mobile_warfare Adds doctrine mastery. certain filter is not present, it will be 1.17 sub_doctrine = <string>

Optional - will filter by tracks with the specified subdoctrine.     track = infantry can add mastery to all active tracks in track = <string>     index = 1

all folders by not specifying any filters Optional - will filter by tracks of the specified type. }

at all. index = <int>

Optional - will filter by the track index within the folder (0- indexed).

amount = <float>

Amount of mastery to add per day.

days = <int>

Number of days to apply the daily mastery gain for. name = <loc_key>

add_daily_mastery = { Loc key - will be used in descriptions to show the source of the You can use flexible filters to have this

mastery gain.     days = 90 effect apply to all tracks that match the folder = <string>     name = specified folder, grand doctrine,

Optional - will filter by tracks in the specified folder. CHI_military_affairs_commission_sea subdoctrine or specific track. If a add_daily_mastery     # FILTERS:

grand_doctrine = <string> Adds doctrine mastery daily for a certain duration. certain filter is not present, it will be 1.17 folder = land

Optional - will filter by tracks in folders with the specified grand     grand_doctrine = mobile_warfare counted as a pass. For example, you doctrine.     sub_doctrine = mobile_infantry can add mastery to all active tracks in sub_doctrine = <string>     track = infantry all folders by not specifying any filters

Optional - will filter by tracks with the specified subdoctrine.     index = 1 at all. }

track = <string>

Optional - will filter by tracks of the specified type. index = <int>

Optional - will filter by the track index within the folder (0- indexed).

bonus = <float>

Bonus factor, e.g. 0.1 = +10%

days = <int>

Number of days to apply the bonus mastery gain for. name = <loc_key>

add_mastery_bonus = { Loc key - will be used in descriptions to show the source of the You can use flexible filters to have this

mastery gain.     days = 90 effect apply to all tracks that match the folder = <string>     name = specified folder, grand doctrine,

Optional - will filter by tracks in the specified folder. CHI_military_affairs_commission_sea subdoctrine or specific track. If a add_mastery_bonus     # FILTERS: Get a bonus to doctrine mastery gain for a certain

grand_doctrine = <string> certain filter is not present, it will be 1.17 folder = land duration.

Optional - will filter by tracks in folders with the specified grand     grand_doctrine = mobile_warfare counted as a pass. For example, you doctrine.     sub_doctrine = mobile_infantry can add mastery to all active tracks in sub_doctrine = <string>     track = infantry all folders by not specifying any filters

Optional - will filter by tracks with the specified subdoctrine.     index = 1 at all. }

track = <string>

Optional - will filter by tracks of the specified type. index = <int>

Optional - will filter by the track index within the folder (0- indexed).

<string> Activate (unlock and assign) the specified grand set_grand_doctrine set_grand_doctrine = mobile_warfare 1.17

Grand doctrine id. doctrine.

Subdoctrine id.

OR sub_doctrine = <string>

By default, the subdoctrine is assigned Subdoctrine id. set_sub_doctrine = mobile_infantry

to the first matching track that the folder = <string>

system can find. However, you can Optional, in case you need to specify the folder.

Activate (unlock and assign) the specified also specify a specific folder and track set_sub_doctrine track = <int> set_sub_doctrine = { 1.17

Optional, in case you need to specify the track index within the

folder. Note that this is the track index (starting with 0) among     track = 1 multiple folders, or multiple times in the ALL the tracks in the folder, not just the ones that match the }

same folder. subdoctrine. So in a case where a grand doctrine has the tracks: 'infantry - armor - armor - operations', you would use track = 1 to refer to the first armor track, and track = 2 to refer to the second armor track.

Intelligence ​[ edit | edit source ]

Intelligence-related country-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added

create_intelligence_agency = {

name = <string>

The name of the intelligence agency. (Optional) GFX_intelligence_agency_logo_agency effect with just create_intelligence_agency = yes .

create_intelligence_agency } Creates an Intelligence Agency. 1.9 icon = <sprite> This will check if any specific intelligence agency cosmetics

The icon of the intelligence agency. (Optional) should be used for the nation, and if not it uses the default.

create_intelligence_agency = yes

upgrade_intelligence_agency =

upgrade_form_department Unlocks an Intelligence Agency Upgrades can be found in

upgrade_intelligence_agency Allows to unlock automatically an intelligence agency upgrade 1.9 Upgrade. common/intelligence_agency_upgrades

upgrade_intelligence_agency = <upgrade>

add_decryption = {

target = <tag>     target = GER

Towards which country to add decryption.     amount = 300

}

amount = <int> Adds decryption towards the target add_decryption target  and ratio  arguments are mutually exclusive. 1.9

How much decryption to add in flat numbers. country ratio = <0-1> add_decryption = {

How much decryption ratio to add.     target = GER

}

target = <tag>

Towards which country to add intelligence.

civilian_intel = <int> add_intel = {

How much civilian intel to add.     target = GER

army_intel = <int> Adds the specified amount of intel

add_intel     army_intel = 2 If an intel argument is left out, 0 is assumed. 1.9 How much army intel to add. towards the specified country.

navy_intel = <int>     airforce_intel = 2

How much navy intel to add. }

airforce_intel = <int>

How much airforce intel to add.

tag = <tag>

Towards which country to add a token on. add_operation_token = { Adds an operation token towards the Operation tokens are defined in

add_operation_token     tag = GER country, allowing access to more 1.9 token = <id>     token = token_test /Hearts of Iron IV/common/operation_tokens/*.

intel or applying a targeted modifier. Which token to add. }

tag = <tag>

Towards which country to remove a token from. remove_operation_token = {

remove_operation_token     tag = GER Removes an operation token from Operation tokens are defined in 1.9

token = <id>     token = token_test the country. /Hearts of Iron IV/common/operation_tokens/*. Which token to remove. }

capture_operative = {

operative = <tag>     operative = PREV

Which operative to capture.     ignore_death_chance = yes Operatives can be referred to by using tags that refer to

capture_operative } Captures the specified operative. 1.9 ignore_death_chance = <bool> scopes

Whether to ignore the death chance on capture (no by default). capture_operative = PREV

bypass_recruitment = <bool>

Whether the operative is directly added to the list of available operatives or needs to be recruited.

available_to_spy_master = <bool>

Whether the operative can be recruited by the spy master. bypass_recruitment should be set to no. portrait_tag_override = <bool>

If selecting a random portrait, create one that is from the create_operative_leader = {

name = "Jacques Duclos" specified country rather than the current country. name = Traits are found in

GFX = GFX_portrait_jacques_duclos <string> traits = { operative_infiltrator /Hearts of Iron IV/common/unit_leader/*.txt.

Creates an operative for the current create_operative_leader The name of the operative. operative_natural_orator } All arguments aside from bypass_recruitment are optional. 1.9

scope with the specified attributes. GFX = <string> bypass_recruitment = no Must use a spriteType for the portrait, a direct link as in

The graphical reference of the picture of the leader, defined as available_to_spy_master = yes "gfx/leaders/TAG/filename.dds" will not work. nationalities = { FRA POL }

a sprite within any }

/Hearts of Iron IV/interface/*.gfx file. nationalities = { <tag> }

The nationalities of the operative. traits = { <trait> }

The traits the leader spawns with. gender = <male|female>

The gender of the operative. Defaults to random.

<tag> Operatives can be referred to by using tags that refer to free_operative free_operative = PREV Frees the specifies operative. 1.9

The operative to be freed. scopes

captured_by = <tag>

The country that captured the operative. free_random_operative = {

free_random_operative captured_by = POL Frees one random captured 1.9

all = <bool> all = yes operative or all of them. Whether to free all operatives or not (Defaults to no). }

kill_operative = {

operative = <tag> } Operatives can be referred to by using tags that refer to kill_operative Kills the targeted operative. 1.9

The operative that is killed. scopes

kill_operative = PREV

turn_operative = {

Turns the targeted operative against

operative = <tag> } scopes. This counts as the operative dying and will trigger turn_operative their own country, transferring them 1.9

The operative that is turned. the corresponding On action. Logs an error if used against to the current country.

your own operative. turn_operative = PREV

category = <category name>

The category to steal from. See /Hearts of Iron IV/common/technology_tags/* for list.

folder = naval_folder

The folder to steal from. See steal_random_tech_bonus = {

/Hearts of Iron IV/common/technology_tags/* for category = air_equipment

list. ahead_reduction = <float>     folder = naval_folder

The reduction to the ahead of time penalty. bonus =     ahead_reduction = 0.8

<float> Steals a random tech bonus from If a country does not have a tech to be stolen, a random

steal_random_tech_bonus     base_bonus = 1.1 1.9 The bonus to research speed. base_bonus = <float> the specified country. bonus will be applied by using base_bonus as a base.

The backup bonus if no tech is available. instant =     name = LOC_KEY

<bool>     target = POL

Whether to instantly give a tech instead of a bonus or not. No     uses = 2

by default. } dynamic = <bool>

Changes between instant and non-instant based on type. No by default. name = <localisation key> The name of the bonus. target = <tag> The country to steal from. uses = <int> How many times the bonus can be used.

Characters ​[ edit | edit source ]

These are the character-related effects in the country scope. For effects in character scope, see § Character scope.

Character-related country-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added If you wish to change the nationality of a specific character, and the country getting the effect doesn't have the character recruited already, use the

every_possible_country =

{

target_country = <country> / <variable> set_nationality = {

has_character = ID }

set_nationality The target country.     target_country = TZN Switches the specified character to the specified random_character = { 1.11

character = <character>  The character to transfer.         limit = {

} is_character = ID }

TAG

}

command to call them up. Only necessary in 1.11 and beyond.

Retires the character, removing every role they hold Country scope only. The character retire_character <character> retire_character = GER_Character_Token 1.11

and making them disappear from the game. cannot be re-recruited after retiring.

character = <character>

The character to modify. set_character_name = {

set_character_name character = my_character Sets the new name for the target character. Can also be used in character scope. 1.11 name = <localisation key> name = my_name

The new name of the character. }

character_list_tooltip = { limit = { <triggers> } limit = {

Triggers that must be fulfilled to show up in the list.         has_character_flag = Displays a list of every character meeting the character_list_tooltip SOV_targeted_for_purge_flag specified limitation and recruited by the current 1.11

random_select_amount = <int> } country.

Upper bound on the characters that may be shown.     random_select_amount = 4

}

add_trait = {

character = <character>

The character to modify.      trait = really_good_boss ideology group assigned to characters,

} slot = <slot>  Slot of the character. Necessary for advisors. commonly referred to as sub-

Adds the specified country leader trait to the add_trait ideology = <sub-ideology>  Ideology type of the character. ideologies in community jargon. The 1.11

character. Necessary for country leaders. character slot can be the character's

add_trait = { trait = <trait>      character = TAG_my_leader name or id. Using name is

The trait to add.      ideology = liberalism recommended because 1.11 made id trait = field_of_gar obsolete. }

remove_trait = {

character = <character>

The character to modify.     trait = really_good_boss ideology group assigned to characters,

} slot = <slot>  Slot of the character. Necessary for advisors. commonly referred to as sub-

remove_trait ideology = <sub-ideology>  Ideology type of the character. Removes the specified trait from the character. ideologies in community jargon. The 1.11 Necessary for country leaders. character slot can be the character's

remove_trait = { trait = <trait>      character = TAG_my_leader name or id. Using name is

The trait to remove.      ideology = liberalism recommended because 1.11 made id trait = field_of_gar obsolete. }

Unit leaders ​[ edit | edit source ]

Unit leader-related country-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added name = <string>

The name of the leader.

picture = <string> OR portrait_path = <string> OR gfx = <string>

The graphical reference of the picture of the leader. skill = <int> create_corps_commander = {

The skill of the leader. name = "Jean de Lattre de Tassigny"

picture = Traits are found in attack_skill = <int>

"Portrait_France_Jean_de_Lattre_de_Tassigny.dds" /Hearts of Iron IV/common/unit_leader/*.txt. The attack skill of the leader.

traits = { trickster Deprecated, recommended to use defense_skill = <int>

create_corps_commander brilliant_strategist } Creates a commander for the current add_corps_commander_role instead when possible. The 1.0

The defense skill of the leader. skill = 4 scope with the specified attributes. attack_skill = 4 created corps commander will not be able to have a

planning_skill = <int> defense_skill = 2 portrait if assigned to be a minister via officer corps,

The planning skill of the leader. planning_skill = 4 causing errors.

logistics_skill = <int> logistics_skill = 3

The logistics skill of the leader. }

traits = { <trait> }

The traits the leader spawns with. female = <bool>

The gender of the leader. legacy_id = <int>

The legacy ID used for the unit leader. Optional.

name = <string>

The name of the leader.

picture = <string> OR portrait_path = <string> OR gfx = <string>

The graphical reference of the picture of the leader. skill = <int>

create_field_marshal = { The skill of the leader. name = "Maurice Gamelin"

attack_skill = <int> portrait_path = Traits are found in The attack skill of the leader. "GFX_portrait_FRA_maurice_gamelin" /Hearts of Iron IV/common/unit_leader/*.txt.

traits = { defensive_doctrine } defense_skill = <int> Creates a field marshal for the current Deprecated, recommended to use add_field_marshal_role

create_field_marshal skill = 2 1.0 The defense skill of the leader. scope with the specified attributes. instead when possible. The created field marshal will not

attack_skill = 1

planning_skill = <int> defense_skill = 3 be able to have a portrait if assigned to be a minister The planning skill of the leader. planning_skill = 2 via officer corps, causing errors.

logistics_skill = 1 logistics_skill = <int>

} The logistics skill of the leader. traits = { <trait> }

The traits the leader spawns with. female = <bool>

The gender of the leader. legacy_id = <int>

The legacy ID used for the unit leader. Optional.

name = <string>

The name of the leader.

picture = <string> OR portrait_path = <string> OR gfx = <string>

The graphical reference of the picture of the leader. skill = <int>

The skill of the leader. create_navy_leader = {

name = "François Darlan" Traits are found in attack_skill = <int> The attack skill of the

gfx = "GFX_portrait_FRA_francois_darlan" /Hearts of Iron IV/common/unit_leader/*.txt. leader.

traits = { superior_tactician } Deprecated, recommended to use defense_skill = <int> The defense skill of

create_navy_leader skill = 3 Creates a naval leader for the current add_naval_commander_role instead when possible. The 1.0

the leader. attack_skill = 2 scope with the specified attributes. defense_skill = 4 created admiral will not be able to have a portrait if

maneuvering_skill = <int> The maneuvering_skill = 3 assigned to be a minister via officer corps, causing

maneuvering skill of the leader. coordination_skill = 2 errors.

coordination_skill = <int> The }

coordination skill of the leader. traits = { <trait> }

The traits the leader spawns with. female = <bool>

The gender of the leader. legacy_id = <int>

The legacy ID used for the unit leader. Optional.

Does not work with the character ID. Instead, <id> Removes the specified unit leader by

remove_unit_leader remove_unit_leader = 70 remove_unit_leader_role within the scope of the character 1.0 The id of the unit leader. their legacy ID.

is recommended when possible.

add_corps_commander_role = {

character = <character>     Character = GER_Character_token

The character to modify.     skill = 4

add_corps_commander_role     attack_skill = 2 Sets the specified character to also act Can also be used in character scope. 1.11

<...>     defense_skill = 3 as a corps commander. Army leader role definition     planning_skill = 3

}

add_field_marshal_role = {

character = <character>   character = GER_Character_token

The character to modify.   skill = 4

add_field_marshal_role   attack_skill = 2 Sets the specified character to also act Can also be used in character scope. 1.11

<...>   defense_skill = 3 as a field marshal. Army leader role definition   planning_skill = 3

logistics_skill = 5

}

add_naval_commander_role = {

character = <character>   Character = GER_Character_token

The character to modify.   skill = 4

Sets the specified character to also act add_naval_commander_role   attack_skill = 2 Can also be used in character scope. 1.11

<...>   defense_skill = 3 as an admiral. Navy leader role definition   planning_skill = 3

logistics_skill = 5

}

<character> Shows the name of the specified show_unit_leaders_tooltip show_unit_leaders_tooltip = TAG_my_leader 1.11

The character whose name is to be shown. character as a tooltip.

Country leaders ​[ edit | edit source ]

Country leader-related country-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added name = <string>

The name of the leader.

desc = <string>

The description of the leader. create_country_leader = {

picture = <spriteType> name = AFG_mohammed_zahir_shah

The graphical reference to the leader portrait. The portrait uses a spriteType, defined within desc =

expire = <string> "POLITICS_MOHAMMED_ZAHIR_SHAH_DESC" /Hearts of Iron IV/interface/*.gfx.

When the leader dies in history. picture = Sub-ideologies are defined in create_country_leader GFX_AFG_mohammed_zahir_shah   1.0

ideology = <string> /Hearts of Iron IV/common/ideologies. expire = "1965.1.1"

The sub-ideology of the country leader. Does not ideology = despotism Deprecated. Recommended to use add_country_leader_role accept regular ideologies. traits = { instead when possible.

} female = <bool>

} The gender of the leader. Traits scope <trait>

The trait to add. Can add multiple.

character = <character> add_country_leader_role = {

The character to modify.     character = GER_character_token

country_leader = { ... }     country_leader = { Sets the specified character to also act Can also be used in character scope. Does absolutely nothing

add_country_leader_role Country leader role definition         ideology = fascism_ideology as a country leader, promoting to the if the character already has a country leader role in the 1.11 promote_leader = <bool>         expire = "1965.1.1.1" party leader if specified. ideology group.

Will promote the leader to be the leader of the         traits = { war_industrialist }

assigned party. Optional, defaults to false.

}

The character to promote. promote_character = GER_erwin_rommel Can also be used in character scope. If the character has

OR multiple country leader roles, specifying the ideology type is Promotes a character to the leader of

promote_character character = <character> promote_character = { mandatory. Ideology type refers to a sub-type of an ideology 1.11 their political party.

The character to promote.     character = GER_erwin_rommel group assigned to characters, commonly referred to as sub- ideology = <ideology type>     ideology = nazism ideologies in community jargon.

The ideology type used by the country leader role. }

character = <character>

The character to modify. remove_country_leader_role = { Can also be used in character scope. Ideology type refers to a der role from a

remove_country_leader_role     character = GER_Character_Token Removes a country lea sub-type of an ideology group assigned to characters, 1.11

ideology = <string>     ideology = socialism character. commonly referred to as sub-ideologies in community jargon.

The ideology type of the character. }

<ideology> Kills the country leader of the designated kill_ideology_leader kill_ideology_leader = communism   1.9

Ideology. ideology for the current scope.

Retires and removes the country leader <ideology>

retire_ideology_leader retire_ideology_leader = fascism of the ideology party for the current   1.9 Ideology.

scope.

<bool> Kills the country leader for the current kill_country_leader kill_country_leader = yes   1.0

Boolean. scope.

Retires and removes the country leader <bool>

retire_country_leader retire_country_leader = yes as head of their party for the current   1.0 Boolean.

scope.

<government> Changes the country leader's set_country_leader_ideology set_country_leader_ideology = socialism Creates no tooltip. 1.0

The government to set. government type for the current scope.

ideology = <ideology> Must use a localisation key from any The ideology of the country leader, optional. set_country_leader_description = {

set_country_leader_description ideology = neutrality /Hearts of Iron IV/localisation/*.yml file, putting Changes the country leader's description. 1.9.1

desc = <localisation key> desc = LOC_KEY the description in quotes will not work. Localisation for more The new description. } info

ideology = <ideology>

The ideology of the country leader, optional. set_country_leader_name = {

set_country_leader_name ideology = neutrality Changes the country leader's name. 1.9.1 name = <localisation key> name = LOC_KEY

The new name. }

ideology = <ideology>

The ideology of the country leader, optional. set_country_leader_portrait = {

The portrait must be defined in set_country_leader_portrait ideology = neutrality Changes the country leader's portrait. 1.9.1

portrait = <sprite name> portrait = GFX_IMAGE_NAME /Hearts of Iron IV/interface/*.gfx

The new portrait. }

Traits are found in <trait>

add_country_leader_trait add_country_leader_trait = Adds the specified trait to the current /Hearts of Iron IV/common/country_leader/*.txt 1.0

The trait to add. nationalist_symbol country's country leader. files.

Traits are found in <trait>

remove_country_leader_trait remove_country_leader_trait = Removes the specified trait from the /Hearts of Iron IV/common/country_leader/*.txt 1.0

The trait to remove. nationalist_symbol current scope's country leader. files.

Similar to swap_ideas. Removes one trait and adds swap_ruler_traits swap_ruler_traits = { remove = <trait> add Swaps traits. Use swap_country_leader_traits in character scope. 1.6

another. = <trait> }

Advisors ​[ edit | edit source ]

Advisor-related country-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added

<character> n advisor, placing them into their respective activate_advisor activate_advisor = Hires a

1.11 The character to activate. GER_character_token_air_chief slot.

1.11 The character to deactivate. GER_character_token_air_chief leaving it empty.

add_advisor_role = {

character = <character>     activate = yes

The character to modify.     advisor = { Can also be used in character scope. slot = air_chief

advisor = { ... }         cost = 50 Trigger and effect blocks (such as Sets the specified character to also act as an

add_advisor_role Advisor role definition         idea_token = allowed  and on_add ) cannot be 1.11 advisor, activating if specified.

activate = <bool> GER_character_token_air_chief added within advisor definitions Will activate the advisor (add them directly when the command         traits = { created this way.

is run to the countries government). Optional, defaults to false.

}

character = <character>

Specifies the character if the effect is executed in country remove_advisor_role = {

remove_advisor_role scope.   character = "SOV_genrikh_yagoda" Removes the specified advisor role from the Can also be used in character scope. 1.11

slot = political_advisor character. slot = <int> }

The slot where to remove the advisor slot from.

character = <character>

The character to modify. set_can_be_fired_in_advisor_role = { Changes the can_be_fired  attribute of the

slot = <slot> set_can_be_fired_in_advisor_role     character = BHR_important_advisor advisor, preventing the player from dismissing the Can also be used in character scope. 1.12.8

The slot of the character to modify.     value = no

} advisor. value = <bool>

The value to set.

Scientists ​[ edit | edit source ]

Scientist-related country-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added

add_scientist_role = {

character = my_character / var:my_char_var The scientist role format is the same

character = <character> / <variable> / PREV

as in the character DB. Except the The character to modify.   scientist = {

add_scientist_role     desc = desc_loc_key Adds the scientist role to a character. visible trigger, a scientist role created 1.15 <...>     traits = { scientist_trait_token ... } via effect cannot have triggers.

Scientist role definition     skills = { specialization_token = 2 ...

} Can also be used in character scope. }

}

remove_scientist_role = {

remove_scientist_role   character = my_character / var:my_char_var character = <character> / <variable> Remove the scientist role from a character. Can also be used in character scope. 1.15

/ PREV

}

portrait = <GFX>

Optional, random portrait by default.

portrait_tag_override = <country> / <variable> generate_scientist_character = {

Optional, accepts variable and keyword, only relevant if using   portrait = GFX_portrait

random portrait, by default use country in scope.   portrait_tag_override = CHI

gender = male gender = <gender> Generate a new character with a scientist role and

generate_scientist_character   skills = { 1.15 Optional, by default random gender. recruit it in the country in scope.

skills = { ??? }   }

Optional array, same format as in scientist role in character DB,   traits = { trait_token }

by default all skills are at 1. }

traits = { <trait> }

Optional array.

MIOs ​[ edit | edit source ]

These are the MIO-related effects in the country scope. For effects in military industrial organisation scope, see § MIO scope.

MIO-related country-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added

<MIO> Displays a tooltip that shows the name of the Doesn't change the availability of show_mio_tooltip show_mio_tooltip = my_mio 1.13

MIO to display. MIO and its initial trait (if present). the MIO directly.

unlock_military_industrial_organization_tooltip

= mio:my_mio_token

<mio> / <variable> Display a tooltip saying the MIO is made unlock_military_industrial_organization_tooltip 1.13

MIO to unlock. available (aka unlocked). unlock_military_industrial_organization_tooltip

= var:my_mio_var

Policy to display. unlock_mio_policy_tooltip = my_policy_1

OR policy = <policy> Displays a tooltip that says that the policy is Doesn't change the availability of

unlock_mio_policy_tooltip 1.13 Policy to display. unlock_mio_policy_tooltip = { made available. the policy directly.

show_modifiers = <bool>

Whether the trait's modifiers should be shown in the }

tooltip. Defaults to true.

policy = <policy>

Policy to modify. add_mio_policy_cost = {

The base amount is capped at 0 add_mio_policy_cost     policy = my_policy Modifies the base cost of a MIO policy. 1.13

value = <int>     value = 10 from below. Amount in political power to add. }

policy = <policy>

Policy to modify. set_mio_policy_cost = {

set_mio_policy_cost     policy = my_policy Modifies the base cost of a MIO policy. Cannot be negative. 1.13 value = <int>     value = 100

Amount in political power to set. }

policy = <policy>

Policy to modify. add_mio_policy_cooldown = {

add_mio_policy_cooldown     policy = my_policy Modifies the base length of a MIO policy The base amount is capped at 0 1.13

value = <int>     value = 10 cooldown. from below. Amount in days to add. }

policy = <policy>

Policy to modify. set_mio_policy_cooldown  = {

set_mio_policy_cooldown     policy = my_policy Modifies the base length of a MIO policy Cannot be negative. 1.13

value = <int>     value = 100 cooldown. Amount in days to set. }

Special Projects ​[ edit | edit source ]

These are special project related effects in the country scope.

Special project-related country-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added

sp:<project> Project to complete. complete_special_project =

sp:sp_naval_midget_submarine OR project = sp:<project>

Complete a special project for the country in scope. Project to complete.

complete_special_project = { This effect will not take into account the current scientist = <character>   project = sp:sp_naval_midget_submarine

state of the project tree and will allow you to unlock Optional, default to current scientists on the project.   scientist = ITA_curio_bernardis project, scientist, state accepts

complete_special_project   state = my_state a project even if the one before is not unlocked. 1.15 state = <string> variables and keywords.

iteration_output = { Since the project is not completed within a facility, Optional, default to current state of the project.

iteration_output = { <list> }     my_other_reward applied.

Optional, can be a single reward or reward = option.     my_third_reward = my_option_1

} show_modifiers = <bool>

show_modifiers = no Optional, default = yes.

}

add_breakthrough_points = {

specialization = specialization_land

value = 3 specialization = <dp_specialization_id>

} The specialization e.g. specialization_land. Add breakthrough points to one specialization or all

add_breakthrough_points 1.15 value = <int> for a country scope.

The amount of specialization breakthrough points to add. add_breakthrough_points = {

specialization = all

value = 1

}

add_breakthrough_progress = {

specialization = specialization_land

value = 3 specialization = <dp_specialization_id>

} The specialization e.g. specialization_land. Add breakthrough progress to one specialization or The value can either be an absolute

add_breakthrough_progress 1.15 value = <int> all for a country scope. value or a script constant.

The amount of specialization breakthrough progress to be added. add_breakthrough_progress = {

specialization = all

value = sp_breakthrough_progress.medium

}

Career profile ​[ edit | edit source ]

These are career profile related effects in the country scope.

Career profile-related country-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added

<bool> Step completed Mussolini missions by one for the career_profile_step_missiolini career_profile_step_missiolini = yes ???

Boolean. career profile.

History ​[ edit | edit source ]

These effects can only be used within history files, failing when used outside. However, they're considered effects anyway rather than history arguments, as they can be used in if statements.

Effects to be used in country history files: [Collapse] Version

Name Parameters Examples Description Notes Added

Initially assigns the specified character to recruit_character <character> recruit_character = GER_Character_token 1.11

the current country.

generate_character = {

token_base = <string>

Mandatory, acts as the character token.         idea_token = token is not set) concatenated, with an underscore as a separator. In the

generate_character GER_character_token_air_chief Generates a character for current name = <localisation key> provided example, the idea token will be 1.11

The name used for the character. Generates a             air_chief_ground_support_2 army_chief_defensive_1_GER_character_token_air_chief ; random name if not set.         } if idea_token  wasn't present, it'd be

}

Sets the order of battle to be used for the <order of battle>

current country's divisions, overriding Orders of battle are defined in set_oob The name of the file used for the order of battle set_oob = BHR_1936 1.0

every other non-naval and non-air order /Hearts of Iron IV/history/units/*.txt files. without the .txt  extension.

of battle.

<order of battle> Sets the order of battle to be used for the Orders of battle are defined in

set_naval_oob The name of the file used for the order of battle set_naval_oob = BHR_1936_naval_legacy current country's divisions, overriding 1.0 /Hearts of Iron IV/history/units/*.txt files.

without the .txt  extension. every other naval order of battle.

<order of battle> Sets the order of battle to be used for the Orders of battle are defined in

set_air_oob The name of the file used for the order of battle set_air_oob = ITA_1936_air_bba current country's divisions, overriding 1.12 /Hearts of Iron IV/history/units/*.txt files.

without the .txt  extension. every other air order of battle.

key = <string>

The key used for the file. Sets the order of battle to be used for the set_keyed_oob = {

Orders of battle are defined in set_keyed_oob     key = naval current country's divisions, overriding

name = <order of battle> 1.0 name = BHR_1936_mtg every other keyed order of battle that /Hearts of Iron IV/history/units/*.txt files.

The name of the file used for the order of battle } uses the same key. without the .txt  extension.

Variable ​[ edit | edit source ]

These are variable related effects in the country scope.

Variable-related country-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added

scorer = <???>

Id that is used in country scorer. get_highest_scored_country_temp = { Calculates the highest scored country that is get_highest_scored_country_temp   scorer = scorer_id

var defined in a country scorer and sets it to a ??? var = var_name

Variable name that the result will be stored. (default is } variable. highest_scored_country)

scorer = <???>

Id that is used in country scorer.

array = <string> get_sorted_scored_countries_temp = { culates & sorts all countries in a country

A name to store sorted countries as a temp array (default to   scorer = scorer_id Cal get_sorted_scored_countries_temp   array = array_name scorer and stores them and their scores in temp ???

sorted_country_list) scores = array_name arrays.

scores = <string> }

Corresponding score temp array for countries stored in array (default to country_list_scores)

var = <string>

Variable name to set. get_supply_vehicles = {

type = <type>   var = trucks_needed Sets a variable to the number of supply vehicles get_supply_vehicles Can be truck or train.   type = truck ???

in stockpile or that are needed. need = <bool>   need = yes

Default no. If yes, gets the number of needed vehicles. }

var = <string>

Variable name to set. get_supply_vehicles_temp = {

var = trucks_needed type = <type> Sets a temp variable to the number of supply

get_supply_vehicles_temp   type = truck ??? Can be truck or train. vehicles in stockpile or that are needed.

need = yes need = <bool> }

Default no. If yes, gets the number of needed vehicles.

State scope ​[ edit | edit source ]

The effects here must be used within a state scope.

General ​[ edit | edit source ]

General state-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added id = <event>

The event to fire.

days = <int> / <variable>

Fires the event in the specified number of days. Optional. state_event = { Where triggers do not need to be repeatedly checked

hours = <int> / <variable> id = my_event.1

Fires the event in the specified number of hours. random  can be a performance light alternative to days = 10 Fires the specified event for the current

state_event Optional. mean_time_to_happen  for scheduling events. random = 50 1.0

state. random = <int> / <variable>     random_days = 10 Using days = <event> / <variable> or hours may still be

Adds a random number (between 0 and random,     trigger_for = controller bugged and will not fire the event. }

inclusive) of hours to the scheduled fire time. Optional. random_days = <int> / <variable>

Adds a random number (between 0 and random_days, inclusive) of days to the scheduled fire time. Optional.

An unique string to identify the state flag with.

OR set_state_flag = my_flag

flag = <flag>

The flag to set. No tooltip is shown. The flag in this effect is used in the

set_state_flag days = <int> set_state_flag = { Defines a state flag. 1.0 flag = my_flag meaning of 'boolean flag', used to store information.

Sets the flag to last for the specified amount of days. days = 123

Optional.     value = 1

value = <int> }

The new value of the flag on the scale from -2 147 483 648 to 2 147 483 647.

The unique string of a state flag to clear.

flag = <flag>

The flag to modify.

value = <value> modify_state_flag = {

modify_state_flag The value to add to the flag. Defaults to 0.     flag = my_flag Adds an integer value to a flag. The flag must be already set. 1.3 value = 3

days = <int> }

The amount of days that the flag should last for before being cleared. Optional, defaults to permanent.

<string> Changes the current state's name to the set_state_name set_state_name = "Funland"   1.3

Defines the new name. specified name.

<bool> Resets any changes to the current state's reset_state_name reset_state_name = yes   1.3

Boolean. name.

<country> / <variable> Adds a claim for the specified country on add_claim_by add_claim_by = SOV   1.0

The country to add the claim for. the current scope.

<country> / <variable> Removes a claim by the specified country remove_claim_by remove_claim_by = SOV   1.0

The country to remove the claim for. on the current scope.

<country> / <variable> Adds a core for the specified country on add_core_of add_core_of = SOV   1.0

The country to add the core for. the current scope.

<country> / <variable> Removes a core for the specified country remove_core_of remove_core_of = SOV   1.0

The country to remove the core for. on the current scope.

<bool> Makes the current scope a demilitarized set_demilitarized_zone set_demilitarized_zone = yes   1.0

Boolean. zone.

<category> Changes the current state category to the Categories are found in set_state_category set_state_category = rural 1.3

The category to change to. specified category. /Hearts of Iron IV/common/state_category/*.txt

Modifier scope add_state_modifier = {

add_state_modifier <modifier> = <float>         local_resources = 2.0 Adds a modifier to the current state.   1.3 Adds a modifier to the state.     }

}

Note that when using negative manpower it will, besides <int> / <variable> Adds the specified amount of total

add_manpower add_manpower = 10000 reducing the population, also add directly to the recruitable 1.0 The amount to add. population to the current state.

manpower of the state. Which will increase your manpower

type = <resource>

The resource to add. add_resource = {

add_resource     type = oil Adds the specified resource in the Can also be used in country scope. 1.0

amount = <int> / <variable>     amount = 100 specified amount to the current state. The amount to add. }

Used for the state-based border wars, represented with orange stripes, see § Border wars for the border wars that

<bool> Enables Border War status for the current set_border_war set_border_war = yes simulate combat on a border between two countries. On the 1.0

Boolean. state. end of the border war, the on_border_war_lost on action is fired for the state that where the border war was lost.

division = <division string>

The division string.

owner = <country>

The owner of the division. prioritize_location = <province>

create_unit = { If possible, this province within the state gets used.     division = "name = "Infantry

Optional. Division" division_template =

allow_spawning_on_enemy_provs = yes "Infantry Division"

Allows the units to be created on provinces owned by the start_experience_factor = 0.5"

division owner's enemy. Defaults to false.

}

count = <int>

The amount of units to create. Defaults to 1. id = <int> create_unit = {

The ID to identify the unit. Only used in delete_unit.     division = "name = "Artie"

division_template = "Artillery country_score = { ... }

Division" start_manpower_factor = 0.3" A MTTH block deciding the province in the state where

the division should spawn, evaluates in the scope of the     count = 3 middle of division = "..."  will break the effect and controller. Defaults to prioritising owner's controlled     allow_spawning_on_enemy_provs = yes result in no units being spawned. provinces first and then owner's allies.     country_score = {

divisional_commander_xp = <int>

create_unit         modifier = { Adds the specified division to the current give the division commander experience on unit creation capital_scope. The effect will do nothing when put into a 1.3

The following arguments go within division = "" :         } Equipment factor cannot be set to zero. If set to zero, it will } be treated as a 1. Created equipment will be the latest

name = "<string>"     id = 123

The name of the division. available to the country. }

division_template = "<string>"

The template to be used by the division. create_unit = {

start_experience_factor = <double> division = "name = "Tank division"

Experience of the division, with 0 being none and 1 division_template = "Tank Division"

being full training. Defaults to 1. start_manpower_factor = 1

start_equipment_factor = <double> force_equipment_variants = {

Equipment stockpile of the division. Defaults to 1. medium_tank_chassis_2 = { owner =

"USA" amount = 100 version_name = start_manpower_factor = <double> "M4 Sherman" }}"

Manpower stockpile of the division. Defaults to 1.   owner = USA

count = 1 force_equipment_variants = { <equipment

} type> = { owner = "<country>" amount

= <int> version_name = "<string>" } }

Forces a certain type of equipment to be used. Multiple equipment types can be added by adding multiple <equipment type> = {} lines.

limit = { <triggers> }

The condition that must be true for the owner of the armies for them to teleport. teleport_armies = {

to_state_array = <array>         has_war_together_with = ROOT Teleports all armies in the specified state

Only define one of to_state_array, to_state, or to_province. If teleport_armies The state array the armies will get teleported to.     } if the owner of the armies meets the 1.9

none is specified, it defaults to the capital. to_province = <ID>     to_state_array = condition.

The province the armies will get teleported to. owned_controlled_states

} to_state = <ID>

The state the armies will get teleported to.

add_province_modifier = {

static_modifiers = {

mod_modifier_1 mod_modifier_2 }

province = 1234

}

add_province_modifier = {

static_modifiers = { <modifiers> } static_modifiers = {

The list of modifiers. mod_modifier_1 mod_modifier_2 }

province = <id> The province to apply the modifiers province = {

to. id = 1234 provinces = {} Scope for selecting multiple

id = 4321 provinces. The following arguments have to go inside it: id = <id> The ID of the province. Multiple can be

specified. days = 7

all_provinces = yes Selects all provinces to which the limitations apply. The following arguments require it: limit_to_coastal = yes  Limits the selection of Adds a province modifier to the specified Province modifiers are defined in

add_province_modifier 1.6 provinces to only coastal ones. } provinces in this state. /Hearts of Iron IV/common/modifiers/*.txt

limit_to_border = yes  Limits the selection of provinces to only ones bordering a different country. }

limit_to_naval_base = yes  Limits the selection of provinces to only ones that have a naval base.

add_province_modifier = { limit_to_victory_point = yes  Limits the

selection of provinces to only ones that have a victory static_modifiers = {

point, or a city, in them. mod_modifier_1 mod_modifier_2 }

province = { days = <int>  Will be temporary if specified, can be

all_provinces = yes

variable limit_to_coastal = yes

limit_to_border = yes

limit_to_naval_base =

yes

limit_to_victory_point

= yes

}

}

remove_province_modifier = {

static_modifiers = {

mod_modifier_1 mod_modifier_2 }

province = 1234

}

static_modifiers = { <modifiers> }

The list of modifiers. remove_province_modifier = {

province = <id> The province to apply the modifiers static_modifiers = {

to. provinces = {} Scope for selecting multiple mod_modifier_1 mod_modifier_2 }

provinces. The following arguments have to go inside it: province = {

id = 1234 id = <id> The ID of the province. Multiple can be

id = 4321 specified. }

all_provinces = yes Selects all provinces to which the limitations apply. The following arguments require it:

remove_province_modifier } Removes a province modifier to the Province modifiers are defined in 1.6

limit_to_coastal = yes  Limits the selection of specified provinces in this state. /Hearts of Iron IV/common/modifiers/*.txt

provinces to only coastal ones. limit_to_border = yes  Limits the selection of remove_province_modifier = {

provinces to only ones bordering a different country. static_modifiers = {

limit_to_naval_base = yes  Limits the selection mod_modifier_1 mod_modifier_2 }

of provinces to only ones that have a naval base. province = {

all_provinces = yes limit_to_victory_point = yes  Limits the

limit_to_coastal = yes

selection of provinces to only ones that have a victory limit_to_border = yes

point, or a city, in them. limit_to_naval_base =

yes

limit_to_victory_point

= yes

}

}

add_victory_points = {

add_victory_points Add victory points to a province province = 1234 Adds victory points to a province. Accepts negative values 1.10 value = 10

}

set_victory_points = {

set_victory_points Set the victory points of a province province = 1234 Sets the number of victory point in a Accepts negative values 1.10

value = 10 province. }

controller = <tag> set_state_province_controller = {

The new controller of the province.     controller = POL

limit = { <triggers> }         OR = { within that state controlled by countries

set_state_province_controller The triggers that must be fulfilled by the             tag = GER   1.9 that meet triggers to the specified

province's current controller to be             is_in_faction_with = GER

transferred to the new controller.

}

<country> Sets owner and controller of the state to transfer_state_to transfer_state_to = JAM 1.11

Country to transfer the state to. the given country

set_state_owner_to Country to set the owner (but not the controller) of the set_state_owner_to = JAM 1.11 country be given.

state to.

set_state_controller_to Country to set the controller (but not the owner) of the set_state_controller_to = ITA 1.11 given country

state to.

Adds a contested owner to a state. The <country> / <variable> effect can be used either from a country

add_contested_owner add_contested_owner = GER Can also be used in country scope. 1.15 Country to add contest to state. or a state scope and accepts the other as

parameter.

Removes a contested owner to a state. <country> / <variable> The effect can be used either from a

remove_contested_owner remove_contested_owner = GER Can also be used in country scope. 1.15 Country to remove contest to state. country or a state scope and accepts the

other as parameter.

Add a strategic location to a province strategic_province_location = { using state scope. The available strategic

strategic_province_location <string> = <int>     defensible_coastline = 10124 locations are defined in Can contain multiple strategic locations. 1.17 } strategic_locations and are specified with

a province id.

strategic_state_location = { Add strategic locations to a state in strategic_state_location <string> = <int>     favorable_approach = 11932 scope. The available strategic locations Can contain multiple strategic locations. 1.17

} are defined in strategic_locations.

Buildings ​[ edit | edit source ]

Building-related state-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added Shared buildings slots being the ones used for multiple building types, such as military or civilian factories. This is in contrast to non-shared slots, such as those used by radio stations or air

<int> / <variable> Changes the number of shared building slots bases, which only can be changed add_extra_state_shared_building_slots add_extra_state_shared_building_slots = 2

The amount of slots to add or remove. for the current state. globally with technologies. 1.0

Note: When using a variable and a saved event target, must be used as "saved_event.var_name" because "event_target:saved_event.var_name" will not work.

type = <string>

The building to add.

level = <int> / <variable>

The level to set the building to. instant_build = <bool>

Defines whether the buildings are instantly built. province = <id>

Defines the exact province to add provincal buildings in. Can be used as a scope.

add_building_construction = { Province scope     type = arms_factory For provincial buildings, must be all_provinces = <bool>     level = 5 done within the scope of the state

Affect all provinces within the state that meet each limit.     instant_build = yes that contains the province even if Used in the province scope. } done on a specific province. If the id = <id> controller country doesn't have an

Affect the specified province ID. Used in the province scope, order of battle assigned within the add_building_construction = {

will apply for each province if inserted multiple times. type = bunker history file, the buildings will not

limit_to_coastal = <bool>     level = 10 show up within the production Affect only coastal provinces within the selection. Used in     instant_build = yes menu until a recalculation of buildings, the province scope.     province = { Starts construction in the current state for the such as by changing consumer goods

add_building_construction         all_provinces = yes 1.0 limit_to_naval_base = <bool> specified building. or reloading a savefile.

Affect only provinces that have naval bases built. Used in         limit_to_victory_point > 1 Can only be used within a state the province scope.     } scope, such as

} limit_to_border = <bool> random_owned_controlled_state. The

Affect only provinces that lie on a border between countries. effect will do nothing when put into a Used in the province scope.

add_building_construction = { country's scope. limit_to_border_country = <country>

Affect only provinces that border a specific other country. For the list of building IDs present in

Used in the province scope. the base game, see Building instant_build = yes

modding#Types. limit_to_victory_point = <int>/<bool>     province = 2999

Affect only provinces that meet the victory point amount }

prerequisite. If yes  is used in place of a number, any amount of victory points works. Used in the province scope. limit_to_supply_node = <bool>

Affect only provinces that have a supply node. Used in the province scope. level = <int>

Affect only provinces with buildings level below, at or above the specified level. Used in the province scope.

type = <string>

The building to add.

level = <int> / <variable>

The level to set the building to. instant_build = <bool>

Defines whether the buildings are instantly built. province = <id>

Defines the exact province to add provincal buildings in. Can be used as a scope. Province scope all_provinces = <bool>

Affect all provinces within the state that meet each limit. Used in the province scope.

set_building_level = { The province scope is used for id = <id>     type = infrastructure

provincal level buildings. You can limit Affect the specified province ID. Used in the province scope,     level = 10

the construction to victory points will apply for each province if inserted multiple times.     instant_build = yes

} using : limit_to_victory_point limit_to_coastal = <bool>

5  (only build province buildings on Affect only coastal provinces within the selection. Used in

province with VP over 5 ) the province scope. Sets the specified building to the current state

set_building_level set_building_level = { limit_to_victory_point = 1.4 limit_to_naval_base = <bool>     type = bunker (or provinces within the state).

yes  (only build province buildings on Affect only provinces that have naval bases built. Used in     level = 3

province with VP) For provincial the province scope.     province = {

limit_to_border = <bool>

Affect only provinces that lie on a border between countries.         level < 3 province even if done on a specific Used in the province scope.     }

province. }

limit_to_border_country = <country>

Affect only provinces that border a specific other country. Used in the province scope. limit_to_victory_point = <int>/<bool>

Affect only provinces that meet the victory point amount prerequisite. If yes  is used in place of a number, any amount of victory points works. Used in the province scope. limit_to_supply_node = <bool>

Affect only provinces that have a supply node. Used in the province scope. level = <int>

Affect only provinces with buildings level below, at or above the specified level. Used in the province scope.

type = <building>

The building to damage. damage_building = {

type = infrastructure tags = <building_tag>

damage = 1 The buildings with this tag to damage.

} The health of buildings is determined tags = { <building_tag> } by the value attribute in a building's

The buildings with these tags to damage. Damages a building in a targeted state or damage_building definition. This is multiplied by their 1.3

repair_speed_modifier = <float> damage_building = { province. level to get their total health. Repair will be x% slower until building is fully repaired   tags = dam_building

damage = 1 Can also be used in country scope. damage = <float>

repair_speed_modifier = -0.8 The amount of damage to inflict.

province = 3488 province = <id> / <variable> }

The province to target for provincal buildings.

type = <building> remove_building = {

The building to remove.     type = arms_factory

tag = <building_tag> } Removes the specified building in the current The buildings with this tag to remove. state. For shared buildings level determines

remove_building   1.0 tag = { <building_tag> } the amount, whereas for the others it is the

The buildings with these tags to remove. remove_building = { actual level. tag = facility

level = <int> / <variable> level = 1

The levels to remove. }

65 = {

<building> = <int> construct_building_in_random_province { Set building level in a random province of

1.15 Building to build.         land_facility = 1 state scope.

}

Resistance and compliance ​[ edit | edit source ]

Resistance-related state-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added

<int> / <variable> add_compliance add_compliance = 30 Adds compliance to the specified state.   1.9

The amount to add.

<int> / <variable> add_resistance add_resistance = 30 Adds resistance to the specified state.   1.9

The amount to add.

<int> / <variable> add_resistance_target add_resistance_target = 30 Increases resistance target in the specified state.   1.9

The amount to add.

id = <int>

The ID of the target increase. amount = <int>/<variable>

The amount to increase the resistance target by. occupied = <country> add_resistance_target = {

Will only apply the increase if the the occupied country is the     id = 123

specified scope.     amount = 30

add_resistance_target     occupied = ENG occupier = <country> Increases resistance target in the specified state.   1.9

Will only apply the increase if the the occupier is the specified     days = 365

scope.     tooltip = my_localisation_key

days = <int>/<variable> }

If set, the resistance target will only be increased for the specified amount of days. tooltip = <string>

The tooltip to show in the resistance target tooltip.

Boolean.

<country> Disables resistance for the scoped state when the force_disable_resistance force_disable_resistance = GER   1.9

The target country. occupier is the specified country.

clear = <bool>

If set to yes, will clear resistance. force_disable_resistance = {

occupier = <country>     clear = yes Disables resistance for the scoped state when the force_disable_resistance Resistance will be disabled if the occupier is the specified scope.     occupier = GER   1.9

occupier is the specified country. occupied = <country>     occupied = ENG

Resistance will be disabled if the occupied country is the specified }

scope.

Does not start resistance by itself, only removes the checks forcefully

<country> Enables resistance for the scoped state when the force_enable_resistance force_enable_resistance = GER disabling it. Use with start_resistance 1.9

The target country. occupier is the specified country. in order to immediately start resistance.

clear = <bool>

If set to yes, will clear resistance. Does not start resistance by itself, only force_enable_resistance = {

occupier = <country>     clear = yes removes the checks forcefully Enables resistance for the scoped state when the

force_enable_resistance Resistance will be enabled if the occupier is the specified scope.     occupier = GER disabling it. Use with start_resistance 1.9 occupier is the specified country.

occupied = <country>     occupied = ENG in order to immediately start Resistance will be enabled if the occupied country is the specified } resistance. scope.

<int> / <variable> Removes a set resistance target increase in the

remove_resistance_target The id of the resistance target to remove. (Must be set with remove_resistance_target = 30 Has no tooltip. 1.9 specified state.

add_resistance_target)

<int> / <variable> set_compliance set_compliance = 30 Sets compliance in the specified state.   1.9

The amount to set the compliance to.

The resistance should be enabled in the state, either via start_resistance or through the in-game process. Occassionally it may take a tick for

<int> / <variable> resistance to start after the set_resistance set_resistance = 30 Sets resistance in the specified state. 1.9

The amount to set the resistance to. controllership change, so it's preferable to do so on states that are given to the country immediately before this gets executed, such as if this is executed in country history.

<bool>/<country> start_resistance = POL If used on a state that normally can't Whether to start resistance or not. If using a country as the

start_resistance Starts resistance in the specified state. start resistance, use alongside with 1.9 parameter, the state will only start resistance if occupied by the

force_enable_resistance. target country. start_resistance = yes

<0-1> Sets the strength of the garrison in the specified set_garrison_strength set_garrison_strength = 0.5 1.9

The new garrison strength. state.

PREV will be the country for whom the occupation law will be changed. If

GER = {

every_controlled_state = { PREV is not a country, nothing <law ID>     set_occupation_law = changes. If PREV doesn't occupy the

set_occupation_law The new occupation law enacted by the previous scope or military_governor_occupation Sets the occupation law of the state. state, nothing happens until it does. If 1.12 }

default_law . using default_law , resets to the }

law set by the country's occupation.

# Changes GER's occupation law for every controlled state. Can also be used in country scope.

Raids ​[ edit | edit source ]

Raid-releated state-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added Reduce progress to the special project in state. Root scope is raid instance scope. The input

Value to reduce. complete the special project, i.e. a decimal number between 0 and 1.

Character scope ​[ edit | edit source ]

The effects here must be used within a character scope.

General ​[ edit | edit source ]

General character-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added <flag>

An unique string to identify the character flag with.

OR set_character_flag = my_flag

flag = <flag>

The flag to set. No tooltip is shown. The flag in this effect is set_character_flag days = <int> set_character_flag = { Defines a character flag. used in the meaning of 'boolean flag', used to 1.11

Sets the flag to last for the specified amount of days. store information.

Optional.     value = 1

value = <int> }

The new value of the flag on the scale from -2 147 483 648 to 2 147 483 647.

<localisation key> Changes the character's name to the specified set_character_name set_character_name = GER_my_cool_flag 1.11

The name to use. localisation key's value.

flag = <flag>

The flag to modify.

value = <value> modify_character_flag = {

modify_character_flag The value to add to the flag. Defaults to 0.     flag = my_flag Adds an integer value to a flag. The flag must be already set. 1.11 value = 3

days = <int> }

The amount of days that the flag should last for before being cleared. Optional, defaults to permanent.

clr_character_flag clr_character_flag = <bool> Clears a character flag 1.11

Boolean>

If you wish to change the nationality of a specific character, and the country getting the effect doesn't have the character recruited already, use the

every_possible_country = {

ID } <country> / <variable> Switches the current character to the specified

set_nationality set_nationality = GER     random_character = { 1.5 The target country. country, giving them the character.

= ID }

}

command to call them up. Only necessary in 1.11 and beyond.

character = <character>

The character name. Optional if in character scope. set_portraits = {

Army scope: small = <sprite>     character = my_character Sprites are defined within set_portraits The sprite used as an advisor.     army = { small ="MySmallCharacterGFX" }

large = <sprite> Changes the specified portraits of a character. /Hearts of Iron IV/interface/*.gfx 1.11 civilian = { large

The sprite used as a general. ="MyLargeCharacterGFX" } files. Character scope: large = <sprite> }

The sprite used as a country leader.

add_trait = {

slot = <slot>  Slot of the character. Necessary for     slot = political_advisor Ideology type refers to a sub-type of an advisors.     trait = really_good_boss ideology group assigned to characters,

} commonly referred to as sub-ideologies in ideology = <sub-ideology>  Ideology type of the Adds the specified country leader trait to the

add_trait community jargon. The character slot can be 1.11 character. Necessary for country leaders. character.

the character's name or id. Using name is trait = <trait> add_trait = {

The trait to add.     ideology = liberalism recommended because 1.11 made id trait = field_of_gar obsolete. }

remove_trait = {

slot = <slot>  Slot of the character. Necessary for     slot = political_advisor Ideology type refers to a sub-type of an advisors.     trait = really_good_boss ideology group assigned to characters,

} commonly referred to as sub-ideologies in ideology = <sub-ideology>  Ideology type of the

remove_trait Removes the specified trait from the character. community jargon. The character slot can be 1.11 character. Necessary for country leaders.

the character's name or id. Using name is trait = <trait> remove_trait = {

recommended because 1.11 made id The trait to remove.     ideology = liberalism

}

add_corps_commander_role = {

<...> Sets the specified character to also act as a

add_corps_commander_role     defense_skill = 3 1.11 Army leader role definition corps commander.

}

add_field_marshal_role = {

skill = 4

attack_skill = 2 <...> Sets the specified character to also act as a

add_field_marshal_role   defense_skill = 3 1.11 Army leader role definition field marshal.

planning_skill = 3

logistics_skill = 5

}

add_naval_commander_role = {

skill = 4

attack_skill = 2 <...> Sets the specified character to also act as an

add_naval_commander_role   defense_skill = 3 1.11 Navy leader role definition admiral.

planning_skill = 3

logistics_skill = 5

}

character = <character> add_country_leader_role = {

The character to modify.     character = GER_character_token

country_leader = { ... }     country_leader = { Sets the specified character to also act as a

Does nothing if the character already has a add_country_leader_role Country leader role definition         ideology = fascism_type country leader, promoting to the party leader if 1.11

country leader role in the ideology group. promote_leader = <bool>         expire = "1965.1.1.1" specified.

Will promote the leader to be the leader of the assigned         traits = { war_industrialist }

party. Optional, defaults to false. }

<bool> If the character has multiple country leader Boolean. promote_character = yes roles, specifying the ideology type is

Promotes a character to the leader of their mandatory. Ideology type refers to a sub-type promote_character OR 1.11

political party. of an ideology group assigned to characters, <ideology type>

promote_character = liberalism commonly referred to as sub-ideologies in The ideology type used by the country leader role.

community jargon.

Ideology type refers to a sub-type of an remove_country_leader_role = {

ideology = <string> Removes a country leader role from a ideology group assigned to characters, remove_country_leader_role     ideology = socialism 1.11

The ideology type of the character. character. commonly referred to as sub-ideologies in }

community jargon.

add_advisor_role = {

advisor = { ... }     advisor = {

Advisor role definition         slot = air_chief

activate = <bool>

add_advisor_role         idea_token = Sets the specified character to also act as an and on_add ) cannot be added within 1.11

Will activate the advisor (add them directly when the GER_character_token_air_chief advisor, activating if specified. advisor definitions created this way.

command is run to the countries government). Optional,         traits = {

defaults to false.

}

remove_advisor_role = { slot = <int> Removes the specified advisor role from the

remove_advisor_role   slot = political_advisor 1.11 The slot where to remove the advisor slot from. character.

}

add_scientist_role = { The scientist role format is the same as in the

scientist = {

<...>

add_scientist_role     traits = { scientist_trait_token ... } Adds the scientist role to a character. scientist role created via effect cannot have 1.15 Scientist role definition     skills = { specialization_token = 2 ... triggers.

}

} Can also be used in country scope. }

remove_scientist_role <bool> remove_scientist_role = yes Remove the scientist role from a character. Can also be used in country scope. 1.15

level = <int> / <variable>

Level to add. add_scientist_level = {

Add levels to a special project specialization add_scientist_level   level = 2 1.15

specialization = <specialization>   specialization = specialization_nuclear for a scientist character in scope. Specialization to add. }

<int> / <variable> Injure a scientist for x amount of days to a injure_scientist_for_days injure_scientist_for_days = 12 1.15

Amount of days to apply injure. scientist character in scope.

Trait to add.

experience = <int> / <variable>

Expierience to add. add_scientist_xp = {

add_scientist_xp   experience = 2 Add experience to a special project 1.15

specialization = <specialization>   specialization = specialization_nuclear specialization for a scientist character in scope. Specialization to add. }

slot = <slot>

The slot of the character to modify. set_can_be_fired_in_advisor_role = { Changes the can_be_fired  attribute of the set_can_be_fired_in_advisor_role     slot = political_advisor advisor, preventing the player from dismissing 1.12.8

value = <bool>     value = no the advisor.

The value to set. }

Unit leaders ​[ edit | edit source ]

These can only be used with characters of the unit leader type.

General unit leader-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added id = <event>

The event to fire.

days = <int> / <variable>

Fires the event in the specified number of days. Optional. Uses a special interface displaying the current unit leader

unit_leader_event = { hours = <int> / <variable>

Fires the event in the specified number of hours.

unit_leader_event     days = 10 Fires the specified event for the owner of the Where triggers do not need to be repeatedly checked 1.5

Optional.     random = 50 current unit leader. random_days = 10 random  can be a performance light alternative to

random = <int> / <variable> } mean_time_to_happen  for scheduling events.

Adds a random number (between 0 and random, inclusive) of hours to the scheduled fire time. Optional. random_days = <int> / <variable>

Adds a random number (between 0 and random_days, inclusive) of days to the scheduled fire time. Optional.

<flag> Deprecated. Use set_character_flag instead. No tooltip is set_unit_leader_flag set_unit_leader_flag = my_flag Defines a unit leader flag. 1.5

An unique string to identify the unit leader flag with. shown.

<flag> Deprecated. Use clr_character_flag instead. No tooltip is clr_unit_leader_flag clr_unit_leader_flag = my_flag Clears a defined unit leader flag. 1.5

The unique string of a unit leader flag to clear. shown.

flag = <flag>

The flag to modify.

value = <value> modify_unit_leader_flag = {

modify_unit_leader_flag The value to add to the flag. Defaults to 0.     flag = my_flag The flag must be already set. Deprecated. Use Adds an integer value to a flag. 1.5

days = <int> }

The amount of days that the flag should last for before being cleared. Optional, defaults to permanent.

<bool> Promotes the current unit leader to Field promote_leader promote_leader = yes   1.5

Boolean Marshal (if Commander).

<bool> Demotes the current unit leader to demote_leader demote_leader = yes   1.5

Boolean Commander (if Field Marshal).

Traits are found in <trait> Adds the specified trait to the current unit

add_unit_leader_trait add_unit_leader_trait = old_guard /Hearts of Iron IV/common/unit_leader/*.txt 1.0 The trait to add. leader.

files.

Traits are found in <trait> Removes the specified trait from the current

remove_unit_leader_trait remove_unit_leader_trait = old_guard /Hearts of Iron IV/common/unit_leader/*.txt 1.0 The trait to remove. unit leader.

files.

add_random_trait = { old_guard Traits are found in <trait> Adds a random trait from the list to the

add_random_trait brilliant_strategist /Hearts of Iron IV/common/unit_leader/*.txt 1.5 The trait to add. character.

inflexible_strategist } files.

The trait to add. add_timed_unit_leader_trait = { Traits are found in add_timed_unit_leader_trait     trait = wounded Adds the specified trait to the current unit

/Hearts of Iron IV/common/unit_leader/*.txt 1.5 days = <int>     days = 90 leader for the specified duration.

The duration of the trait. } files.

Traits are found in /Hearts of Iron IV/common/unit_leader/*.txt

trait = <trait>

The trait to replace. replace_unit_leader_trait = { files.

replace_unit_leader_trait     trait = old_guard Replaces the specified trait with the new trait. Warning: This effect is extremely buggy. It does not 1.5 replace = <trait>     replace = brilliant_strategist

properly replace traits and is crash prone. Use The new trait to add. }

remove_unit_leader_trait and add_unit_leader_trait instead.

Remove the exile tag on an army leader, making them remove_exile_tag remove_exile_tag = yes Removes a leaders exile tag.   1.6

no longer be considered exile leaders.

Adds experience to the current unit leader, gain_xp <int> gain_xp = 5 Cannot be used with negatives. 1.9

promoting to the next skill level if applicable.

remove_unit_leader <bool> remove_unit_leader = yes Removes the current unit leader. 1.0

<bool> Removes every unit leader role from the remove_unit_leader_role remove_unit_leader_role = yes 1.11

Boolean. character

Country leaders ​[ edit | edit source ]

These can only be used with characters of the country leader type.

Country leader-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added

The trait to add. add_country_leader_trait =

nationalist_symbol

OR: Traits are found in ideology = <sub-ideology> Adds the specified trait to the current /Hearts of Iron IV/common/country_leader/*.txt

add_country_leader_trait 1.11 The sub-ideology of the country leader role to which add_country_leader_trait = { character. files. The former only if the character has one country leader the trait is added.     ideology = marxism role.

trait = <trait>

} The trait to add.

The trait to remove. remove_country_leader_trait =

nationalist_symbol

OR: Traits are found in ideology = <sub-ideology> Removes the specified trait from the current /Hearts of Iron IV/common/country_leader/*.txt

remove_country_leader_trait 1.11 The sub-ideology of the country leader role to which remove_country_leader_trait = { character. files. The former only if the character has one country leader the trait is added.     ideology = marxism role.

trait = <trait>

} The trait to remove.

remove = <trait>

Trait to remove swap_country_leader_traits = {

add = <trait>

swap_country_leader_traits     add = anti_communist Swaps traits of the current character. Use swap_ruler_traits in country scope. 1.11 Trait to add

ideology = <sub-ideology> }

Sub-ideology of the leader where to swap traits.

Combat ​[ edit | edit source ]

Combat-related unit leader-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added

<int> / <variable> Adds the specified amount of hours of supply to supply_units supply_units = 24   1.5

The amount of hours of supply. troops led by the current unit leader.

<int> Adds the specified amount of assignable trait slots add_max_trait add_max_trait = 1   1.5

The amount to add. to the current unit leader.

The skill to add.

How many skill levels to add.

How many skill levels to add.

How many skill levels to add.

How many skill levels to add.

How many skill levels to add.

How many skill levels to add.

combat_offense = <float>

The bonus to grant. Optional.

combat_breakthrough = <float>

The bonus to grant. Optional. combat_defense = <float>

The bonus to grant. Optional. combat_entrenchment = <float>

add_temporary_buff_to_units = { The bonus to grant. Optional.     combat_offense = 0.25

org_damage_multiplier = <float>     combat_breakthrough = 0.25

The bonus to grant. Optional.     org_damage_multiplier = -1.0

str_damage_multiplier = <float> Adds the specified combat buff to the current unit

add_temporary_buff_to_units     war_support_reduction_on_damage = 0.2   1.5 The bonus to grant. Optional. leader.

war_support_reduction_on_damage = <float>

The bonus to grant. Optional.     days = 7

cannot_retreat_while_attacking = <float>     tooltip = ABILITY_FORCE_ATTACK_TOOLTIP

} The bonus to grant. Optional. cannot_retreat_while_defending = <float>

The bonus to grant. Optional. days = <int>

The duration of the buff. Optional. tooltip = <string>

The tooltip to display for the buff.

Operatives ​[ edit | edit source ]

Operative-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added

The country to set the nationality to.

captured_by = <tag>

By which country to get captured. capture_operative = {

Makes the current operative be captured by a capture_operative     captured_by = POL 1.9

ignore_death_chance = <bool>     ignore_death_chance = yes specific country. Whether to ignore the death chance on capture (no by default). }

force_operative_leader_into_hiding <bool> force_operative_leader_into_hiding = yes Forces the current operative into hiding. 1.9

captured_by = <tag> free_operative free_operative = { captured_by = POL } Frees the current operative. 1.9

The country that captured the operative.

How much to harm the operative.

killed_by = <tag> kill_operative kill_operative = { killed_by = POL } Kills the current operative. 1.9

The country that'll kill the operative.

This counts as the operative dying turn_operative = { Turns the current operative against their own

turned_by = <tag> and will trigger the corresponding On turn_operative     turned_by = PREV country, transferring them to the specified 1.9

The country to which the operative defects. action. Logs an error if used against } country.

your own operative.

id = <event>

The event to fire.

days = <int> / <variable>

Fires the event in the specified number of days. Optional. hours = <int> / <variable>

Fires the event in the specified number of hours. Optional. random = <int> / <variable>

Adds a random number (between 0 and random, inclusive) of hours to the scheduled fire time. Optional. operative_leader_event = {

random_days = <int> / <variable>

Adds a random number (between 0 and random_days, originator = POL the current operative portrait. recipient = GER

inclusive) of days to the scheduled fire time. Optional.     days = 10 Where triggers do not need to be operative_leader_event originator = <tag>     random = 50 Fires the specified event for the operative. repeatedly checked random  can be 1.9

The originator of the event. Optional, defaults to owner of     random_days = 10 a performance light alternative to operative. set_from = ENG

mean_time_to_happen  for set_root = SOV

recipient = <tag> scheduling events. set_from_from = FRA

The recipient of the event. Optional, defaults to owner of }

operative. set_from = <tag>

Sets the scope of FROM in scripted localization. Optional. set_from_from = <tag>

Sets the scope of FROM.FROM in scripted localization. Optional. set_root = <tag>

Sets the scope of ROOT in scripted localization. Optional.

Division scope ​[ edit | edit source ]

The effects here must be used within a division scope.

Division-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added

Boolean.

key = <localisation key>

The name of the entry. add_history_entry = {

subject = "<string>"     key = my_history_entry Creates an entry within the command history of a add_history_entry Logged entry. Never shown to the player.     subject = "Test entry" 1.12

division. allow = <bool>     allow = no

Whether a medal can be awarded to the division over the }

history entry.

change_division_template = { <string> Changes the template of the division to the

change_division_template     division_template = "New template" 1.12 The name of the division. specified one.

}

Only possible to use if the division <character>

add_random_valid_trait_from_unit add_random_valid_trait_from_unit = FROM Adds a random valid unit trait to a unit leader. scope is the same as the ROOT 1.12 Character to grant the trait to.

scope.

add_unit_medal_to_latest_entry = { unit_medals = <medal ID> Adds the specified medal to the latest entry within

add_unit_medal_to_latest_entry     unit_medals = my_medal 1.12 The medal to add. the unit's history.

}

<decimal> Adds the specified amount of experience to the add_divisional_commander_xp add_divisional_commander_xp = 10 1.12

Experience to add. divisional commander.

<int> Re-randomises the division commander using the reseed_division_commander reseed_division_commander = 760 Does not have a tooltip. 1.12

The seed to use. given seed.

Boolean.

The level to set to.

MIO scope ​[ edit | edit source ]

The effects here must be used within a military industrial organisation scope.

MIO-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added If the amount goes above the "Size Up" limit, the MIO will

Funds to add. amount of funds is capped at 0 from below.

If the amount goes above the <int> "Size Up" limit, the MIO will

set_mio_funds set_mio_funds = 1000 Sets the funds of a MIO to the certain level. 1.13 Amount to set. automatically gains sizes. Cannot

be negative.

<decimal> The multiplier is capped at 0 from add_mio_funds_gain_factor add_mio_funds_gain_factor = 0.1 Changes the base multiplier to MIO's funds. 1.13

Amount to add. below.

Amount to set.

<int> Funds will not be changed by the add_mio_size add_mio_size = 2 Adds sizes to the MIO. 1.13

Amount to add. effect. Cannot be negative.

<decimal> Changes the base multiplier to the The multiplier is capped at 0 from add_mio_size_up_requirement_factor add_mio_size_up_requirement_factor = 0.1 1.13

Amount to add. requirement to size up a MIO. below.

<decimal> Changes the base multiplier to the set_mio_size_up_requirement_factor set_mio_size_up_requirement_factor = 0.1 Cannot be negative. 1.13

Amount to set. requirement to size up a MIO.

If the capacity is reduced to below the amount of assigned tasks,

<int> Changes the base maximum task capacity of add_mio_task_capacity add_mio_task_capacity = 2 they'll be turned allowed. The 1.13

Amount to add. the MIO. base amount is capped at 0 from below. Doesn't instantly apply.

If the capacity is reduced to below the amount of assigned tasks,

<int> Changes the base maximum task capacity of set_mio_task_capacity set_mio_task_capacity = 2 they'll be turned allowed. Cannot 1.13

Amount to set. the MIO. be negative. Doesn't instantly apply.

<decimal> Changes the base research bonus of the The base amount is capped at 0 add_mio_research_bonus add_mio_research_bonus = 0.3 1.13

Amount to add. MIO. from below.

<decimal> Changes the base research bonus of the set_mio_research_bonus set_mio_research_bonus = 0.3 Cannot be negative. 1.13

Amount to set. MIO.

May also refer to a scripted <localisation key>

set_mio_name_key set_mio_name_key = mio_new_name Changes the name of the MIO. localisation definition, which'll be 1.13 The new name.

evaluated in MIO's scope.

The new sprite.

<decimal> Changes the base political power cost of the The base amount is capped at 0 add_mio_design_team_assign_cost add_mio_design_team_assign_cost = 0.3 1.13

Amount to add. MIO to assign research. from below.

<decimal> Changes the base political power cost of the set_mio_design_team_assign_cost set_mio_design_team_assign_cost = 0.3 Cannot be negative. 1.13

Amount to set. MIO to assign research.

<decimal> The base amount is capped at 0 add_mio_industrial_manufacturer_assign_cost add_mio_industrial_manufacturer_assign_cost Changes the base political power cost of the

1.13 Amount to add. = 0.3 MIO to assign production lines. from below.

Cannot be negative. 1.13 Amount to set. = 0.3 MIO to assign production lines.

The base amount is capped at 0 from below. Rounded down, e.g.

<decimal> Changes the base experience cost of the add_mio_design_team_change_cost add_mio_design_team_change_cost = 0.3 0.3  with a cost of 5  should 1.13

Amount to add. MIO to assign to equipment by a percentage. result in 6.5 , but becomes 6 instead.

Cannot be negative. Rounded <decimal> Changes the base experience cost of the down, e.g. 0.3  with a cost of 5

set_mio_design_team_change_cost set_mio_design_team_change_cost = 0.3 1.13 Amount to set. MIO to assign to equipment by a percentage. should result in 6.5 , but

becomes 6  instead.

Trait to display. unlock_mio_trait_tooltip = my_trait_1

OR trait = <trait> Displays a tooltip that says that the trait is Doesn't change the availability of

unlock_mio_trait_tooltip Trait to display. unlock_mio_trait_tooltip = { 1.13

made available. the trait directly. trait = my_trait_2

show_modifiers = <bool> show_modifiers = no

Whether the trait's modifiers should be shown in the }

tooltip. Defaults to true.

Trait to complete. complete_mio_trait = my_trait_1

OR Automatically adds 1 size to the

trait = <trait> complete_mio_trait Completes the specified MIO trait. MIO. No checks are placed on the 1.13

Trait to complete. complete_mio_trait = {

show_modifiers = <bool>

Whether the trait's modifiers should be shown in the }

tooltip. Defaults to true.

An unique string to identify the MIO flag with.

OR set_mio_flag = my_flag

flag = <flag>

The flag to set. set_mio_flag days = <int> set_mio_flag = { Defines a MIO flag. No tooltip is shown. 1.13

Sets the flag to last for the specified amount of days.     flag = my_flag

Optional.     value = 1

value = <int> }

The new value of the flag on the scale from -2 147 483 648 to 2 147 483 647.

The unique string of a country flag to clear.

flag = <flag>

The flag to modify.

value = <value> modify_mio_flag = {

modify_mio_flag The value to add to the flag. Defaults to 0.     flag = my_flag Adds an integer value to a flag. The flag must be already set. 1.13 value = 3

days = <int> }

The amount of days that the flag should last for before being cleared. Optional, defaults to permanent.

Contract scope ​[ edit | edit source ]

The effects here must be used within a contract scope.

Contract-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added

Boolean.

Raid scope ​[ edit | edit source ]

The effects here must be used within a raid scope.

Raid-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added

add_raid_history_entry <bool> add_raid_history_entry = yes/no Add history entry to a raid. 1.15

The value defines the progress <float> Will give experience to any type of unit assigned to towards the max level, e.g. 0.2 = gain

raid_add_unit_experience raid_add_unit_experience = 0.2 1.15 Can use either an explicit value or a variable the raid, e.g. divisions or air wings. 20% of the experience needed to

reach max level.

# Apply 50% damage to units

raid_damage_units = {

damage = 0.5

<flag> ratio = yes

An unique string to identify the project flag with. }

OR # Apply 10 strength loss and 20 organization

loss to units If 'ratio = yes', then all damage / losses damage = <float/int>

The amount of strength and organization damage taken. raid_damage_units = { are applied as a fraction of the current org_damage = 20 amount.

org_damage = <float/int> str_damage = 10 Damage is applied to ground units while damage to raid_damage_units The amount of organization damage taken. } For units, damage can be defined 1.15

plane is defined as the amount of planes lost. str_damage = <float/int> through one value 'damage' or

The amount of strength damage taken # Lose 40% of all planes separately through 'org_damage' and raid_damage_units = {

plane_loss = <float/int> 'str_damage' plane_loss = 0.4

The amount of planes lost ratio = yes

ratio = <bool> }

optional, default no

# Lose 5 planes

raid_damage_units = {

plane_loss = 5

}

Special Project scope ​[ edit | edit source ]

The effects here must be used within a special project scope. Special projects must always be pre-pended with sp:<special project>  when used a a scope or value.

special_project-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added

sp:my_project = { The input value is a ratio of the total <float>

add_project_progress_ratio   add_project_progress_ratio = 0.1 Add progress to the project's prototype phase. needed progress to complete the 1.15 remove or add between -1 and 1 proect progress   add_project_progress_ratio = var:my_var

} special project.

prototype_reward = <prototype_reward>

The protypereward to compete

prototyp_reward_option = my_option complete_prototype_reward_option = {

If multiple choice use the given one, use default one if not set. prototype_reward = my_reward The effect will respect the fire only Complete a prototype reward option for the

complete_prototype_reward_option prototyp_reward_option = my_option once and allowed property of 1.15 Optional. project in scope

show_modifiers = yes prototype rewards. show_modifiers = <bool> }

Yes if the effects of the prototype reward should be shown (default no)

An unique string to identify the project flag with. set_project_flag = my_flag

OR flag = <flag>

The flag to set. set_project_flag set_project_flag = { Defines a project flag. No tooltip is shown. 1.15

days = <int>     flag = my_flag

Sets the flag to last for the specified amount of days. Optional.     days = 123

value = <int>

} The new value of the flag on the scale from -2 147 483 648 to 2 147 483 647.

The unique string of a country flag to clear.

flag = <flag>

The flag to modify.

value = <value> modify_mproject_flag = {

modify_project_flag The value to add to the flag. Defaults to 0.     flag = my_flag Adds an integer value to a flag. The flag must be already set. 1.15 value = 3

days = <int> }

The amount of days that the flag should last for before being cleared. Optional, defaults to permanent.

Other scopes ​[ edit | edit source ]

The effects here must be used within a scope that's specified within the notes.

Otherwise-scoped effects: [Collapse]

Version Name Parameters Examples Description Notes

Added

amount = <int> execute_operation_coordinated_strike = { All prepared Port Strike and Strategic Bombing execute_operation_coordinated_strike How many times the operation will get executed within the     amount = 12 in the target region will execute multiple times Can only be used within operations. 1.9

days set in the operation. } without air defence being able to intercept them.

Flow control ​[ edit | edit source ]

These scopes are used within effect scopes to control the execution of effects.

If statements ​[ edit | edit source ]

An if statement allows an execution of effects to only be done if certain triggers are met. Conditional statements are represented with the if = { ... }  effect. limit = { ... }  inside of the if statement serves as a trigger block that defines the conditions when it should be executed, and everything else directly inside of if = { ... }  is interpreted as the effects that should be executed if the condition is true.

For example, the following will add 10% Stability to the country this is executed on if it has positive Political Power and below 90% stability:

if = {

}

If the limit is not met, then none of the effects inside will be executed. If it is, then each one will be. If the limit is omitted, it defaults to being always true.

The effects must be inside of the if statement to be tied to the limit. For example, this will always give 100 Political Power, regardless of what country is played:

if = {

}                         # Closes if = { ... }. Since no effects are inside, this means that the if statement does absolutely nothing

add_political_power = 100 # Outside of if = { ... }, so it will always give 100 political power, even if not playing as BHR

Optionally, else_if = { ... }  (with limit = { ... }  serving in a similar fashion) and else = { ... }  can be added. If the initial limit within if = { ... }  is false, it moves on to the next else_if = { ... } , checking the limit there. If the limit there is false, then it moves on to the next one, until hitting an end or an else = { ... } . Two variants exist: nested and unnested. In the first case, the else_if  or else  is put directly inside of the preceding if  or else_if , while in the second case it's put right after. In case of overlap, unnested if statements are preferred. Here is an example using unnested if statements:

if = {

}

else_if = {

}

else = {

}

Within the tooltip, only effects that would be executed are shown. The effects within an unfulfilled if statement (or an else / else_if  that's not read due to the if statement being met) will be hidden from the player, and so will the trigger. In order to avoid player confusion, custom effect tooltips can be used to tell the player what this effect block would do, such as being used within an else .

Random effects ​[ edit | edit source ]

If you want an effect to have a random chance to be done or have nothing happen otherwise, the random = { ... }  block is the simplest way to accomplish that:

random = {

}

This in particular will have an 80% chance to add 40% stability and 30% war support and, accordingly, a 20% chance to do nothing. The chance here is on the scale from 0 to 100.

If you want the game to choose between effect blocks, random_list can be used instead. For example, if you wanted an effect to randomly given the player one out of four bonuses, you'd do the following:

random_list = {

}

The number is not the chance, but the weight for each option, as they don't have to add up to 100 or any number. An option with the weight of 20 is twice as likely to be picked as the option with the chance of 10, for instance. In total, the probability for an option to be picked is equal to the weight of the option divided by the sum of all weights.

It is also possible to use modifiers (akin to MTTH blocks) to affect the weight of each possible random effect or to use variables as chances.

random_list = {

}

If the country flag inward_perfect_flag is set, it'll multiply the above chance of 30 by 1.3 to get 39. Meanwhile, my_variable  will take the value of the according temp variable or the current scope's variable as the weight of the option.

Note that if you want to create a repeatable decision including a random list, by default the same decision will pick the same random result every time it is triggered in a game. You can reverse this behaviour by including the following line in the decision block:

fixed_random_seed = no

This is only for decisions. Elsewhere, random seed is unfixed by default, making this argument unnecessary to set to "no".

Tooltip manipulation ​[ edit | edit source ] See also: Localisation

The "tooltip" in this case refers to the text shown to the player in-game that explains what the effect block changes within the game, such as "+50 Political Power".

There are 3 ways to edit the tooltip within an effect block:

hidden_effect = { ... }  is used in order to hide the effects within from the tooltip, making their execution not get shown to the player. effect_tooltip = { ... }  is, instead, used in order to put the effects into the tooltip without actually executing them. custom_effect_tooltip = my_localisation_key  is used in order to put an arbitrary paragraph of text as an effect that will get executed.

For example, this sample focus' completion reward utilises all three:

completion_reward = {

}

In this case, send_event_to_subjects_tt and reject_war_tt are localisation keys defined within any /Hearts of Iron IV/localisation/english/*_l_english.yml file encoded with UTF-8-BOM, assuming the English language.

send_event_to_subjects_tt: "Sends a demand to our every subject.\nIf they agree, we get the following for each subject:"

reject_war_tt: "If they reject the demand, we gain a wargoal against them."

In-game, this will appear as such:

Effect:

Sends a demand to our every subject. If they agree, we get the following for each subject: Political Power: +100 If they reject the demand, we gain a wargoal against them.

Noticably, the effect that fires the country event gets hidden from the tooltip. After completing the focus, the only thing that happens is that every subject country receives an event with the ID of my_event.1 , the country does not immediately gain 100 political power.

Meta effects ​[ edit | edit source ]

Meta effects allow you to use non-dynamic effects (the ones that do not accept modifiers and can only use static tokens or constant values) as if they were accepting variables.

add_equipment_to_stockpile = {

}

In the effect shown above, amount of equipment added is dynamic and can be set using the variable "eq_amount". However, this effect does not let you use a variable as equipment type. You can not store "infantry_equipment_2" in a variable and use it here.

However, meta effects will let you use variables and scripted localization within them to build effects as if they were texts and run them. Let's make previous effect accept equipment type and equipment level as variables stored in "eq_type" and "eq_level".

set_variable = { eq_type = 1 } # Sets the equipment type to "1", which determines the equipment given using scripted localisation, included below

set_variable = { eq_amount = 10 } # Sets the amount of equipment given to 10

set_variable = { eq_level = 2 } # Sets the equipment level to 2, which is used directly in the meta effect, no scripted localisation required

meta_effect = { # The actual meta effect. This can go anywhere you need it: in a decision, in a scripted effect, in a scripted GUI click effect, etc...

}

# The scripted localization for the "eq_type" variable, which goes in a scripted localisation file

defined_text = { # Since the "eq_type" variable in this example is equal to 1, the equipment given by the effect is "artillery_equipment"

}

As you can see, we have created a meta_effect that takes two arguments. These arguments will be used replacing the parameters [EQ_TYPE] and [EQ_LEVEL] inside the meta effect. EQ_LEVEL will be replaced by [?eq_level|.0] which is the integer value of eq_level (in this case 2.000 becomes 2). EQ_TYPE is a bit more complicated, it is being replaced by a scripted localization. This scripted localization will check eq_type variable and depending on its value it will return the key token for the equipment. If it is 0, it will return "infantry_equipment". If it is 1, it will return "artillery_equipment".

So the final result is [EQ_TYPE] is being replaced by "artillery_equipment" and [EQ_LEVEL] is being replaced by "2" and in the end our effect will be built as:

add_equipment_to_stockpile = {

}

which will give you 10 artillery_equipment_2.

debug = yes can be added to meta effects. Which will print the final effect to game.log when the effect is executed and make debugging easier.

Scripted effects ​[ edit | edit source ]

Scripted effects serve a similar purpose to functions  in that they can be defined in /Hearts of Iron IV/common/scripted_effects/*.txt and then used elsewhere as a shortened version. A scripted effect will never run by itself and requires being used as an effect elsewhere to be executed. Alongside that, the game allows the creation of custom console commands, which are scripted effects.

A scripted effect is defined simply as

scripted_effect_name = {

}

This example can be used as an effect in regular code as scripted_effect_name = yes .

Scripted effects can be accessed in console by typing e scripted_effect_name  to run them.

To create a custom console command, the scripted effect's name should begin with d_ . The console command itself does not include d_ , so d_test_command  would be run in console as test_command In custom console commands, the country running the command is FROM, while ROOT is the selected country, state, or character. Anything entered after the console command, separated by spaces like test_command 123 321 GER  is added to the 'args' temp array. An example of a scripted effect which will transfer every state entered as an argument to the country that runs the console command is

d_transfer_states = {

for_each_scope_loop = {

array = args

FROM = {

transfer_state = PREV

}

}

}

used like transfer_states 123 321

Useful scripted effects [ edit | edit source ]

These scripted effects are defined in base game and might be useful to keep in the mod to cut down on the amount of code. As scripted effects, all of these use a boolean value as argument.

Base game scripted effects: [Collapse]

Name Scope Example Description Notes

Creates a collaboration government, with the current The target of the collaboration government is stored in the instantiate_collaboration_government Country instantiate_collaboration_government = yes

scope as overlord. country_to_initiate  temp variable.

Switches the economy law one level towards total If already on total mobilisation, adds 150 Political Power. Must upgrade_economy_law Country upgrade_economy_law = yes

mobilisation. be adjusted manually for new laws.

Grants a random available intelligence agency gain_random_agency_upgrade Country gain_random_agency_upgrade = yes Only results in an agency being created if one doesn't exist.

upgrade.

Requires manual adjustment if new ideologies are added. See All of the ruling party's popularity gets added to the

add_ruling_to_dem Country add_ruling_to_dem = yes also: add_ruling_to_fas , add_ruling_to_com , Democratic ideology group.

add_ruling_to_neu

remove_any_country_role_from_character Character remove_any_country_role_from_character = yes Removes all advisor roles from the current scope. Requires manual adjustment if new slots are added.

Has no effect on small islands, megalopolises, or large_city Changes the state category to the next one that

increase_state_category State increase_state_category = yes (Dense Urban Region). city  (Urban Region) gets upgraded contains more building slots.

straight to Metropolis, skipping large_city .

, , and Failed to parse (SVG (MathML can be enabled via browser plugin): Invalid response ("Math extension cannot connect to Restbase.")

Creates the lerp_result  regular variable with Failed to parse (SVG (MathML can be from server

enabled via browser plugin): Invalid "https://en.wikipedia.org/api/rest_v1/":):

lerp Any lerp = yes response ("Math extension cannot {\displaystyle x} are stored as lerp_a , lerp_b ,

connect to Restbase.") from server lerp_x  temp variables. Failed to parse (SVG (MathML

"https://en.wikipedia.org/api/rest_v1/":): can be enabled via browser plugin): Invalid

{\displaystyle result := a + (b-a)\cdot x} response ("Math extension cannot connect to Restbase.") from server "https://en.wikipedia.org/api/rest_v1/":): {\displaystyle x} is clamped between 0 and 1.

The created array will be named Stores the current core states of the current scope in

store_core_states_on_game_start Country store_core_states_on_game_start = yes core_states_at_game_start . Intended to be called in an array in ROOT's scope.

country history only once.

Modding Documentation Effects • Triggers • Defines • Modifiers • List of modifiers • Scopes • Localisation • On actions • Data structures (Flags, Event targets, Country tag aliases, Variables, Arrays)

Achievements • AI • AI focuses • Autonomous states • Balances of power • Bookmarks/Scenarios (Game rules) • Buildings • Characters and traits • Cosmetic tags • Countries • Divisions • Decisions • Doctrines • Equipment • Events • Factions • Ideas • Ideologies • Military industrial organizations • Scripting

National focuses • Resources • Scripted GUI • Technologies and doctrines • Units

Map Map • States • Supply areas • Strategic regions

Graphical Interface • Graphical assets • Entities • Posteffects • Particles • Fonts

Cosmetic Portraits • Namelists • Music • Sound

Other Console commands • Troubleshooting • Mod structure • Mods • Nudger

Categories: 1.16 Modding Pages with math errors Pages with math render errors

About Hearts of Iron 4 Wiki Mobile view

GAMES COMMUNITY ABOUT SOCIAL MEDIA

Discover Paradox Forums News

Our Brands Paradox Wikis About us Legal Information

Browse Support Careers EU Online Dispute Resolution Report Illegal Content

Play on Paradox technology Join our playtests

Media contact