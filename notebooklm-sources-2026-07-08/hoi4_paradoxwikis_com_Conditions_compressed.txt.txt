hoi4_paradoxwikis_com__Conditions_compressed.txt
================================================

Paradox Wikis Hearts of Iron 4 Wiki Search Hearts of Iron 4 Log in

Page Discussion Read Edit Edit source View history

Triggers (Redirected from Conditions)

Conditions (also known as Triggers) are used to check whether certain conditions are fulfilled in the game's current state or not, without being able to change anything in the game's state.[a] These return a boolean (true This is a community maintained wiki. If you spot a mistake, please

or false), which may be interpreted by the block where it's used. Usually blocks that allow using triggers are an AND unless stated otherwise, which would instantly stop execution upon receiving a single false statement help with fixing it. and return false.

The list of triggers may be outdated. A complete, but unsorted, list of triggers can be found in /Hearts of Iron IV/documentation/triggers_documentation.html or /Hearts of Iron IV/documentation/triggers_documentation.md. As of 1.17.1.1, this file was last updated in the version 1.17.0.

Hearts of Iron 4 Wiki Recent changes Random page Contents [hide]

1 Operators Paradox Wikis

2 Context scopes

- Main portal 3 Scopes

- Wikis discord

3.1 Trigger scopes

- Contact us

- Style guidelines 3.2 Dual scopes

4 Flow control tools HoI4 links 5 Any scope

- Forums 5.1 General

- Game discord 5.2 Career profile

- Mods 5.3 Variables

- Store

- YouTube 5.4 Debugging

6 Country scope Tools 6.1 General What links here 6.2 National focuses Related changes 6.3 Politics Special pages 6.4 Balance of power Printable version 6.5 Buildings Permanent link Page information 6.6 Technology

6.7 Ideas 6.8 Diplomacy 6.9 Faction 6.10 War 6.11 State 6.12 Military 6.13 Doctrine 6.14 Equipment 6.15 Intelligence 6.16 AI 6.17 Characters 6.18 Peace conferences

7 Faction scope 8 State scope

8.1 General 8.2 Resistance and Compliance

9 Character scope 9.1 General 9.2 Advisors 9.3 Country leaders 9.4 Unit leaders 9.5 Operatives 9.6 Scientists

10 Combat 11 Division scope 12 MIO scope 13 Contract scope 14 Special projects 15 Meta triggers 16 Scripted triggers

16.1 Diplomacy scripted triggers 16.2 Resistance initiation triggers 16.3 Useful scripted triggers

17 References and notes

Operators ​[ edit | edit source ]

Every trigger uses one of the three operators: The equality sign =  or the comparison signs >  and < .

The equality sign can either mean strict equality (Unlike previous games in the series where it checked for being equal or greater than) or can serve as a way to introduce a block of additional triggers, as in has_opinion = { target = GRE value < -10 } . The comparison signs serves as strict comparison: has_political_power > 100  will not be true if the country has exactly 100 political power, for instance.

Not all triggers support the equality sign or comparison signs. See the details for each trigger in the notes for it. If not stated otherwise, the trigger only supports the equality sign.

Context scopes ​[ edit | edit source ]

There are three general scopes all triggers operate in:

country state character

They give context to a trigger by stating what the trigger is checking against.

Each country acts as a sub-scope of the general country scope, i.e. GER = { }  will check only against Germany, whereas any_country  will check against all countries. Likewise for states and characters.

Triggers won't work in scopes they are not assigned to. Country triggers will not work for states or vice-versa.

Scopes ​[ edit | edit source ]

Main article: Scopes

These don't serve as triggers, but rather as scopes that change for whom the triggers are being checked. Each one also serves as an AND statement.

Trigger scopes ​[ edit | edit source ]

This is a community maintained wiki. If you spot a mistake, please help with fixing it.

These can only be used as triggers; trying to use them as effects will result in nothing happening.

Trigger scopes: [Collapse]

Version Name Usage Target type Example Description

Added all_country Always usable Country all_country = { … } Checks if all countries meet the triggers. 1.0

any_country Always usable Country any_country = { … } Checks if any country meets the triggers. 1.0

all_other_country Within country scope only Country all_other_country = { … } Checks if all countries other than the one where this scope is located meet the triggers. 1.0

any_other_country Within country scope only Country any_other_country = { … } Checks if any country other than the one where this scope is located meets the triggers. 1.0

all_country_with_original_tag = {

all_country_with_original_tag Always usable Country #required created for civil wars and other purposes, meet the triggers. original_tag_to_check = 1.9

check TAG  is used to specify the original tag. }

any_country_with_original_tag = {

any_country_with_original_tag Always usable Country #required created for civil wars and other purposes, meets the triggers. original_tag_to_check = 1.9

check TAG  is used to specify the original tag. }

all_neighbor_country Within country scope only Country all_neighbor_country = { … } Checks if all countries that border the one where this scope is located meet the triggers. 1.0

any_neighbor_country Within country scope only Country any_neighbor_country = { … } Checks if any country that borders the one where this scope is located meets the triggers. 1.0

Checks if any country that borders the one where this scope is located, as well as being in its any_home_area_neighbor_country Within country scope only Country any_home_area_neighbor_country = { … } home area - meaning a direct land connection between the capitals of countries - meets the 1.0

triggers.

Checks if all countries that are guaranteed by the one where this scope is located meet the all_guaranteed_country Within country scope only Country all_guaranteed_country = { … } 1.9

triggers.

Checks if any country that is guaranteed by the one where this scope is located meets the any_guaranteed_country Within country scope only Country any_guaranteed_country = { … } 1.9

triggers.

Checks if all countries that are allied with the one where this scope is located - meaning that all_allied_country Within country scope only Country all_allied_country = { … } they are either a subject of the country, its overlord, or that they share a faction - meet the 1.9

triggers. Does not include the country itself.

Checks if any country that is allied with the one where this scope is located - meaning that any_allied_country Within country scope only Country any_allied_country = { … } they are either a subject of the country, its overlord, or that they share a faction - meets the 1.9

triggers. Does not include the country itself.

Checks if all countries that are occupied by the one where this scope is located - meaning all_occupied_country Within country scope only Country all_occupied_country = { … } that the occupied country has core states controlled by the occupier country - meet the 1.9

triggers.

Checks if any country that is occupied by the one where this scope is located - meaning that any_occupied_country Within country scope only Country any_occupied_country = { … } 1.9

the occupied country has core states controlled by the occupier country - meets the triggers.

Checks if all countries that are at war with the one where this scope is located meet the all_enemy_country Within country scope only Country all_enemy_country = { … } 1.9

triggers.

Checks if any country that are at war with the one where this scope is located meets the any_enemy_country Within country scope only Country any_enemy_country = { … } 1.9

triggers.

Checks if all countries that are a subject of the one where this scope is located meet the all_subject_countries Within country scope only Country all_subject_countries = { … } 1.11

triggers. Notice the plural spelling in the scope.

Checks if any country that is a subject of the one where this scope is located meets the any_subject_country Within country scope only Country any_subject_country = { … } 1.11

triggers.

Checks if any country that has the current scope as a core state meets the triggers. Does not any_country_with_core Within state scope only Country any_country_with_core = { … } 1.12

have an equivalent for other effect/trigger scope types.

all_country_of = {

tooltip = my_loc # Optional

bindable localization

target = { SWE NOR FIN DEN ICE }

has_defensive_war = yes

}

Checks if all of the provided countries fulfill the specified triggers. The `target` supports script all_country_of Alway usable Country 1.17

all_country_of = { constants and `tooltip` supports bindable localization. tooltip = my_loc # Optional bindable

localization

target =

constant:country_groups:nordics

has_defensive_war = yes

}

any_country_of = {

tooltip = my_loc # Optional

bindable localization

target = { SWE NOR FIN DEN ICE }

has_defensive_war = yes

}

Checks if any of the provided countries fulfills the specified triggers. The `target` supports any_country_of Alway usable Country 1.17

any_country_of = { script constants and `tooltip` supports bindable localization. tooltip = my_loc # Optional bindable

localization

target =

constant:country_groups:nordics

has_defensive_war = yes

}

all_state Always usable State all_state = { … } Check if all states meet the triggers. 1.0

any_state Always usable State any_state = { … } Check if any state meets the triggers. 1.0

any_state_in = {

array = array_of_states  #required

check

}

any_state_in Always usable State Requires on of the following fields Check if any state in the given category meets the trigger. 1.15

array = <array_of_states>

continent = <continent_name>

ai_area = <ai_area_name>

strategic_region =

<strategic_region_number>

any_state_of = {

tooltip = my_loc # Optional

bindable localization

target = { 1 42 1992 }

controller = {

has_defensive_war = yes

}

}

Checks if any of the provided states fulfills the specified triggers. The `target` supports script any_state_of Alway usable State 1.17

any_state_of = { constants and `tooltip` supports bindable localization. tooltip = my_loc # Optional bindable

localization

target =

constant:country_groups:nordics

controller = {

has_defensive_war = yes

}

}

all_neighbor_state Within state scope only State all_neighbor_state = { … } Check if all states that are neighbour to the one where this scope is located meet the triggers. 1.0

any_neighbor_state Within state scope only State any_neighbor_state = { … } Check if any state that is neighbour to the one where this scope is located meets the triggers. 1.0

Check if all states that are owned by the country where this scope is located meet the all_owned_state Within country scope only State all_owned_state = { … } 1.0

triggers.

Check if any state that is owned by the country where this scope is located meets the any_owned_state Within country scope only State any_owned_state = { … } 1.0

triggers.

all_core_state Within country scope only State all_core_state = { … } Check if any state that is cored by the country where this scope is located meets the triggers. 1.11

any_core_state Within country scope only State any_core_state = { … } Check if all states that are cored by the country where this scope is located meet the triggers. 1.11

Check if all states that are controlled by the country where this scope is located meet the all_controlled_state Within country scope only State all_controlled_state = { … } 1.9

triggers.

Check if any state that is controlled by the country where this scope is located meets the any_controlled_state Within country scope only State any_controlled_state = { … } 1.9

triggers.

Checks if all unit leaders (corps commanders, field marshals, admirals) that are employed by all_unit_leader Within country scope only Unit Leader all_unit_leader = { … } 1.5

the country where this scope is located meet the triggers.

Checks if any unit leader (corps commander, field marshal, admiral) that is employed by the any_unit_leader Within country scope only Unit Leader any_unit_leader = { … } 1.5

country where this scope is located meets the triggers.

Checks if all army leaders that are employed by the country where this scope is located meet all_army_leader Within country scope only Unit Leader all_army_leader = { … } 1.5

the triggers.

Checks if any army leader that is employed by the country where this scope is located meets any_army_leader Within country scope only Unit Leader any_army_leader = { … } 1.5

the triggers.

Checks if all navy leaders that are employed by the country where this scope is located meet all_navy_leader Within country scope only Unit Leader all_navy_leader = { … } 1.5

the triggers.

Checks if any navy leader that is employed by the country where this scope is located meets any_navy_leader Within country scope only Unit Leader any_navy_leader = { … } 1.5

the triggers.

Within country scope or operations Checks if all operatives that are employed by the country where this scope is located meet all_operative_leader Operative all_operative_leader = { … } 1.9

only the triggers.

Within country scope or operations Checks if any operative that is employed by the country where this scope is located meets the any_operative_leader Operative any_operative_leader = { … } 1.9

only triggers.

Checks if all characters that are recruited by the country where this scope is located meet the all_character Within country scope only Character all_character = { … } 1.11

triggers.

Checks if any character that is recruited by the country where this scope is located meets the any_character Within country scope only Character any_character = { … } 1.11

triggers.

any_country_division Within country scope only Division any_country_division = { … } Checks if any division owned by the current country meets the triggers. 1.12

any_state_division Within state scope only Division any_state_division = { … } Checks if any division within the current state meets the triggers. 1.12

all_military_industrial_organization = { … all_military_industrial_organization Within country scope only MIO Checks if all MIOs within the current country meet the conditions. 1.13

}

any_military_industrial_organization = { … any_military_industrial_organization Within country scope only MIO Checks if any MIO within the current country meets the conditions. 1.13

}

all_purchase_contract Within country scope only Purchase contract all_purchase_contract = { … } Checks if all purchase contracts within the current country meet the conditions. 1.13

any_purchase_contract Within country scope only Purchase contract any_purchase_contract = { … } Checks if any purchase contract within the current country meets the conditions. 1.13

all_scientists Within country scope only Character all_scientistst = { … } Checks if all scientists of the Country in scope matches the triggers. 1.15

any_scientist Within country scope only Character any_scientist = { … } Checks if at least one active scientist of the Country in scope matches the triggers. 1.15

all_active_scientist Within country scope only Character all_active_scientist = { … } Checks if all active scientists of the Country in scope matches the triggers. 1.15

any_active_scientist Within country scope only Character any_active_scientist = { … } Checks if at least one active scientist of the Country in scope matches the triggers. 1.15

Dual scopes ​[ edit | edit source ]

The following scopes can be used either as effect or trigger scopes; some can also be used as the right side of some effects and triggers as a target. If usage as a target is possible, it's marked within the table.

Several dual scopes may have a scope that varies depending on where it's used, such as variables, which can be set to anything.

Dual scopes: [Collapse]

Usable Version

Name Usage Target type Example Description as Added

target The country defined by the tag or tag alias. Tag aliases are defined in /Hearts of Iron IV/common/country_tag_aliases, as a way to refer to a specific country (such

TAG Always usable Country scope SOV = { country_event = my_event.1 } ✓ 1.0 as a side in a civil war) in addition to its actual tag. If the country with the exact tag doesn't exist, but a dynamic country originating from the specified tag does, the scope will refer to the dynamic country.

<state_id> Always usable State scope 123 = { transfer_state_to = SCO } The state defined by this id. ✓ 1.0

not within Character ENG_theodore_makhno = { set_nationality = On game versions prior to 1.12.8, the character must be already recruited by the country this is scoped <character> Character scope ✓ 1.11

scope UKR } from.

The MIO identified by that ID as defined within the Within country scope

mio:<MIO> MIO scope mio:AST_cockatoo_doe_organization = { … } /Hearts of Iron IV/common/military_industrial_organization/organizations/*.txt ✓ 1.13 only

file.

sp: Within country scope The special project identified by that ID as defined within the Special project scope sp:sp_land_flamethrower_tank = { … } ✓ 1.15

<special_project> only /Hearts of Iron IV/common/special_projects/projects/*.txt file.

ENG = {

ROOT Always usable Depends on usage                 type = annex_everything ✓ 1.0 distinguish between the default scope and ROOT, such as certain scripted GUI contexts or certain on

} #GER declares war on ENG (if there is

no scope before ENG)

Targets the current scope where it's used. For example, when used in every_state, it will refer to the state that's currently being evaluated. Primarily useful for variables (as in the example, where omitting it

THIS Always usable Depends on usage set_temp_variable = { target_country = wouldn't work) or for built-in localisation commands, where some scope must be specified. More rarely, ✓ 1.0 THIS }

this may help with scope manipulation when using PREV. Since omitting it makes no difference in how the code gets interpreted, there is little to no usage outside of these cases.

FRA = {

Targets the scope that the current scope is contained in. Can have additional applications where the

PREV Always usable Depends on usage                 target = PREV chained indefinitely as PREV.PREV. Commonly results in broken-looking tooltips: what's shown to the ✓ 1.0

} #Germany declares war on random_country

declare_war_on = { Can be chained indefinitely as FROM.FROM. Used to target various hardcoded scopes inherent to the

FROM Always usable Depends on usage } In events, this refers to the country that sent the event (i.e. if the event was fired using an effect, then it's ✓ 1.0 FROM = { the ROOT scope where it was fired). load_oob = defend_ourselves In targeted decisions or diplomacy scripted triggers, this refers to the scope that is targeted. }

Within country scope overlord Country scope overlord = { … } The overlord of the country if it is a subject. Subject to the 'invalid event target' error. X 1.3

only

Within country scope faction_leader Country scope faction_leader = { add_to_faction = FROM } Faction leader of the faction the country is a part of. Subject to the 'invalid event target' error. X 1.10.1

only

In state scope, the country that owns the state. In combatant scope, the country that owns the divisions. In Within state, character,

owner Country scope owner = { add_ideas = owns_this_state } character scope, the country that has recruited the character. Subject to the 'invalid event target' error X 1.0 or combatant scope only

when used for a state.

controller = {

controller Within state scope only Country scope             type = take_state_focus The controller of the current state. Subject to the 'invalid event target' error. X 1.0 generator = { 123 }

}

Within country scope The state where the capital of the current country is located in. Subject to the 'invalid event target' error in capital_scope State scope capital_scope = { … } X 1.0

only rare cases.

event_target: Saved event target or global event target, with no space after the colon. Subject to the 'invalid event target' Always usable Depends on usage event_target:my_event_target = { … } ✓ 1.0

<event_target_key> error.

var:my_variable = { … } Variable set to a scope. var:<variable> Always usable Depends on usage add_to_faction = my_variable  or ✓ 1.5

When used as a target rather than a scope, the var:  can be omitted in most cases. add_to_faction = var:my_variable

Flow control tools ​[ edit | edit source ]

Main article: Scopes#Flow control tools

These are triggers that serve as more of a way to establish a connection in how triggers are evaluated. Each one serves as a trigger scope with additional arguments and can be used regardless of scope.

Flow control tools: [Collapse]

Version Name Additional parameters Examples Description Notes

Added

AND = { Returns false if any sub-trigger returns false, true Only necessary within OR statements AND None     original_tag = GER otherwise. Evaluation stops at the first false sub- and NOT statements, as everything 1.0

} trigger. else is implicitly AND.

OR = { Returns true if any sub-trigger returns true, false OR None     original_tag = ENG otherwise. Evaluation stops at the first true sub- 1.0

} trigger.

NOT = { Returns false if any sub-trigger returns true, true ivalent to a NOR rather than a

NOT None     has_stability > 0.5 Equ otherwise. Evaluation stops at the first true sub- 1.0

} trigger.

count_triggers = {

amount = <int>

count_triggers     10 = { state_population = 100000 } true=1) and returns true if the sum is at least 1.5 The amount of triggers that need to be fulfilled.     11 = { state_population = 100000 }

}

Both nested (As in inside of if = { ... } ) and unnested (As in the example) else  and else_if

if = { exist. In case of overlap, unnested is limit = { preferred. has_dlc = "Poland: United and Ready"

limit = <trigger block>     has_political_power > 100

If limit  is true, the sub-triggers are evaluated same as in effects, if the limit is unmet, }

else_if = <if-trigger> nothing appears. If it is met, then the else_if = { like an AND-trigger. If limit  is false,

if Alternative condition. Optional.     limit = { else_if  blocks are tried in sequence and limit is hidden while the condition 1.0 else = <AND-trigger>         has_dlc = "Waking the Tiger" finally else  (if present). Otherwise true is outside of the limit appears in the

Final alternative condition. Optional.     } returned. tooltip. has_war_support > 0.5

} if = { limit = { trigger_1

else = { = yes } trigger_2 = yes }  is always = no equivalent to OR = { NOT = { }

trigger_1 = yes } trigger_2

= yes } , but generates a different tooltip.

hidden_trigger = { Hides the triggers from the tooltip shown to the hidden_trigger None     country_exists = GER Also serves as an AND statement. 1.0

player. }

custom_trigger_tooltip = { Alias for #custom_override_tooltip tooltip = sunrise_invasion_tt trigger (see that trigger for more info). any_state = { Hides the triggers from the tooltip shown to the Kept for backward compatibility. Prefer

tooltip = <string> custom_trigger_tooltip         is_owned_by = JAP player and instead uses the specified localisation

The localisation key to use.         is_on_continent = europe #custom_override_tooltip instead. 1.0 is_coastal = yes key.

Also supports }

Localisation#Bindable_localisation. }

A positive tooltip can be set with tooltip  and the tooltip to be used

inside a NOT can be set with custom_override_tooltip = { not_tooltip . If no positive tooltip tooltip = { is provided and the root key is a localization_key = localization key (not a formatter, see

tooltip = <string> GER_inner_circle_focus_in_progress_tt formatted localization), then a negative The localisation key to use.   CHARACTER = GER_rudolf_hess An AND trigger that has an overriden custom

custom_override_tooltip tooltip will be generated by appending FLAG_DAYS = [? 1.15

not_tooltip = <string> tooltip. GER_rally_the_industrialists_in_progress_flag:days] _NOT  to the root localization for the

The localisation key to use for NOT block. Optional.     } positive tooltip. Both tooltip  and not_tooltip = MY_TOOLTIP_NOT not_tooltip  are bindable <triggers>

localizations. }

Can also be used as effect. Also supports Localisation#Bindable_localisation.

Any scope ​[ edit | edit source ]

This is a community maintained wiki. If you spot a mistake, please help with fixing it.

These triggers do not require any particular scope.

General ​[ edit | edit source ]

General any-scoped triggers: [Collapse]

Version Name Parameters Examples Description Notes

Added

<bool> Always returns true or false. Useful always always = yes 1.0

Boolean. for debugging.

<string> Checks if the specified flag has been has_global_flag has_global_flag = my_flag 1.0

The flag to check. set.

flag = <string>

The flag to check. has_global_flag = {

value = <int> flag = my_flag Compares the specified flag's last

The flag value to check for. Optional. has_global_flag     days > 30 If not set, the value comparison is >0 . value  is limited between

set date, days since last set, and/or 1.0 date = <date>     date > 1936.6.1 -32768 and 32767.

The flag creation date to check for. Optional.     value > 0 value. }

days = <int>

The duration the flag existed for. Optional.

<string> Checks if the specified DLC is has_dlc has_dlc = "Waking the Tiger" 1.0

The DLC name to check for. enabled.

Year.Month.Day <date> Checks if the specified date was the

has_start_date has_start_date > 1950.01.01 1.0 The date to check for. start date used for the current game. Must use either > or < operators.

<date> Checks if the specified date against Year.Month.Day date date < 1950.01.01 1.0

The date to check for. the current date. Must use either > or < operators.

<int> checks if the specified difficulty difficulty difficulty > 0 Must use either > or < operators. 1.0

The difficulty value. against the current difficulty.

Checks if any custom difficulty Custom difficulty in this case refers to <bool>

has_any_custom_difficulty_setting has_any_custom_difficulty_setting = yes setting is changed from their default /Hearts of Iron IV/common/difficulty_settings/*.txt, 1.0 Boolean.

value. used in base game to strengthen a specific country.

Checks if the specified custom Custom difficulty in this case refers to <string>

has_custom_difficulty_setting has_custom_difficulty_setting = difficulty setting is changed from the /Hearts of Iron IV/common/difficulty_settings/*.txt, 1.0 The setting to check. custom_diff_strong_sov

default value. used in base game to strengthen a specific country.

<bool> Checks if all of the active game rule game_rules_allow_achievements game_rules_allow_achievements = yes 1.9

Boolean. options allow achievements.

<scope> / <variable> Checks if the specified country country_exists country_exists = GER 1.0

The country to check. currently exists in game.

<bool> Checks if the current game is is_ironman is_ironman = yes 1.0

Boolean. running in Ironman mode.

<bool> Checks if the current game is is_historical_focus_on is_historical_focus_on = yes 1.0

Boolean. running with Historical Focuses on.

<bool> Checks if the current game is is_tutorial is_tutorial = yes 1.0

Boolean. running in Tutorial mode.

<bool> Checks if game is in debug mode is_debug is_debug = yes 1.9

Boolean. (launched with -debug argument).

<float> Checks if World Tension is above the threat threat > 0.5 Must use either > or < operators. 1.0

The amount to check for. specified amount.

The game rule to check for. has_game_rule = { rule = Checks if a game rule is set to a has_game_rule GER_can_remilitarize_rhineland option = 1.5

<string> / <bool> particular option. yes }

The option to check.

The achievement (including the ID of the mod it's from) is defined mod = <mod ID> within /Hearts of Iron IV/common/achievements/*.txt

The mod where the achievement is from. has_completed_custom_achievement = { Checks if the player controlling the files[1]. The achievement could be completed during a previous has_completed_custom_achievement     mod = my_mod_unique_id current scope has completed the session, not necessarily the current one. 1.12.5

achievement = <achievement ID>     achievement = my_achievement_token

The name of the achievement. } specified custom achievement. If the mod defining the achievement is not loaded, the trigger evaluates as false.

Career profile ​[ edit | edit source ]

Career profile-releated any-scoped triggers: [Collapse]

Version Name Parameters Examples Description Notes

Added

medal = <medal> career_profile_check_medal = {

career_profile_check_medal Medal to check.   medal = raining_debris_medal Checks if the required medal is Provide no tooltip. ???

 ??? achieved and collected. ??? }

ribbon = <ribbon>

Ribbon to check. career_profile_check_ribbon = {

career_profile_check_ribbon   ribbon = orchestra_of_boom Checks if the required ribbon is By default provide no tooltip. ???

tooltip = <loc_key>   tooltip = my_loc_key achieved and collected. Optional. }

Provide no tooltip. frits = <variable>

Possible compare types: second = <variable> career_profile_check_playthrough_ratio = {

Compares the ratio first = enemy_casualties less_than

(first/second) of two career_profile_check_playthrough_ratio   second = total_own_casualties less_than_or_equals

ratio = <int> ??? ratio = 4 playthrough values to a

Ratio. greater_than compare = greater_than_or_equals number.

compare = <type> } greater_than_or_equals The type of comparison. equals

not_equals

{ ... }

career_profile_check_playthrough_value = { OR

plan_landlocked_battleship > 1 By default provide no tooltip. var = <variable>   plan_landlocked_carrier > 0

Value to compare. } Possible compare types: value = <int> less_than

Value to compare to. Compares a playthrough value career_profile_check_playthrough_value less_than_or_equals

career_profile_check_playthrough_value = { ??? compare = <type> to a number.

var = deployed_airplanes_with_air_defense_gold greater_than The type of comparison. Optional.

value = 100 greater_than_or_equals tooltip = <loc_key>   compare = greater_than_or_equals equals

Optional.   tooltip = not_equals

CAREER_PROFILE_TRIGGER_DEPLOYED_AIRPLANES_WITH_AIR_DEFENSE tooltip_value = <int>

tooltip_value = 100 Optional.

By default provide no tooltip. value = <int> Possible compare types:

Value to compare. career_profile_check_points = { less_than value = 5000

compare = <type> Compares a career points career_profile_check_points less_than_or_equals

compare = greater_than_or_equals ??? The type of comparison. value to a number.

tooltip = CAREER_PROFILE_TRIGGER_MINED_SEA_REGIONS greater_than tooltip = <loc_key> } greater_than_or_equals

Optional. equals not_equals

Compares the ratio Possible the same as Possible the same as

career_profile_check_ratio Possible the same as #career_profile_check_playthrough_ratio. (first/second) of two career ??? #career_profile_check_playthrough_ratio. #career_profile_check_playthrough_ratio.

profile values to a number.

Possible the same as Compares a career profile Possible the same as career_profile_check_value Possible the same as #career_profile_check_playthrough_value. ???

#career_profile_check_playthrough_value. value to a number. #career_profile_check_playthrough_value.

<string>  is set for the career_profile_has_player_flag career_profile_has_player_flag = Checks if the flag

??? The flag to check. career_profile_overrun_infantry_flag local player.

Variables ​[ edit | edit source ]

This is a community maintained wiki. If you spot a mistake, please help with fixing it.

Variable-related triggers: [Collapse]

Version Name Parameters Examples Description Notes

Added

<variable> Checks if the specified variable exists for the current has_variable has_variable = my_var 1.5

The variable to check. scope.

check_variable = { Possible compare types:

var = <variable>     var = my_var

The variable to check.     value = 10 less_than compare = greater_than_or_equals less_than_or_equals

value = <float> / <variable> } check_variable Check the specified variable for the current scope. greater_than 1.5

The value to check for. greater_than_or_equals

compare = <type>

The type of comparison. Optional, can use < or > instead. check_variable = { equals my_var > 10 not_equals }

Remember that variables need to refer to the scope they were set in. This means you can't check a country variable in a state scope without scoping the variable.

For example, to get the country variable whilst in a state scope, you'd do the following:

<country> = {

}

See Variables for more information.

Debugging ​[ edit | edit source ]

These are usable as both effects and triggers and are used for debugging, with the player never seeing them.

Debugging-helpful triggers: [Collapse]

Version Name Parameters Examples Description Notes

Added

game.log is stored within <string> Appends an entry into the game.log and, if open,

log log = "Added [?temp_add] to /Hearts of Iron IV/logs/ in the user 1.5

What to log. Supports dynamic localisation. [THIS.GetTag]'s variable [?THIS.varvalue]" the console when evaluating the trigger. directory

print_global = <bool>

Print global variables. Defaults to no .

var_list = <list>

The variables to print. Defaults to all variables. print_variables = {

file = <string> The file path to log to. Defaults to

print_variables     file = "my_dump_file" scope and optionally the global scope into a log /Hearts of Iron IV/logs/variable_dumps/ 1.5 "variable_dump". Does not include the .log  extension.

text = <string> Text to prepend. Defaults to "No }

Header". append = <bool> Whether to append to the file instead of

overwrite. Defaults to yes

Country scope ​[ edit | edit source ]

This is a community maintained wiki. If you spot a mistake, please help with fixing it.

Can be used in country scope.

General ​[ edit | edit source ]

This is a community maintained wiki. If you spot a mistake, please help with fixing it.

General country-scoped triggers: [Collapse]

Version Name Parameters Examples Description Notes

Added

<bool> Checks if the current scope exists in exists exists = yes 1.0

Boolean. game.

tag = GER

<scope> / <variable> Checks if the current scope is the Only checks the actual tag while excluding dynamic countries, see tag 1.0

The country to check. specified country. original_tag if they should be included. tag = var:my_country

original_tag = GER Checks if the current scope This also includes dynamic countries: civil war breakaways and <scope> / <variable>

original_tag originates from the specified countries created via create_dynamic_country. True for the original 1.0 The country to check.

country. country. original_tag = var:my_country

Boolean.

target = <country>

The country to check. has_collaboration = { Checks if the current scope has a has_collaboration     target = GER The target is occupied by the current scope. Must use < or > in the

value <> <decimal> collaboration level in the target 1.9 value > 0.5 value argument.

The value of the collaboration on the 0-1 } scope. scale.

<string> Checks if the current scope has the has_country_flag has_country_flag = my_flag 1.0

The flag to check. specified flag.

flag = <string>

The flag to check. has_country_flag = {

value = <int> flag = my_flag Compares the specified flag's last

The flag value to check for. Optional. has_country_flag     days > 30 If not set, the value comparison is >0 . value  is limited between

set date, days since last set, and/or 1.0 date = <date>     date > 1936.6.1 -32768 and 32767.

The flag creation date to check for. Optional.     value > 0 value. }

days = <int>

The duration the flag existed for. Optional.

<string> Checks if the current scope has the has_cosmetic_tag has_cosmetic_tag = SOV_custom 1.5

The cosmetic tag to check. specified cosmetic tag active.

Checks if current scope or global <event target>

has_event_target has_event_target = my_var scope has the specified event 1.0 The event target to check.

target saved.

<string> Checks if the current scope has the has_decision has_decision = my_decision 1.5

The decision to check. specified decision activated.

modifier = <string>

The dynamic_modifier to check. has_dynamic_modifier = { Checks if the current scope has the has_dynamic_modifier     modifier = my_dynamic_modifier

scope = <scope> specified dynamic modifier 1.6 scope = GER

The country to check. Optional, if the original } activated. modifier has been targeted.

<string> Checks if the current scope has the has_active_mission has_active_mission = my_mission 1.5

The mission to check. specified mission active.

Checks if the any custom difficulty Custom difficulty in this case refers to <bool>

has_country_custom_difficulty_setting has_country_custom_difficulty_setting = setting targeting the current scope /Hearts of Iron IV/common/difficulty_settings/*.txt, 1.0 Boolean. yes

is changed from the default value. used in base game to strengthen a specific country.

<terrain> Checks if the current scope has any has_terrain has_terrain = urban Only can be used in country scope. 1.11

Terrain. provinces of the specified terrain.

Dynamic countries include those generated in civil wars as well as <bool> Checks if the current scope is a

is_dynamic_country is_dynamic_country = yes those generated with the create_dynamic_country effect, such as 1.11 Boolean. dynamic country.

collaboration governments.

Checks if the current scope has the <int>

num_of_supply_nodes num_of_supply_nodes > 10 specified amount of supply nodes Can only use < or > operators. 1.11 The amount to check for.

under control.

Checks if the current scope has the Must use either > or < operators for amount. <int>

<resource> (resource_count_trigger) tungsten > 10 specified amount of the specified ??? The amount to check for. Can also be used in state scope.

resource.

resource = <resource>

The resource to check for.

amount = <int>

The amount to check for. extracted = <bool>

has_resources_in_country = { Limits the checked resources only to those     resource = oil Checks if the current scope has the

Must use either > or < operators for amount. 'In reserve' means that has_resources_in_country gained from the state's base value and     amount > 10 specified amount of the specified 1.12

it's not spent on equipment production or exports. multiplicative modifiers on top of it if true.     extracted = yes resource in reserve. Optional, defaults to false. }

buildings = <bool>

Limits the checked resources only to those gained from the state's modifiers applied by buildings. Optional, defaults to false.

National focuses ​[ edit | edit source ]

National focus-related country-scoped triggers: [Collapse]

Version Name Parameters Examples Description Notes

Added

<string> Checks if the current scope has the specified focus has_focus_tree has_focus_tree = soviet_tree 1.3

The focus tree to check. tree.

<string> Checks if the current scope has the specified focus has_completed_focus has_completed_focus = my_focus 1.0

The focus to check. completed.

focus = <string>

The focus to check. focus_progress = {

focus_progress   focus = my_focus Checks if the specified focus has been completed Must use either > or < operators for 1.0

progress = <string>   progress > 0.5 the specified percent for the current scope. progress. The progress to check for. }

Note that tooltips are only shown in debug mode.

<string> Check if country has shine effect on focus (either has_shine_effect_on_focus has_shine_effect_on_focus = GER_wunderwaffe Shine can be added manually by 1.15

The focus to check. manually achieved or by being worked on). selecting focus, or via activate_shine_on_focus effect.

Politics ​[ edit | edit source ]

This is a community maintained wiki. If you spot a mistake, please help with fixing it.

Political country-scoped triggers: [Collapse]

Version Name Parameters Examples Description Notes

Added

fascism > 0.5

<ideology> = <float> / <variable> Checks if the current scope has popularity of the <ideology> (ideology_support_trigger) 1.0

The amount of the ideology to check for. specified ideology above the specified amount. democratic > party_popularity@communism

<float> / <variable> Checks if the current scope has the specified has_political_power has_political_power > 100 Must use either > or < operators. 1.0

The amount to check for. amount of political power.

<float> / <variable> Checks if the current scope has the specified political_power_daily political_power_daily > 1 Must use either > or < operators. 1.5

The amount to check for. amount of daily political power gain.

<float> / <variable> Checks if the current scope has the specified political_power_growth political_power_growth > 1 Must use either > or < operators. 1.5

The amount to check for. amount of daily political power gain.

<float> / <variable> Checks if the current scope has the specified command_power command_power > 1 Must use either > or < operators. 1.5

The amount to check for. amount of command power.

<float> / <variable> Checks if the current scope has the specified command_power_daily command_power_daily > 1 Must use either > or < operators. 1.5

The amount to check for. amount of daily command power gain.

<float> / <variable> Checks if the current scope has the specified has_war_support has_war_support > 0.5 Must use either > or < operators. 1.5

The amount to check for. percentage of War Support.

<float> / <variable> Checks if the current scope has the specified has_stability has_stability > 0.5 Must use either > or < operators. 1.5

The amount to check for. percentage of Stability.

The ideology group to check for. has_government = fascism Checks if the ruling party of the current scope meets the requirements of being either the

has_government OR 1.0 specified ideology group or having the same

The country to compare with.

Boolean.

Boolean.

<bool> Checks if the current scope is the target of a is_target_of_coup is_target_of_coup = yes 1.0

Boolean. coup.

<bool> Checks if the current scope has a civil war has_civil_war has_civil_war = yes 1.0

Boolean. active.

<scope> Checks if the specified country is a target of a civilwar_target civilwar_target = GER 1.0

The target country. civil war.

value = <float>

The amount to check for. has_manpower_for_recruit_change_to = { Checks if the current scope has the specified Must use either > or < operators as has_manpower_for_recruit_change_to     value > 0.05 amount of manpower for changing the specified = operator checks for the exact 1.0

group = <group>     group = mobilization_laws

The group to check for. } idea group. value

<string> Checks if the current scope has the specified has_rule has_rule = can_create_factions 1.6

The rule to check for. country rule.

Checks if the current scope has the specified <float> / <variable>

has_casualties_war_support has_casualties_war_support < 0 percentage of war support from own combat Must use either > or < operators. 1.12 The amount to check for.

casualties.

Checks if the current scope has the specified <float> / <variable>

has_convoys_war_support has_convoys_war_support < 0 percentage of war support from own convoys Must use either > or < operators. 1.12 The amount to check for.

sunk.

Checks if the current scope has the specified <float> / <variable>

has_bombing_war_support has_bombing_war_support < 0 percentage of war support from own states Must use either > or < operators. 1.12 The amount to check for.

bombed by the enemy.

Balance of power ​[ edit | edit source ]

This is a community maintained wiki. If you spot a mistake, please help with fixing it.

Balances of power are stored within /Hearts of Iron IV/common/bop/*.txt files.

Balance of power-related country-scoped triggers: [Collapse]

Version Name Parameters Examples Description Notes

Added

has_power_balance = { id = <bop ID> Checks if the current scope has the specified

has_power_balance     id = TAG_my_bop 1.12 The balance to check for. balance of power active.

}

<bool> Checks if the current scope has any balance has_any_power_balance has_any_power_balance = yes 1.12

Boolean. of power active.

id = <bop ID>

The balance to check in. power_balance_value = {

power_balance_value     id = TAG_my_bop Checks if the current scope has the specified Either =, >, or < operators are allowed. 1.12

value = <float>     value > 0.7 value within the balance of power. The value to check for. }

id = <bop ID>

The balance to check in. power_balance_daily_change = { Checks if the current scope's balance of power_balance_daily_change     id = TAG_my_bop power changes each day by the specified Either =, >, or < operators are allowed. 1.12

value = <float>     value < -0.01

The value to check for. } value.

id = <bop ID>

The balance to check in. power_balance_weekly_change = { Checks if the current scope's balance of power_balance_weekly_change     id = TAG_my_bop power changes each week by the specified Either =, >, or < operators are allowed. 1.12

value = <float>     value < -0.01 value.

The value to check for. }

id = <bop ID>

The balance to check in. is_power_balance_in_range = { Ranges are defined within the balance of power. Can Checks if the current scope's balance of

is_power_balance_in_range     id = TAG_my_bop use either =, >, and < operators. In case of > or <, the 1.12 range = <range ID>     range > TAG_my_bop_right_range power value lies within the specified range.

comparison is 'strict', i.e. excluding the range itself. The range to check for. }

id = <bop ID>

The balance to check in. is_power_balance_side_active = { Sides are defined within the balance of power. "Active" is_power_balance_side_active     id = TAG_my_bop Checks if the specified balance of power has

means that the side is among those that are currently 1.12 side = <side ID>     side = TAG_my_bop_right_range a side active.

visible instead of relying on the current value. The side to check. }

id = <bop ID> BoP modifiers are defined within The balance to check in. has_power_balance_modifier = {

rent scope's balance of /Hearts of Iron IV/common/modifiers/*.txt has_power_balance_modifier     id = TAG_my_bop Checks if the cur

1.12 modifier = <modifier ID>     modifier = TAG_my_bop_modifier power value activates a modifier. files, while they're activated in the balance of power

The static modifier. } definition.

Buildings ​[ edit | edit source ]

This is a community maintained wiki. If you spot a mistake, please help with fixing it.

Building-related country-scoped triggers: [Collapse]

Version Name Parameters Examples Description Notes

Added Must use either > or < operators. Works in the state

<int> Checks if the current scope has the scope as well, unlike the other <building> (building_count_trigger) arms_factory > 10

The amount of the specified building to to check for. specified amount of the specified building. triggers. 1.0

Can also be used in state scope.

<int> Checks if the current scope has the Must use either > or < num_of_military_factories num_of_military_factories > 10 1.0

The amount to check for. specified amount of military factories. operators.

<int> Checks if the current scope has the Must use either > or < num_of_civilian_factories num_of_civilian_factories > 10 1.0

The amount to check for. specified amount of civilian factories. operators.

<int> Checks if the current scope has the Must use either > or < num_of_naval_factories num_of_naval_factories > 10 1.0

The amount to check for. specified amount of dockyards. operators.

Checks if the current scope has the <int> Must use either > or <

num_of_available_military_factories num_of_available_military_factories > 10 specified amount of available military 1.0 The amount to check for. operators.

factories.

Checks if the current scope has the <int> Must use either > or <

num_of_available_civilian_factories num_of_available_civilian_factories > 10 specified amount of available civilian 1.0 The amount to check for. operators.

factories.

<int> Checks if the current scope has the Must use either > or < num_of_available_naval_factories num_of_available_naval_factories > 10 1.0

The amount to check for. specified amount of available dockyards. operators.

Checks if the current scope has the <int> Must use either > or <

num_of_factories num_of_factories > 10 specified amount of military, civilian or 1.0 The amount to check for. operators.

dockyard factories.

Checks if the current scope has the <int> Must use either > or <

num_of_controlled_factories num_of_controlled_factories > 10 specified amount of military, civilian or 1.11 The amount to check for. operators.

dockyard factories under control.

Checks if the current scope has the <int> Must use either > or <

num_of_owned_factories num_of_owned_factories > 10 specified amount of military, civilian or 1.11 The amount to check for. operators.

dockyard factories under owned states.

Checks if the current scope has the <int> Must use either > or <

num_of_civilian_factories_available_for_projects num_of_civilian_factories_available_for_projects specified amount of civilian factories usable 1.5 The amount to check for. > 10 operators.

for projects.

tag = <scope>

The country to check. ic_ratio = { Checks if the current scope has the use either > or < operators

ic_ratio     tag = GER Must specified ratio of factories with the target 1.0

ratio = <float>     ratio > 0.5 for ratio. The ratio to check for. } country.

<bool> Checks if the current scope has any has_damaged_buildings has_damaged_buildings = yes 1.0

Boolean. damanged buildings in their states.

type = <building>

The building to check for. has_built = { Checks if the current scope has built the has_built     type = arms_factory Must use either > or < operators

specified building the specified number of 1.0 value = <int>     value > 10 for value.

times. The amount to check for. }

Technology ​[ edit | edit source ]

This is a community maintained wiki. If you spot a mistake, please help with fixing it.

Technology-related country-scoped triggers: [Collapse]

Version Name Parameters Examples Description Notes

Added

<string> Checks if the current scope has the specified has_tech has_tech = my_technology 1.0

The technology to check for. technology.

<string> Checks if the current scope is currently is_researching_technology is_researching_technology = my_tech 1.0

The technology to check for. researching the specified technology.

<string> Checks if the current scope can start researching can_research can_research = my_tech 1.0

The technology to check for. the specified technology.

<int> Checks if the current scope had the specified original_research_slots original_research_slots > 3 Must use either > or < operators. 1.0

The amount to check for. amount of slots at game start.

<int> Checks if the current scope has the specified amount_research_slots amount_research_slots > 3 Must use either > or < operators. 1.3

The amount to check for. amount of research slots.

<string> Checks if the current scope is in the specified is_in_tech_sharing_group is_in_tech_sharing_group = us_research 1.3

The group to check for. technology sharing group.

<int> Checks if the current scope is in the specified num_tech_sharing_groups num_tech_sharing_groups > 3 Must use either > or < operators. 1.3

The amount to check for. amount of technology sharing groups.

has_tech_bonus = {

technology = <string>

} The technology to check for. Optional. Checks if the current scope has a technology

has_tech_bonus bonus in the specified category, or for the specific 1.3 category = <string>

technology. The category to check for. Optional. has_tech_bonus = {

}

<int> Checks if the current scope has the specified land_doctrine_level land_doctrine_level > 2 Must use either > or < operators. 1.0

The amount to check for. amount of land doctrine technologies.

<int> Checks how many technologies the target has num_researched_technologies num_researched_technologies > 10 Must use either > or < operators. 1.3

The amount to check for. researched.

sp:<string> if the country in scope is currently is_special_project_being_researched is_special_project_being_researched = Checks

1.15 A special project to check for. sp:sp_air_radar researching the special project in input.

sp:<string> is_special_project_completed is_special_project_completed = Checks if the current scope has the specified

1.15 A special project to check for. sp:sp_land_flamethrower_tank special project completed.

Ideas ​[ edit | edit source ]

This is a community maintained wiki. If you spot a mistake, please help with fixing it.

Idea-related country-scoped triggers: [Collapse]

Version Name Parameters Examples Description Notes

Added

<string> Checks if the current scope has the specified has_idea has_idea = my_idea

The idea to check for. idea.

<string> Checks if the current scope has any ideas has_idea_with_trait has_idea_with_trait = my_trait 1.0

The trait to check for. with the specified trait.

idea = <string>

The trait to check for. has_available_idea_with_traits = {

limit = <int> idea = my_trait

The amount to check for. has_allowed_idea_with_traits     limit = 1 Checks if the current scope has the specified

ignore = idea_name works for 1 idea. 1.9.1 characters = <bool>     ignore = { amount of ideas with the specified trait.

If set, will only run this on characters. generic_head_of_intelligence }

} ignore = { <ideas> }

If set, ignores the ideas inside. Optional.

idea = <string>

The trait to check for. has_available_idea_with_traits = {

limit = <int> idea = my_trait

The amount to check for. has_available_idea_with_traits     limit = 1 Checks if the current scope has the specified

ignore = idea_name works for 1 idea. 1.0 characters = <bool>     ignore = { amount of ideas with the specified trait.

If set, will only run this on characters. generic_head_of_intelligence }

} ignore = { <ideas> }

If set, ignores the ideas inside. Optional.

amount = <int> amount_taken_ideas = {

The amount to check for.     amount > 3 Checks if the current scope has the specified Slots types are found in

amount_taken_ideas     slots = { amount of ideas of the specified slot type. 1.4 slots = { <string> }         political_advisor /Hearts of Iron IV/common/idea_tags/*.txt.

Excludes spirits, hidden ideas, and laws. The slot type.     }

}

Diplomacy ​[ edit | edit source ]

This is a community maintained wiki. If you spot a mistake, please help with fixing it.

Diplomatic country-scoped triggers: [Collapse]

Version Name Parameters Examples Description Notes

Added

<bool> Checks if the current scope is considered a is_major is_major = yes 1.0

Boolean. Major.

is_ally_with = GER Checks if the current scope is an ally <scope> / <variable>

is_ally_with (Faction members or subject-master 1.0 The country to check for.

relation). is_ally_with = var:country

<bool> Checks if the current scope is the is_spymaster is_spymaster = yes 1.9

Boolean. spymaster of a faction.

<scope> / <variable> Checks if the current scope has a non- has_non_aggression_pact_with has_non_aggression_pact_with = GER 1.0

The country to check for. aggression pact with the specified country.

<scope> / <variable> Checks if the current scope has been is_guaranteed_by is_guaranteed_by = GER 1.0

The country to check for. guaranteed by the specified country.

<scope> / <variable> Checks if the current scope has guaranteed has_guaranteed has_guaranteed = GER 1.0

The country to check for. the specified country.

<scope> / <variable> Checks if the current scope has military has_military_access_to has_military_access_to = GER 1.0

The country to check for. access to the specified country.

<scope> / <variable> Checks if the current scope gives military to gives_military_access_to gives_military_access_to = GER 1.0

The country to check for. the specified country.

<scope> / <variable> Checks if the current scope is a neighbor of is_neighbor_of is_neighbor_of = GER 1.0

The country to check for. the specified country.

Checks if the current scope is a neighbor of <scope> / <variable>

is_owner_neighbor_of is_owner_neighbor_of = GER the specified country with their core territory 1.0 The country to check for.

only.

A "puppet" is an autonomous state that has is_puppet = <scope> / <variable> Checks if the current scope is a puppet of yes  in its definition within

is_puppet_of is_puppet_of = GER 1.0 The country to check for. the specified country. /Hearts of Iron IV/common/autonomous_states/.

For any subject type, see is_subject_of.

<scope> / <variable> Checks if the current scope is a subject of is_subject_of is_subject_of = GER 1.0

The country to check for. the specified scope.

A "puppet" is an autonomous state that has is_puppet = <bool> Returns true if the current country is a yes  in its definition within

is_puppet is_puppet = yes 1.0 Boolean. puppet. /Hearts of Iron IV/common/autonomous_states/.

For any subject type, see is_subject.

Boolean.

<bool> Checks if the country has for subject the has_subject has_subject = GRE 1.0

Boolean. given country.

<int> Checks if the current scope has the num_subjects num_subjects > 3 Must use either > or < operators. 1.3

The amount to check for. specified amount of subjects.

<string> Checks if the current scope is in the has_autonomy_state has_autonomy_state = autonomy_dominion 1.0

The autonomy state to check for. specified autonomous state.

Checks if the current scope's autonomy state min_freedom_level  is less or greater than that of the specified autonomy state. The special value "autonomy_free"

<string> compares as greater than any autonomy compare_autonomy_state compare_autonomy_state > 1.0

The autonomy state to check for. autonomy_dominion state. If the current scope is not a subject, it is treated as greater than any autonomy state (including "autonomy_free"). With = , checks if the current scope is in the specified autonomous state.

Checks if the current scope autonomy <float>

compare_autonomy_progress_ratio compare_autonomy_progress_ratio > 0.5 progress is at the specified ratio. If the 1.3 The amount to check for.

current scope is not a subject, the ratio is 1.

<string> Checks if the current scope has the has_opinion_modifier has_opinion_modifier = my_modifier 1.0

The opinion modifier to check for. specified opinion modifier.

target = <scope>

The country to check for. has_opinion = {

has_opinion     target = GER Checks if the current scope has the Must use either > or < operators. 1.0

value = <float>     value > 50 specified opinion of the target country. The amount to check for. }

target = <scope>

The country to check for. has_relation_modifier = { Checks if the current scope has the has_relation_modifier     target = GER specified relation modifier with the specified 1.0

modifier = <modifier>     modifier = my_modifier

The modifier to check for. } country.

<int> Checks how much legitimacy the current Must use either > or < operators. Legitimacy ranges from 0 has_legitimacy has_legitimacy > 50 1.6

Amount to check. government in exile has. to 100.

<bool> Checks if the current country is hosting an is_exile_host is_exile_host = yes 1.6

Boolean. exile.

<tag> Checks if the current country is hosting a is_hosting_exile is_hosting_exile = POL 1.6

Country. specific exile.

<bool> Checks if the current country is exiled in a is_government_in_exile is_government_in_exile = yes 1.6

Boolean. different country.

<tag> Checks if the current country is exiled in a is_exiled_in is_exiled_in = POL 1.6

Country to be exiled in. specific country.

sender = <tag>

Country which sent forces. received_expeditionary_forces = { Checks if the current country received X received_expeditionary_forces     sender = POL units in expeditions from the specified 1.6

value <> <int>     value > 10 country.

Amount of forces. }

<tag> Checks if the current scope is able to can_declare_war_on can_declare_war_on = POL 1.9

Country to check. declare war on the specified country.

<int> Checks how much foreign manpower we foreign_manpower foreign_manpower > 10000 Must use either > or < operators. 1.9

Amount to check. have received for garrisoning.

<scope> Checks if the current scope is embargoed is_embargoed_by is_embargoed_by = USA 1.12

Amount to check. by the specified country.

<scope> Checks if the current scope is embargoing is_embargoing is_embargoing = CUB 1.12

Amount to check. the specified country.

Faction ​[ edit | edit source ]

Faction-related country-scoped triggers: [Collapse]

Version Name Parameters Examples Description Notes

Added

Boolean.

is_in_faction_with = GER

<scope> / <variable> Checks if the current scope is in a faction with is_in_faction_with 1.0

The country to check for. the specified country. is_in_faction_with = var:country

<bool> Checks if the current scope is the leader of a is_faction_leader is_faction_leader = yes 1.0

Boolean. faction.

<int> / <variable> Checks if the faction of the current scope has num_faction_members num_faction_members > 1 Must use either > or < operators. 1.0

The amount to check for. the specified amount of members.

Checks if the current country exceeds the <bool>

has_manpower_to_become_leader has_manpower_to_become_leader = yes current faction leader and its subjects in 1.17 Boolean

deployed manpower.

<bool> Checks if the current country exceeds the has_industry_to_become_leader has_industry_to_become_leader = yes 1.17

Boolean faction leader in number of factories.

<bool> Checks if the current country has enough has_enough_influence_for_leadership has_enough_influence_for_leadership = yes 1.17

Boolean political influence to become faction leader.

<template_id> Checks if the current country is in a faction has_faction_template has_faction_template = 1.17

Faction template id. faction_template_chinese_united_front with a template.

<rule_id> has_active_rule has_active_rule = Checks if the country's faction has a specific

1.17 Faction rule id. government_in_exile_allowed active rule.

<goal_id> has_faction_goal has_faction_goal = Checks if the country's faction has an active or

1.17 Faction goal id. faction_goal_resource_control completed goal.

<goal_id> ion has has_completed_faction_goal has_completed_faction_goal = Checks if the country's fact

1.17 Faction goal id. faction_goal_resource_control successfully completed a goal.

faction_goal_fulfillment = {

goal = <goal_id> }

Faction goal id. Value supports > and <, can Checks fulfillment of a faction goal for the

faction_goal_fulfillment accept variables, can be repeated 1.17 value = <float> / <variable> current country's faction.

faction_goal_fulfillment = { multiple times. The amount to check for.

}

<float> / <variable> Checks manifest fulfillment value of current faction_manifest_fulfillment faction_manifest_fulfillment > 0.95 1.17

The amount to check for. country's faction manifest.

<upgrade_token> Checks the active faction member upgrade faction_upgrade_level faction_upgrade_level > upgrade_token Works with >, <, = 1.17

Faction upgrade token. against the specified upgrade.

<int> / <variable> Checks power value of current country's faction_power_projection faction_power_projection > 100 1.17

The amount to check for. faction projection.

<int> / <variable> Checks influence rank in the faction of the faction_influence_rank faction_influence_rank < 5 1.17

The amount to check for. current country.

<float> / <variable> Checks influence ratio of current country in the faction_influence_ratio faction_influence_ratio > 0.1 1.17

The amount to check for. faction.

<int> / <variable> Checks influence value of current country in faction_influence_score faction_influence_score > 100 1.17

The amount to check for. the faction.

Checks if the faction from the country in scope <specialization>

can_assign_supportive_scientist_to_faction can_assign_supportive_scientist_to_faction = has a free slot for a supportive scientist for the 1.17 Specialization. specialization_land

country with the specialization type.

<bool> Whether the faction has unlocked the has_faction_research_unlocked has_faction_research_unlocked = yes 1.17

Boolean. research.

<bool> Whether the faction has unlocked the military has_faction_military_unlocked has_faction_military_unlocked = yes 1.17

Boolean. operations.

value = <float> / <variable>

The amount to check for. compare_ideology_with_faction = { Compares the ideology support of the Tooltip is visible only if 'leader' is in

compare_ideology_with_faction     value > 0.5 country's ruling party for the ideology of the 1.17 leader = <tag>     leader = FROM faction.

Country to check. } faction it wants to join.

War ​[ edit | edit source ]

War-related country-scoped triggers: [Collapse]

Version Name Parameters Examples Description Notes

Added

Boolean.

has_war_with = GER

<scope> / <variable> Checks if the current scope is at war with has_war_with 1.0

The country to check for. the specified country. has_war_with = var:country

<scope> / <variable> Checks if the current scope is in an has_offensive_war_with has_offensive_war_with = GER 1.0

The country to check for. offensive war against the specified country.

<scope> / <variable> Is country at offensive war without specific has_offensive_war_without_friend has_offensive_war_without_friend = GER 1.17

The country to check for. ally present.

<scope> / <variable> Checks if the current scope is in an has_defensive_war_with has_defensive_war_with = GER 1.0

The country to check for. defensive war against the specified country.

<bool> Checks if the current scope is in an has_offensive_war has_offensive_war = yes 1.0

Boolean. offensive war.

<bool> Checks if the current scope is in a defensive has_defensive_war has_defensive_war = yes 1.0

Boolean. war.

<scope> / <variable> Checks if the current scope is in a war has_war_together_with has_war_together_with = GER 1.0

The country to check for. alongside the specified country.

<bool> Checks if the current scope is at war with has_war_with_major has_war_with_major = yes 1.12

Boolean. any other country that is considered major.

target = <scope> / <variable> Wargoals are stored within The country to check for. has_war_with_wargoal_against = { Checks if the current scope is at war with /Hearts of Iron IV/common/wargoals/*.txt

has_war_with_wargoal_against     target = ENG the specified country with the specified files. If no wargoal is specified, checks for any 1.12 type = <wargoal>     type = independence_wargoal

ly in their war does not count as The wargoal to check for. Optional. } wargoal being active. wargoal. Joining an al

a wargoal.

<float> / <variable> Checks if the current scope has the surrender_progress surrender_progress > 0.1 Must use either > or < operators. 1.0

The amount to check for. specified amount of surrender progress.

Checks if the current scope has the <float>

any_war_score any_war_score > 10 specified amount of war progress (not war Must use either > or < operators. 1.0 The amount to check for.

participation)[2] in any war.

Boolean.

If the target never capitulated, the amount of days is <int> Checks the amount of days since the target

days_since_capitulated days_since_capitulated > 10 extremely large. Recommended to combine with 1.9 Amount of days. last capitulated.

has_capitulated.

<scope> / <variable> Checks if the current scope has a border has_border_war_with has_border_war_with = GER 1.5

The country to check for. war with the specified country.

attacker = <scope> / <variable>

The state to check for. has_border_war_between = {

has_border_war_between     attacker = 1 Checks if there is a border war between the 1.5

defender = <scope> / <variable>     defender = 2 two specified states. The state to check for. }

<bool> Checks if the current scope has a border has_border_war has_border_war = yes 1.5

Boolean. war active.

<float> / <variable> Checks if the current scope has caused the has_added_tension_amount has_added_tension_amount > 10 Must use either > or < operators. 1.0

The amount to check for. specified amount of World Tension.

<scope> / <variable> Checks if the current scope has any wargoal has_wargoal_against has_wargoal_against = GER 1.0

The country to check for. against the specified country.

target = <scope> / <variable>

The country to check for. has_wargoal_against = {

has_wargoal_against     target = FROM Checks if the current scope has a specific 1.8

type = <string>     type = take_state wargoal type against the specified country. The type of wargoal to check for. }

<scope> / <variable> Checks if the current scope is justifying a is_justifying_wargoal_against is_justifying_wargoal_against = GER 1.0

The country to check for. wargoal against the specified country.

<scope> / <variable> Checks if the current scope has the Annex has_annex_war_goal has_annex_war_goal = GER 1.0

The country to check for. wargoal against the specified country.

<bool> Checks if the current scope has any claims any_claim any_claim = yes 1.0

Boolean. on another country.

<bool> Checks if the current scope is in a peace is_in_peace_conference is_in_peace_conference = yes Please test this in-game for 1.12. 1.0

Boolean. conference.

<id> Checks if the current scope has control of controls_province controls_province = 1239 1.9

The province to check for. the specified province.

<int> Checks how long a country has been at war, longest_war_length longest_war_length > 3 1.14

Amount of months. in months.

tag = <scope> / <variable>

Target country. war_length_with = {

war_length_with     tag = GER Checks how long a country has been at war 1.14

months = <int>     months > 3 with specific country, in months. Amounth of months. }

<scope> / <variable> Checks if the country has truce with the has_truce_with has_truce_with = GER 1.16

The country to check for. specified country.

Checks if friendly nations and country scope <id> / <variable>

has_naval_control has_naval_control = 16 together has enough naval dominance to 1.17 The region to check in.

assert control in strategic region.

Checks if any enemy has enough naval <id> / <variable>

has_enemy_naval_control has_enemy_naval_control = 16 dominance to assert control in certain 1.17 The region to check in.

strategic region.

State ​[ edit | edit source ]

This is a community maintained wiki. If you spot a mistake, please help with fixing it.

These are state-related triggers in the country scope, not state-scoped triggers.

State-related country-scoped triggers: [Collapse]

Version Name Parameters Examples Description Notes

Added

controls_state = 39

<scope> / <variable> Checks if the current scope has control of the controls_state 1.0

The state to check for. specified state. controls_state = var:state

<scope> / <variable> Checks if the current scope owns the specified owns_state owns_state = 39 1.0

The state to check for. state.

<int> Checks if the current scope has the specified num_of_controlled_states num_of_controlled_states > 5 Must use either > or < operators. 1.0

The amount to check for. amount of controlled states.

<int> Checks if the current scope has the specified num_occupied_states num_occupied_states > 5 Must use either > or < operators. 1.0

The amount to check for. amount of occupied states.

<scope> / <variable> Checks if the current scope has total control (100% has_full_control_of_state has_full_control_of_state = 39 1.3

The state to check for. occupation) of the specified state.

state = <scope> / <variable> Can be used in either state or country The state to check in. Mandatory if used in country scope. has_resources_rights = {

has_resources_rights   state = 123 Checks if there are any resource rights with the scope. Always returns false if the tate has no resources. 1.12

resources = { <resource> <...> <resource> }   resources = { oil steel } specified parameters. s

Resources to check for. Optional, defaults to any if unset. } Can also be used in state scope.

occupied_country_tag = <TAG>

The country for which to check compliance. core_compliance = { Compares the average compliance of core states Must use either > or < operators for

core_compliance     occupied_country_tag = ITA of the specified country within controlled states of 1.9 value = <int>     value > 10 value.

the current scope. The value to check for. }

occupied_country_tag = <TAG>

The country for which to check resistance. core_resistance = { Compares the average resistance of core states of core_resistance     occupied_country_tag = ITA Must use either > or < operators for

the specified country within controlled states of the 1.9 value = <int>     value > 10 value.

current scope. The value to check for. }

<int> Checks how much garrison manpower we need for garrison_manpower_need garrison_manpower_need > 10000 Must use either > or < operators. 1.9

Amount to check. resistance in controlled states.

occupied_country_tag = <scope> / <variable> has_core_occupation_modifier = { Checks if the current scope has an occupation has_core_occupation_modifier The country to check.   occupied_country_tag = ITA modifier for resistance/compliance that applies to 1.9

modifier = token modifier = <token> The modifier to check. } our occupied states of a specified country.

POL = {

POL = {

foreign_civilian_oversight

}

} Checks PREV's occupation law over the current country. If they're the

<law ID> # Checks POL's default occupation law Checks the occupation law that's either the default occupation_law same scope, checks the default 1.12

The law to check. or applied over a specific country. occupation law. HOL = { Can also be used in state scope. BEL = {

foreign_civilian_oversight

}

}

# Checks HOL's occupation law over BEL

Checks if a state has the specified country as a <state> / <variable> contested owner. The trigger can be used either

has_contested_owner has_contested_owner = 42 Can also be used in state scope. 1.15 State to check. from a country or a state scope and accepts the

other as parameter.

The same as:

owns_any_state_of = {

<states> OR = {

owns_any_state_of   123 Check if the country owns any of the states in the States to check.   owns_state = 123 1.16

246 list. owns_state = 246

} }

Checks if the scope country is on the same <scope> / <variable>

is_on_same_continent_as is_on_same_continent_as = 111 continent as the given state. The capital state is Can also be used in state scope. 1.17 The state to check for.

used for given country tag.

Military ​[ edit | edit source ]

Military-related country-scoped triggers: [Collapse]

Version Name Parameters Examples Description Notes

Added

has_army_experience > 10 Checks if the current scope has <float> / <variable> Must use either > or < operators as = operator checks for the

has_army_experience the specified amount of Army 1.3 The amount to check for. exact value

experience. has_army_experience > var:number

Checks if the current scope has <float> / <variable> Must use either > or < operators as = operator checks for the

has_air_experience has_air_experience > 10 the specified amount of Air 1.3 The amount to check for. exact value

experience.

Checks if the current scope has <float> / <variable> Must use either > or < operators as = operator checks for the

has_navy_experience has_navy_experience < 10 the specified amount of Navy 1.3 The amount to check for. exact value

experience.

<float> / <variable> Checks if the current scope has Must use either > or < operators as = operator checks for the has_manpower has_manpower > 1000 1.0

The amount to check for. the specified amount of manpower. exact value

has_army_manpower = { Checks if the current scope has an size = <int>

has_army_manpower     size > 1000 army using the specified amount of Must use either > or < operators. 1.0 The amount to check for.

} manpower.

Checks if the current scope has <float>

manpower_per_military_factory manpower_per_military_factory > 1000 the specified manpower times their Must use either > or < operators. 1.0 The amount to check for.

number of military factories.

Checks if the current scope has <float> / <variable> the specified conscription ratio Must use either > or < operators as = operator checks for the

conscription_ratio conscription_ratio < 0.2 1.9 The ratio to compare with. currently, not to be mixed up with exact value

the target conscription ratio.

Checks if the current scope has <float> / <variable> Must use either > or < operators as = operator checks for the

current_conscription_amount current_conscription_amount > 2000 already conscripted that much 1.9 The amount to compare with. exact value

manpower.

Checks if the current scope is <float> / <variable> Must use either > or < operators as = operator checks for the

target_conscription_amount target_conscription_amount > 2000 targeting to conscript that much 1.9 The amount to compare with. exact value

manpower.

<int> Checks if the current scope has num_divisions num_divisions > 5 Must use either > or < operators. 1.3

The amount to check for. the specified amount of divisions.

<int> Checks if the current scope has num_of_nukes num_of_nukes > 5 Must use either > or < operators. 1.0

The amount to check for. the specified amount of nukes.

Checks if the current scope has <int>

casualties casualties > 10000 suffered the specified amount of Must use either > or < operators. 1.0 The amount to check for.

casualties.

Checks if the current scope has <int>

casualties_k casualties_k > 10 suffered the specified amount of Must use either > or < operators. 1.0 The amount to check for.

casualties in thousands.

opponent = <tag> Checks if the current scope has The tag that inflicted the casualties. casualties_inflicted_by = {

suffered the specified amount of casualties_inflicted_by     opponent = POL Must use either > or < operators for thousands. 1.6

thousands <> <int>     thousands > 10 casualties in thousands from a The amount of casualties in thousands. } specific country.

Checks if the current scope has <float>

amount_manpower_in_deployment_queue amount_manpower_in_deployment_queue > the specified amount of manpower Must use either > or < operators. 1.5 The amount to check for. 1000

in their deployment queue.

<scope> / <variable> Checks if the current scope has an has_attache_from has_attache_from = GER 1.5

The country to check for. attache from the specified scope.

<bool> Checks if the current scope has an has_attache has_attache = yes 1.5

Boolean. attache.

<scope> / <variable> Checks if the current scope is lend is_lend_leasing is_lend_leasing = GER 1.0

The country to check for. leasing to the specified scope.

Checks if the current scope has a <string>

has_template has_template = "Infantry Division" division template of the specified 1.0 The name of the template.

name.

Checks if the current scope has a <string>

has_template_majority_unit has_template_majority_unit = infantry division template composed mostly 1.0 The unit to check for.

of the specified unit.

Checks if the current scope has a <string>

has_template_containing_unit has_template_containing_unit = division template contained any of 1.0 The name of the unit. light_armor

the specified unit.

Checks if the current scope has the specified strength ratio against

tag = <scope> the specified country. The ratio is The country to check for. strength_ratio = { the number of fielded divisions of

strength_ratio     tag = GER the current scope divided by those Must use > or < in the ratio. 1.0 ratio <> <float>     ratio > 1

of tag  (or 1 if tag  has no The ratio to check for. }

divisions). The ratio gets increased by 10% if the current scope has a stronger air forces.[3]

tag = <scope>

The country to check for. fighting_army_strength_ratio = { Compares the total army fighting fighting_army_strength_ratio     tag = GER strength between the scope Ratio can be '<','>' or '='. 1.15

ratio <>= <float> / <variable>     ratio > 0.7

The ratio to check for. } country and the one set with 'tag'.

tag = <scope>

The country to check for. naval_strength_ratio = { Checks if the current scope has naval_strength_ratio     tag = GER the specified naval strength ratio Must use > or < in the ratio. 1.0

ratio <> <float>     ratio <> 1 ied country.

The ratio to check for. } against the specif

other = <scope> naval_strength_comparison = {

The country to check for. other = POL Must use > or < in the ratio. If sub_unit_def_weights is unset,

tooltip = <string>     tooltip = my_loc_key_tt

The ratio to check for. Optional.     ratio > 1 Checks if the current scope has each unit is assumed to have 1 weight. If sub_unit_def_weights naval_strength_comparison     sub_unit_def_weights = { the specified naval strength ratio is set, only specified units will be counted towards strength. 1.6

ratio <> <float> carrier = 1 against the specified country. Units are defined in

The ratio to check for.         submarine = 2 /Hearts of Iron IV/common/units/*.txt.

sub_unit_def_weights = { ... }  The     }

weight to assign to each unit. Optional. }

Checks if the current scope and <float> / <variable> allies has an army strength higher

alliance_strength_ratio alliance_strength_ratio > 0.5 Must use either > or < operators. 1.0 The ratio to check for. than the specified ratio against

estimated enemy strength.

Checks if the current scope and <float> / <variable> allies has an naval strength ratio

alliance_naval_strength_ratio alliance_naval_strength_ratio > 0.5 Must use either > or < operators. 1.0 The ratio to check for. higher than the specified ratio

against estimated enemy strength.

Checks if the estimated enemy <float> / <variable>

enemies_strength_ratio enemies_strength_ratio > 0.5 army strength ratio is higher than Must use either > or < operators. 1.0 The ratio to check for.

the specified ratio.

Checks if the estimated enemy <float> / <variable>

enemies_naval_strength_ratio enemies_naval_strength_ratio > 0.5 naval strength ratio is higher than Must use either > or < operators. 1.0 The ratio to check for.

the specified ratio.

size = <float>

The amount to check for. has_army_size = { Checks if the current scope has Battalion types are defined within

type = <string> has_army_size     size > 10 the specified number of divisions, /Hearts of Iron IV/common/units/*.txt files. Must 1.0

The battalion type to check for. Divisions that     type = armor or of a specified type of division. use either > or < operators for size.

are majority made up of battalions in that type }

count. Optional, counts all divisions by default.

size = <float> / <variable> Ship types are defined within The amount to check for. has_navy_size = { /Hearts of Iron IV/common/units/*.txt files. Must

type = <string> use either > or < operators for size. Ship archetypes are found

has_navy_size     type = capital_ship the specified number of ships, or 1.0 The type to check for. Optional. in

archetype = <string> } /Hearts of Iron IV/common/units/equipment/*.txt

The ship archetype to check for. Optional. files.

size = <float>

The amount to check for. has_deployed_air_force_size = { Checks if the current scope has Airwing types are defined within has_deployed_air_force_size     size > 10 the specified number of aircraft, or /Hearts of Iron IV/common/units/*.txt files. Must 1.0

type = <string>     type = cas of a specified type of aircraft. use either > or < operators for size.

The type to check for. Optional. }

size = <float>

The amount to check for.

type = <string>

The battalion type to check for. Divisions that are majority made up of battalions in that type divisions_in_state = {

count. Optional, counts all divisions by default.     type = armor Checks if the specified state Battalions and their types are defined within divisions_in_state     size > 10 contains the specified amount of /Hearts of Iron IV/common/units/*.txt files. Must 1.0

unit = <string> state = 49 divisions. use either > or < operators for size.

The exact battalion to check for. Divisions that }

are majority made up of that battalions count. Optional, counts all divisions by default. state = <scope> / <variable>

The state to check in.

amount <> <float>

The amount to check for. army_manpower_in_state = {

type = <string>

army_manpower_in_state     amount > 10000 contains the specified amount of /Hearts of Iron IV/common/units/*.txt files. Must 1.6 The type to check for. Optional.

state = <scope> / <variable> }

The state to check in.

size = <float>

The amount to check for.

type = <string>

The battalion type to check for. Divisions that divisions_in_border_state = { Checks if the border provinces

are majority made up of battalions in that type     type = infantry Battalion types are defined within divisions_in_border_state count. Optional, counts all divisions by default.     size > 10 between the specified state and

/Hearts of Iron IV/common/units/*.txt files. Must 1.5 state = 49 border state contain the specified

state = <scope> / <variable>     border_state = var:state use either > or < operators for size. amount of divisions.

The state to check in. }

border_state = <scope> /

The border state to check in.

count = <int>

The amount to check for.

states = { <int> <...> <int> }

The states to check in. types = { <string> <...> <string>

} num_divisions_in_states = { Divisions and their types are defined within

The battalion types to check for. Divisions that     count > 24 Checks if the specified states /Hearts of Iron IV/common/units/*.txt files. Can

num_divisions_in_states are majority made up of battalions in that type     states = { 550 559 271 } contain enough divisions of the 1.12 use either =, >, or < operators for count. The tooltip does not

count. Optional, counts all divisions by default.     exclude = { irregular_infantry } specified types. } specify the states the check runs for nor the filtered types.

exclude = { <string> <...> <string>

}

The sub-units to exclude from the search. Divisions that are majority made up of specified battalions are excluded. Optional, excludes no divisions by default.

count = <int>

The amount to check for.

states = { <int> <...> <int> }

The states to check in. num_battalions_in_states = { Battalions and their types are defined within count > 24 Checks if the specified states

types = { <string> <...> <string> /Hearts of Iron IV/common/units/*.txt files. Can num_battalions_in_states     states = { 550 559 271 } contain enough battalions (or sub- 1.12

} use either =, >, or < operators for count. The tooltip does not exclude = { irregular_infantry } units) of the specified types.

The battalion types to check for. } specify the states the check runs for nor the filtered types. exclude = { <string> <...> <string>

}

The sub-units to exclude from the search.

size = <float>

The amount to check for. ships_in_state_ports = { Checks if the specified state type = capital_ship Ship types are defined within

type = <string> contains the specified amount of ships_in_state_ports     size > 10 /Hearts of Iron IV/common/units/*.txt files. Must 1.0

The type to check for. Optional. ships, or of ships of the specified state = 49 use either > or < operators for size.

state = <scope> / <variable> } type. The state to check in.

value = <float>

The amount to check for. num_planes_stationed_in_regions = { Checks if the current scope has num_planes_stationed_in_regions     value > 10 the specified number of aircraft Must use either =, >, or < operators for value. 1.12

regions = { <id> <...> <id> }     regions = { 123 321 }

The regions to check in. } stationed within strategic regions.

tag = <scope> Checks if the current scope has The country to check for. has_volunteers_amount_from = {

has_volunteers_amount_from     tag = GER recieved volunteers from the Must use either > or < operators for count. 1.0

count = <int>     count > 10 specified country of the specified The amount to check for. } amounts.

<float> Checks how much the convoys are Must use either > or < operators for count. Threat is always convoy_threat convoy_threat > 0.5 1.6

The threat to compate with. threatened. between 0 and 1.

target = <tag>

The country the coast of which is mined. has_mined = { Checks if the current scope has X has_mined     target = POL mines on the coast of the specified Must use either > or < operators for value. 1.6

value <> <int>     value > 1000 country.

The amount of mines to compare with. }

region = <ID>

The strategic region that contains the mines. has_mined = { Checks if the current scope has at has_mines     target = POL least X mines within the specified 1.6

amount = <int>     amount = 1000 strategic region.

The amount of mines to compare with. }

<float> Checks how dangerous enemy Must use either > or < operators for count. Threat is always mine_threat mine_threat < 0.6 1.6

The threat to compate with. mines are. between 0 and 1.

Accepts variables. 1.13 The id to check for. infantry_mio_token MIO with the specified name.

Check if the given tactic is <tactic>

has_tactic has_tactic = tactic_masterful_blitz unlocked (or active by default) for 1.17 The tactic to check for.

the country.

Doctrine ​[ edit | edit source ]

Doctrine-related country-scoped triggers: [Collapse]

Version Name Parameters Examples Description Notes

Added

<string> Checks if any grand doctrine in folder is currently has_any_grand_doctrine has_any_grand_doctrine = land Folders are land, naval and air. 1.17.2

Doctrine folder. active for the country.

has_doctrine = mobile_warfare # Grand

doctrine <grand doctrine> / <subdoctrine> Checks if the given grand doctrine or subdoctrine is

has_doctrine 1.17 The doctrine to check for. currently active for the country.

has_doctrine = mobile_infantry # Subdoctrine

The track to check for. (any instance of) the given track.

Checks if the current country has ever completed <subdoctrine>

has_completed_subdoctrine has_completed_subdoctrine = mobile_infantry the specified subdoctrine (even if it was later 1.17 The subdoctrine to check for.

switched out).

The track to check for. completed

amount = <int>

The amout to check for. has_mastery = {

has_mastery     amount = 200 Checks if any track of the given type has at least X 1.17

track = <track>     track = infantry mastery. The track to check for. }

amount = <int>

The amount to check for. has_mastery_level = { Checks if the country has reached the specified has_mastery_level     amount = 2 number of mastery levels (rewards) for the given 1.17

subdoctrine = <subdoctrine>     subdoctrine = mobile_infantry

The subdoctrine to check for. } subdoctrine.

Equipment ​[ edit | edit source ]

Equipment-related country-scoped triggers: [Collapse]

Version Name Parameters Examples Description Notes

Added

archetype = <string> Must use either > or < operators for ratio. The equipment archetype to check for. stockpile_ratio = { Checks if the current scope has stockpiled the

ment which is not fielded as other stockpile_ratio     archetype = infantry_equipment For the convoy equip

specified equipment to the specified ratio 1.5 ratio = <float>     ratio > 0.5 equipments, ratio shall be not a percentage but a direct

The ratio of equipment to check for. } against fielded equipment of the same type. amount (for instance 256 convoys)

has_equipment = { <equipment> = <int> / <variable> Checks if the current scope has the specified

has_equipment     infantry_equipment_1 > 10 Must use either > or < operators. 1.0 The equipment to check for, and the amount to check for. equipment to the specified amount.

}

<bool> Checks if the current scope has any licenses has_any_license has_any_license = yes 1.0

Boolean. from other countries.

<scope> / <variable> Checks if the current scope is licensing to the is_licensing_any_to is_licensing_any_to = GER 1.0

The country to check for. specified scope.

is_licensing_to = {

target = <scope>     target = GER

The country to check for.     archetype = infantry_equipment

} archetype = <string>

The equipment archetype to check for. Optional. Checks if the current scope is licensing the

is_licensing_to Equipment scope is_licensing_to = { 1.0 specified equipment to the specified country.

type = <string>     target = GER

The equipment to check for. Optional.     equipment = {

version = <int>

The variant id of the equipment. Optional.

}

has_license = {

from = <scope>     from = GER

The country to check for.     archetype = infantry_equipment

} archetype = <string>

The equipment archetype to check for. Optional. Checks if the current scope has a license for has_license Equipment scope has_license = { the specified equipment from the specified 1.0

type = <string>     from = GER country. The equipment to check for. Optional.     equipment = {

version = <int>

The variant id of the equipment. Optional.     }

}

<float> / <variable> fuel_ratio fuel_ratio > 0.4 Checks the fuel ratio of the country. Must use either < or > operators. 1.6

The ratio to check with.

<int> / <variable> has_fuel has_fuel > 400 Checks the fuel amount of the country. Must use either < or > operators. 1.6

The amount to compare with.

Checks if the country has a builtable non- <archetype> Equipment archetypes can be seen in

has_design_based_on has_design_based_on = light_tank_chassis obsolete design based on the specified 1.11 The equipment archetype. /Hearts of Iron IV/common/units/equipment/*.

equipment archetype.

Intelligence ​[ edit | edit source ]

Intelligence-related country-scoped triggers: [Collapse]

Version Name Parameters Examples Description Notes

Added

tag = <scope>

The country to check for. estimated_intel_max_piercing = { Checks if the specified scope has the specified Must use either > or < operators for

estimated_intel_max_piercing     tag = GER amount of piercing based on the current scope's 1.0 value = <int>     value > 2 value.

intel. The amount to check for. }

tag = <scope>

The country to check for. estimated_intel_max_armor = { Checks if the specified scope has the specified estimated_intel_max_armor     tag = GER Must use either > or < operators for

amount of armor based on the current scope's 1.0 value = <int>     value > 2 value.

The amount to check for. } intel.

target = <tag> / <variable>

The target to compare with.

civilian_intel <>= <float> / <variable> compare_intel_with = {

Comparison of civilian intel. Can use < (in which case the target = POL

army_intel <>= <float> / <variable> compare_intel_with     civilian_intel > 0.5 current country has x less intel), >,

Compares intel between 2 countries. 1.9 Comparison of army intel.     army_intel = 0 and = (in which case it must be

navy_intel <>= <float> / <variable> equal).

} Comparison of navy intel. airforce_intel <>= <float> / <variable>

Comparison of airforce intel.

target = <tag> / <variable>

The target to compare with.

civilian_intel <>= <float> / <variable> intel_level_over = {

Comparison of civilian intel. Can use < (in which case the target = POL

army_intel <>= <float> / <variable> nt country has x less intel), >, intel_level_over     civilian_intel > 0.5 Checks the intel level from the current country curre

1.9 Comparison of army intel.     army_intel = 0 over a specified country. and = (in which case it must be

navy_intel <>= <float> / <variable> equal).

} Comparison of navy intel. airforce_intel <>= <float> / <variable>

Comparison of airforce intel.

<boolean> Checks if the current scope has an intelligence has_intelligence_agency has_intelligence_agency = yes 1.9

The intelligence agency to check. agency.

target = <tag> / <variable>

The country which is checked. network_national_coverage = {

network_national_coverage     target = POL Checks network national coverage over a Must use < or > for value.

value <> <float> / <variable>     value < 70 specific country. The value of network. }

target = <tag>

The country which is checked. network_strength = {

state = <id> / <variable> Must use < or > for value. Can use network_strength     target = POL Checks network national coverage over a

1.9 The state which is checked.     value < 70 specific country. either or both of target and state.

} value <> <float> / <variable>

The strength of network.

1.9 The agency upgrade to check. upgrade_army_department agency upgrade (to its highest level).

<int> / <variable> Checks the number of upgrades done in the agency_upgrade_number agency_upgrade_number > 4 Must use either > or < operators. 1.9

The amount of agency upgrades to check for. current scope's intelligence agency.

target = <tag>

The country to compare with. decryption_progress = {

decryption_progress     target = POL Checks the decryption progress towards a Must use either > or < operators for 1.9

value <> <float>     value < 0.5 country. value. The value to compare. }

has_captured_operative = POL <tag>/<bool>

Checks if the current scope has captured an has_captured_operative Country whose operative was captured/Whether an operative 1.9

operative. was captured.

has_captured_operative = yes

target = <tag>

Country towards whom the operation is targeted. has_finished_collecting_for_operation = {

has_finished_collecting_for_operation operation = <token>     operation = 1.9

collecting resources for an operation. The operation which current scope is planning against the operation_infiltrate_armed_forces_navy

target. }

target = <tag>

Country towards whom the operation is targeted. is_preparing_operation = {

is_preparing_operation operation = <token>     operation = 1.9

operation against the specified country. The operation which current scope is planning against the operation_infiltrate_armed_forces_navy

target. Optional. }

target = <tag>

Country towards whom the operation is targeted. is_running_operation = {

is_running_operation operation = <token>     operation = 1.9

operation against the specified country. The operation which current scope is planning against the operation_infiltrate_armed_forces_navy

target. Optional. }

target = <tag>

Country towards whom the operation is targeted. num_finished_operations = {

num_finished_operations operation = <token>     operation = 1.9

current scope had against the specified country. The operation which current scope is planning against the operation_infiltrate_armed_forces_navy

target. Optional. }

tag = <tag>

Country towards whom the operation is targeted. has_operation_token = {

Checks if the current scope has an operation has_operation_token     tag = POL 1.9

token = <token>     token = token_name token against an another country. The operation token. }

<tag> Checks if the current scope has any decryption is_active_decryption_bonuses_enabled is_active_decryption_bonuses_enabled = POL 1.9

The country towards which the bonus is enabled. bonuses towards the specified country.

<bool> Checks if the current scope has a cryptology is_cryptology_department_active is_cryptology_department_active = yes 1.9

Boolean. department active.

<tag> Checks if the current scope is decrypting a is_decrypting is_decrypting = POL 1.9

The country which is decrypted. certain country.

<tag> Checks if the current scope has fully decrypted is_fully_decrypted is_fully_decrypted = POL 1.9

The country which is decrypted. a certain country.

Amount of divisions.

Amount of slots.

Amount of slots.

Amount of operatives.

AI ​[ edit | edit source ]

This is a community maintained wiki. If you spot a mistake, please help with fixing it.

AI-related country-scoped triggers: [Collapse]

Version Name Parameters Examples Description Notes

Added

<int> Checks if the current scope AI has the ai_irrationality ai_irrationality > 10 Must use either > or < operators. 1.0

The amount to check for. specified irrationality.

target = <scope>

The country to check for. ai_liberate_desire = { Checks if the current scope AI has the ai_liberate_desire     target = GER specified liberation desire towards the Must use either > or < operators for count. 1.0

count = <float>     count > 1 specified country.

The amount to check for. }

<string> Checks if the current scope AI has a Roles are defined in ai_has_role_division ai_has_role_division = infantry 1.0

The role to check for. division with the specified role. /Hearts of Iron IV/common/ai_templates/*.txt

<string> Checks if the current scope AI has a Roles are defined in ai_has_role_template ai_has_role_template = armor 1.0

The role to check for. division template with the specified role. /Hearts of Iron IV/common/ai_templates/*.txt

<int> Checks if the current scope AI desires the ai_wants_divisions ai_wants_divisions > 10 Must use either > or < operators. 1.0

The amount to check for. specified amount of divisions.

Checks if the current scope AI has a <string>

has_template_ai_majority_unit has_template_ai_majority_unit = infantry division template mostly made up of the 1.0 The unit to check for.

specified unit.

Characters ​[ edit | edit source ]

This is a community maintained wiki. If you spot a mistake, please help with fixing it.

These are character-related triggers in the country scope, not character-scoped triggers.

Character-related country-scoped triggers: [Collapse]

Version Name Parameters Examples Description Notes

Added

Checks if the specified character has a country <character>

can_be_country_leader can_be_country_leader = POL_character_test leader role, active or not, and can utilise it in this 1.11 The character to check.

country.

Checks if the current scope has the specified <string>

has_character has_character = my_character character recruited. The character does NOT need 1.11 The character to check.

to be in power.

has_country_leader = {

}

ruling_only = <bool> (default = yes) Limit check to ruling only.

has_country_leader = { character = <character_token>  (recommended criteria)

character = SPR_niceto_alcala_zamora

The character to check for. Optional. Checks if the current scope has the specified has_country_leader ruling_only = yes 1.3

name = <string> } country leader. The name to check for. Optional. id = <int>

The id to check for. Optional. has_country_leader = {

}

<ideology> Checks if the current scope's active country leader has_country_leader_ideology has_country_leader_ideology = nazism 1.11

Checks the ideology of the active country leader has the specified ideology.

1.6 The trait to check. champion_of_peace_1 trait.

Boolean.

<int> Checks if the current scope has a unit leader with Only the legacy ID can be used, the has_unit_leader has_unit_leader = 1 1.0

The id to check for. the specified id. character ID doesn't work.

specialization = <specialization_token> y in scope has a scientist with has_scientist_specialization has_scientist_specialization = Checks if the countr

1.15 Specialization. specialization_nuclear a skill level of at least 1 in specialization.

Peace conferences ​[ edit | edit source ]

This is a community maintained wiki. If you spot a mistake, please help with fixing it.

These are not exactly peace conference-related triggers, but those that can only be used within peace conferences.

Peace conference-only country-scoped triggers: [Collapse]

Version Name Parameters Examples Description Notes

Added

<bool> Checks if the current scope is a winner within the pc_is_winner pc_is_winner = yes 1.12

Boolean. peace conference.

<bool> Checks if the current scope is on the winning side pc_is_on_winning_side pc_is_on_winning_side = yes 1.12

Boolean. within the peace conference.

<bool> Checks if the current scope is a loser within the pc_is_loser pc_is_loser = yes 1.12

Boolean. peace conference.

<bool> Checks if the current scope is an untouched loser pc_is_untouched_loser pc_is_untouched_loser = yes 1.12

Boolean. within the peace conference.

<scope> Checks if the current scope is on the same side of pc_is_on_same_side_as pc_is_on_same_side_as = BHR 1.12

Country to check for. the peace conference as the specified country.

<bool> Checks if the current scope has been liberated pc_is_liberated pc_is_liberated = yes 1.12

Boolean. within the peace conference.

Checks if the current scope has been liberated <scope>

pc_is_liberated_by pc_is_liberated_by = BHR within the peace conference by the specified 1.12 Country to check for.

country.

<bool> Checks if the current scope has been puppeted pc_is_puppeted pc_is_puppeted = yes 1.12

Boolean. within the peace conference.

Checks if the current scope has been puppeted <scope>

pc_is_puppeted_by pc_is_puppeted_by = BHR within the peace conference by the specified 1.12 Country to check for.

country.

<bool> Checks if the current scope has had an enforced pc_is_forced_government pc_is_forced_government = yes 1.12

Boolean. government change within the peace conference.

Checks if the current scope has had an enforced <scope>

pc_is_forced_government_by pc_is_forced_government_by = BHR government change within the peace conference 1.12 Country to check for.

demanded by the specified country.

Checks if the current scope has had an enforced <ideology group>

pc_is_forced_government_to pc_is_forced_government_to = democratic government change to the specified ideology 1.12 Ideology group to check for.

group.

<decimal> Checks if the current scope has the specified Can only be used for the winning pc_total_score pc_total_score > 2400 1.12

Scope to check for. amount in total score within the peace conference. countries.

Checks if the current scope has the specified <decimal> Can only be used for the winning

pc_current_score pc_current_score > 100 amount in current score within the peace 1.12 Scope to check for. countries.

conference.

Faction scope ​[ edit | edit source ]

Can be used in faction scope.

There are currently no faction-specific triggers.

State scope ​[ edit | edit source ]

This is a community maintained wiki. If you spot a mistake, please help with fixing it.

Can be used in state scope.

General ​[ edit | edit source ]

This is a community maintained wiki. If you spot a mistake, please help with fixing it.

General state-scoped triggers: [Collapse]

Version Name Parameters Examples Description Notes

Added

state = 10

<scope> / <variable> Checks if the current scope is the specified state 1.0

The state to check for. state. state = var:state

<int> Checks if the current scope is a state in region region = 10 1.0

The strategic region id to check for. the specified strategic region.

<building> (building_count_trigger) The amount of the specified building to arms_factory > 10 1.0 specified amount of the specified building. Can also be used in building scope.

check for.

building = <string>

The building to check for. free_building_slots = {

size = <int> Checks if the current scope has available

free_building_slots     size > 10 Must use either > or < operators for size. 1.0 The amount to check for. slots for the specified amount of buildings.

include_locked = <bool> }

Whether to include locked slots.

building = <string>

The building to check for. non_damaged_building_level = { Checks if the current scope has the non_damaged_building_level     building = arms_factory specified amount of the specified buildings Must use either > or < operators for level. 1.9

level = <int>     level > 4

The amount to check for. } that are undamaged.

building = <string>

The building to check for. any_province_building_level = {

limit = <int>     province = {

The amount to check for.         id = 445 Checks if the current scope has the

Province scope         id = 494

any_province_building_level         limit_to_border = yes specified provincal building at the specified Must use either > or < operators for level. 1.0 id = <int>

The province to check for.     building = bunker

limit_to_border = <bool>     level < 5

Whether to limit check to border }

provinces.

<string> Checks if the current scope has the has_state_flag has_state_flag = my_flag 1.0

The flag to check for. specified flag.

flag = <string>

The flag to check.

value = <int> has_state_flag = {

The flag value to check for. Optional. flag = my_flag

date = <date> has_state_flag     days > 30 Compares the specified flag's last set date, If not set, the value comparison is >0 . value  is limited

1.0 The flag creation date to check for.     date > 1936.6.1 days since last set, and/or value. between -32768 and 32767. Optional.     value > 0

} days = <int>

The duration the flag existed for. Optional.

<float> Checks if the current scope has the state_population state_population > 10000 Must use either > or < operators. 1.0

The amount to check for. specified state population.

<float> Checks if the current scope has the state_population_k state_population_k > 10 Must use either > or < operators. 1.0

The amount to check for. specified state population in thousands.

Boolean.

<scope> / <variable> Checks if the current scope is controlled by is_controlled_by is_controlled_by = GER 1.0

The country to check for. the specified country.

<scope> / <variable> Checks if the current scope is fully is_fully_controlled_by is_fully_controlled_by = GER 1.5

The country to check for. controlled by the specified country.

<scope> / <variable> Checks if the current scope is owned by is_owned_by is_owned_by = GER 1.0

The country to check for. the specified country.

<scope> / <variable> Checks if the current scope is claimed by is_claimed_by is_claimed_by = GER 1.0

The country to check for. the specified country.

<scope> / <variable> Checks if the current scope is a core of the is_core_of is_core_of = GER 1.0

The country to check for. specified country.

<scope> / <variable> Checks if the current scope is owned and is_owned_and_controlled_by is_owned_and_controlled_by = GER 1.0

The country to check for. controlled by the specified country.

<bool> Checks if the current scope is a is_demilitarized_zone is_demilitarized_zone = yes 1.0

Boolean. demilitarized zone.

<bool> Checks if the current scope is part of a is_border_conflict is_border_conflict = yes 1.0

Boolean. border war.

Checks if the current scope is connected <bool> to the capital state over land. The scope

is_in_home_area is_in_home_area = yes 1.0 Boolean. needs to be owned as well for the

statement for it to be true.

<bool> Checks if the current scope is a coastal is_coastal is_coastal = yes 1.0

Boolean. state.

<bool> Checks if the current scope is a coastal is_one_state_island is_one_state_island = yes An adjacent land state may be connected via a naval crossing. 1.13

Boolean. state with no adjacent land states.

Checks if the current scope is a state <bool> An adjacent land division may be connected via a naval

is_island_state is_island_state = yes where every province has no land 1.0 Boolean. crossing.

neighbour.

<string> Checks if the current scope is on the Continents are found in is_on_continent is_on_continent = europe 1.0

The continent to check for. specified continent. /Hearts of Iron IV/map/continent.txt.

Checks if the scope state is on the same <scope> / <variable>

is_on_same_continent_as is_on_same_continent_as = FRA continent as the given state. The capital Can also be used in country scope. 1.17 The country to check for.

state is used for given country tag.

Boolean.

<string> Checks if the current scope has the State categories are found in has_state_category has_state_category = rural 1.0

The category to check for. specified category. /Hearts of Iron IV/common/state_category/*.txt.

<int> Checks if the current scope has the state_strategic_value state_strategic_value > 10 Must use either > or < operators. 1.5

The amount to check for. specified strategic value.

<int> Checks if the current scope has the state_and_terrain_strategic_value state_and_terrain_strategic_value > 10 Must use either > or < operators. 1.5

The amount to check for. specified state and terrain strategic value.

owner = <scope>

The country to check for. num_owned_neighbour_states = { Checks if the current scope has the num_owned_neighbour_states     owner = GER specified amount of neighbor states Must use either > or < operators for count. 1.0

count = <int>     count > 2 belonging to the specified country.

The amount to check for. }

value = <float>

The distance to check for. distance_to = {

hecks if the current scope is at the distance_to     value > 1000 C

Must use either > or < operators for distance. 1.0 target = <scope>     target = 49 specified distance from the specified state.

The state to compare against. }

area = <int>

The strategic region to check for. Checks if the current scope has the ships_in_area ships_in_area = { area = 104 size > 14 } specified amount of ships in the specified Must use either > or < operators for count. 1.0

size = <int> strategic region.

The amount to check for.

<int> Checks if the current scope has the Must use either > or < operators for amount. <resource> (resource_count_trigger) tungsten > 10 ???

The amount to check for. specified amount of the specified resource. Can also be used in country scope.

resource = <string>

The resource to check for.

amount = <int> has_resources_amount = {

The amount to check for.     resource = oil Checks if the current scope has the has_resources_amount     amount > 10 Must use either > or < operators for amount. 1.3

delivered = <bool> specified amount of the specified resource. delivered = yes

If specified, checks the amount after the }

modifiers are applied rather than the base resource value.

receiver = <scope>

The receiver of the resource rights. Mandatory if used in state scope. has_resources_rights = { Can be used in either state or country scope. Always returns

has_resources_rights   receiver = POL Checks if there are any resource rights false if the state has no resources. resources = { <resource> 1.12

resources = { oil steel } with the specified parameters. <...> <resource> } } Can also be used in country scope. Resources to check for. Optional, defaults to any if unset.

Checks how many days have passed <int>

days_since_last_strategic_bombing days_since_last_strategic_bombing < 10 since the last strategic bombing of the Must use either > or < operators. 1.6 The amount to compare with.

state.

has_railway_connection = {

start_state = 10

target_state = 90

<scope> / <variable> }

The states to check. Returns true if the states are connected by

has_railway_connection 1.11 <id> a railway. Can also check provinces.

has_railway_connection = { The provinces to check. Optional. start_province = 402

target_province = 9400

}

can_build_railway = {

start_state = 10

target_state = 90

<scope> / <variable> }

The states to check. Returns true if a railway can be built can_build_railway between states. Can also check for 1.11

The provinces to check. Optional. start_province = 402

target_province = 9400

}

<scope> / <variable>

The states to check. has_railway_level = {

has_railway_level     state = 114 Checks if a state contains a railway at or Works with level 1, 2, 3, 4 or 5. Level 0 does not work. 1.11

<int>     level = 5 above the specified level. }

Railway level.

Checks if the current scope was <bool>

pc_does_state_stack_demilitarized pc_does_state_stack_demilitarized = yes demilitarised during a current or 1.12 Boolean.

previously-ended peace conference.

Checks if the current scope was <bool>

pc_does_state_stack_dismantled pc_does_state_stack_dismantled = yes dismantled during a current or previously- 1.12 Boolean.

ended peace conference.

Checks if the current scope was claimed <scope>

pc_is_state_claimed pc_is_state_claimed = yes by any country during the peace Can only be used within peace conferences. 1.12.8 Country to check for.

conference.

Checks if the current scope was claimed by the specified country during the peace conference. Note, that "claim" in this context, while includes, is NOT limited to

<scope> outright taking: forcing government, pc_is_state_claimed_by pc_is_state_claimed_by = BHR Can only be used within peace conferences. 1.12

Country to check for. puppeting and liberating will render that trigger true as well. If one looks specifically for states taken by victors for themselves, pc_is_state_claimed_and_taken_by should be used.

Checks if the current scope was claimed <scope> with "Take State" action (i.e. annexed) by

pc_is_state_claimed_and_taken_by pc_is_state_claimed_and_taken_by = SOV Can only be used within peace conferences. Country to check for. the specified country during the peace

conference.

<scope> tate is outside of the Can only be used within peace conferences. Was called pc_is_state_outside_influence_for_winner pc_is_state_outside_influence_for_winner = Checks if the current s

1.12.8 Country to check for. ROOT influence of the specified winner country. pc_is_state_outside_influence_for prior to 1.12.8.

Compares the amount of turns that have <int>

pc_turn pc_turn > 20 passed during the peace conference with a Can only be used within peace conferences. 1.12.8 The amount of turns to check for.

number.

Checks if the country (as ROOT) and state can_construct_building <build type> The type of building. can_construct_building = bunker 1.15

in scope can build a building in the state.

Checks if a state has the specified country <country> / <variable> as a contested owner. The trigger can be

has_contested_owner has_contested_owner = GER Can also be used in country scope. 1.15 Country to check. used either from a country or a state scope

and accepts the other as parameter.

Resistance and Compliance ​[ edit | edit source ]

This is a community maintained wiki. If you spot a mistake, please help with fixing it.

Resistance-related state-scoped triggers: [Collapse]

Version Name Parameters Examples Description Notes

Added

<int> Compares the compliance value of the current scope compliance compliance > 50 Must use either > or < operators. 1.9

The amount to compare with. with the given value.

<int> Compares the compliance speed of the current compliance_speed compliance_speed > 50 Must use either > or < operators. 1.9

The amount to compare with. scope with the given value.

Boolean.

Boolean.

<int> Compares the resistance value of the current scope resistance resistance > 50 Must use either > or < operators. 1.9

The amount to compare with. with the given value.

<int> Compares the resistance speed of the current scope resistance_speed resistance_speed > 50 Must use either > or < operators. 1.9

The amount to compare with. with the given value.

<int> Compares the target resistance value of the current resistance_target resistance_target > 50 Must use either > or < operators. 1.9

The amount to compare with. scope with the given value.

<token> Checks if the current scope has an occupation has_occupation_modifier has_occupation_modifier = modifier_name 1.9

The occupation modifier to check. modifier, changing resistance/compliance.

The occupation law to check.

The occupation tag to check.

Character scope ​[ edit | edit source ]

This is a community maintained wiki. If you spot a mistake, please help with fixing it.

Can be used in Character scope.

General ​[ edit | edit source ]

General character-scoped triggers: [Collapse]

Version Name Parameters Examples Description Notes

Added

<scope> Checks if the current character's token matches is_character is_character = POL_test_character 1.11

Character ID. up with the specified one.

<bool> Checks if the character in the current scope has can_be_country_leader can_be_country_leader = yes 1.11

Boolean. a country leader role, active or non-active.

<bool> Checks if the character in the current scope is is_country_leader is_country_leader = yes 1.11

Boolean. the active country leader.

<bool> Checks if the character in the current scope has is_unit_leader is_unit_leader = yes 1.11

Boolean. an active unit leader (Army/Navy leader) role.

Checks if the character in the current scope has <bool>

is_advisor is_advisor = yes an advisor role (includes advisors/theorists/high 1.11 Boolean.

command).

<bool> Checks if the character in the current scope is Prior to 1.12, checked if the character had a role within is_air_chief is_air_chief = yes 1.11

Boolean. selected as an air chief. the slot, regardless of being selected.

<bool> Checks if the character in the current scope is Prior to 1.12, checked if the character had a role within is_army_chief is_army_chief = yes 1.11

Boolean. selected as an army chief. the slot, regardless of being selected.

<bool> Checks if the character in the current scope has is_army_leader is_army_leader = yes 1.11

Boolean. an army leader (General/Field Marshal) role.

<bool> Checks if the character in the current scope is Prior to 1.12, checked if the character had a role within is_navy_chief is_navy_chief = yes 1.11

Boolean. selected as a navy chief. the slot, regardless of being selected.

<bool> Checks if the character in the current scope has is_navy_leader is_navy_leader = yes 1.11

Boolean. an navy leader (Admiral) role.

<bool> Checks if the character in the current scope is Prior to 1.12, checked if the character had a role within is_high_command is_high_command = yes 1.11

Boolean. selected as high command. the slot, regardless of being selected.

<bool> Checks if the character in the current scope is a is_corps_commander is_corps_commander = yes 1.11

Boolean. corps commander.

<bool> Checks if the character in the current scope is is_operative is_operative = yes 1.11

Boolean. an operative.

<bool> Checks if the character in the current scope is Prior to 1.12, checked if the character had a role within is_political_advisor is_political_advisor = yes 1.11

Boolean. selected as a political advisor. the slot, regardless of being selected.

<bool> Checks if the character in the current scope is Prior to 1.12, checked if the character had a role within is_theorist is_theorist = yes 1.11

Boolean. selected as a theorist. the slot, regardless of being selected.

<string> Checks if the character in the current scope has Character slots are defined within is_character_slot is_character_slot = political_advisor 1.11

The advisor slot to check. a role within the specified character slot /Hearts of Iron IV/common/idea_tags/*.txt.

<bool> Checks if the character in the current scope has has_air_ledger has_air_ledger = yes 1.11

Boolean. an air ledger.

<bool> Checks if the character in the current scope has has_army_ledger has_army_ledger = yes 1.11

Boolean. an army ledger.

<bool> Checks if the character in the current scope has has_navy_ledger has_navy_ledger = yes 1.11

Boolean. an navy ledger.

<string> Checks if the current scope has the specified has_character_flag has_character_flag = my_flag 1.11

The flag to check for. flag.

flag = <string>

The flag to check. has_character_flag = {

value = <int> flag = my_flag

The flag value to check for. Optional. has_character_flag     days > 30 Compares the specified flag's last set date, If not set, the value comparison is >0 . value  is

1.11 date = <date>     date > 1936.6.1 days since last set, and/or value. limited between -32768 and 32767.

The flag creation date to check for. Optional.     value > 0

} days = <int>

The duration the flag existed for. Optional.

<trait> Checks if the current scope has the specified has_trait has_trait = really_good_boss 1.5

The trait to check for. trait.

<int> Checks if the current character has the has_id has_id = 1 1.5

The id to check for. specificed ID.

Advisors ​[ edit | edit source ]

This is a community maintained wiki. If you spot a mistake, please help with fixing it.

These triggers are to be used specifically for advisors.

Advisor-scoped triggers: [Collapse]

Version Name Parameters Examples Description Notes

Added

<bool> Checks if the current character is activated as an is_hired_as_advisor is_hired_as_advisor = yes 1.12.10

Boolean. advisor in any slot.

1.11 The slot to check in. political_advisor exception of the specified slot.

<bool> advisor_can_be_fired = no

Boolean. If an advisor is available in multiple Checks if the current character's can_be_fired

advisor_can_be_fired OR slots, the long version is mandatory to 1.12.8 advisor_can_be_fired = { attribute is set or not within a certain slot.

slot = <slot> use. slot = political_advisor

The slot to check in. }

<slot> Checks if the character in scope has an advisor role has_advisor_role has_advisor_role = political_advisor Possible advisor role slots. ???

The slot to check in. for the given slot.

Country leaders ​[ edit | edit source ]

This is a community maintained wiki. If you spot a mistake, please help with fixing it.

These triggers are to be used specifically for country leaders.

Country leader-scoped triggers: [Collapse]

Version Name Parameters Examples Description Notes

Added

<ideology> Checks if the current character has the specificed has_ideology has_ideology = liberalism 1.11

The sub-ideology to check for. sub-ideology assigned.

<ideology> Checks if the current character has the specificed has_ideology_group has_ideology_group = democratic 1.11

The ideology to check for. ideology assigned.

Unit leaders ​[ edit | edit source ]

This is a community maintained wiki. If you spot a mistake, please help with fixing it.

These triggers are to be used specifically for unit leaders, i.e. generals and admirals.

Unit leader-scoped triggers: [Collapse]

Version Name Parameters Examples Description Notes

Added

<string> Checks if the current scope has the Deprecated. Use has_unit_leader_flag has_unit_leader_flag = my_flag 1.5

The flag to check for. specified flag. has_character_flag instead.

flag = <string>

The flag to check. has_unit_leader_flag = { Deprecated. Use

value = <int> flag = my_flag has_character_flag instead. If not

The flag value to check for. Optional. has_unit_leader_flag     days > 30 Compares the specified flag's last set date,

set, the value comparison is >0 . 1.5 date = <date>     date > 1936.6.1 days since last set, and/or value.

The flag creation date to check for. Optional.     value > 0 value  is limited between } -32768 and 32767.

days = <int>

The duration the flag existed for. Optional.

<bool> Checks if the current scope is leading a is_leading_army is_leading_army = yes 1.5

Boolean. single army.

<bool> Checks if the current scope is leading an is_leading_army_group is_leading_army_group = yes 1.5

Boolean. army group.

If the target country is in a civil <tag> Checks if the current scope is leading a

is_leading_volunteer_group is_leading_volunteer_group = POL war, this will only be valid for one 1.11 Country tag. volunteer army within the specified country.

side.

Checks if the current scope is leading a If the target country is in a civil <tag>

is_leading_volunteer_group_with_original_country is_leading_volunteer_group_with_original_country volunteer army within a country of the war, this will only be valid for each 1.11 Country tag. = POL

specified original tag. side.

<bool> Checks if the current scope is a Field is_field_marshal is_field_marshal = yes 1.5

Boolean. Marshal.

<bool> Checks if the current scope is an assigned is_assigned is_assigned = yes 1.5

Boolean. unit leader.

<string> Checks if the current scope can select the can_select_trait can_select_trait = offensive_doctrine 1.5

The trait to check for. specified trait.

<string> Checks if the current scope has the has_ability has_ability = glider_planes 1.5

The ability to check for. specified unit leader ability.

<int> Checks if the current scope has a Skill skill skill > 1 Must use either > or < operators. 1.5

The amount to check for. above the specified amount.

Checks if the current scope has a Skill <int> advantage above the specified amount in

skill_advantage skill_advantage > 1 Must use either > or < operators. 1.5 The amount to check for. against an enemy unit leader whilst in

combat.

<int> Checks if the current scope has a Planning planning_skill_level planning_skill_level > 1 Must use either > or < operators. 1.5

The amount to check for. skill above the specified amount.

<int> Checks if the current scope has a Logistics logistics_skill_level logistics_skill_level > 1 Must use either > or < operators. 1.5

The amount to check for. skill above the specified amount.

<int> Checks if the current scope has a Defense defense_skill_level defense_skill_level > 1 Must use either > or < operators. 1.5

The amount to check for. skill above the specified amount.

<int> Checks if the current scope has a Attack attack_skill_level attack_skill_level > 1 Must use either > or < operators. 1.5

The amount to check for. skill above the specified amount.

<int> Checks if the current scope has an average_stats average_stats > 5 Must use either > or < operators. 1.5

The amount to check for. average skill above the specified amount.

<bool> Checks if the current socpe is in a border is_border_war is_border_war = yes 1.5

Boolean. war.

<int> Checks if the current scope is commanding num_units num_units > 5 Must use either > or < operators. 1.5

The amount to check for. the specified amount of divisions.

<bool> Checks if the current scope is a general is_exiled_leader is_exiled_leader = yes 1.6

Boolean. from an exiled country.

<tag> Checks if the current scope is a general is_exiled_leader_from is_exiled_leader_from = POL 1.6

Country. from the specified exiled country.

Boolean.

Checks if the current unit leader is leading is_leading_army_in_province <province id> is_leading_army_in_province = 1234 an army that has any division in a specific 1.17

province

Operatives ​[ edit | edit source ]

This is a community maintained wiki. If you spot a mistake, please help with fixing it.

These triggers only work for operatives.

Operative-scoped triggers: [Collapse]

Version Name Parameters Examples Description Notes

Added

The nationality to check.

Boolean.

Mission.

<token> Checks if the current scope is on the given operative_leader_operation operative_leader_operation = operation_name 1.9

Operation. operation.

Scientists ​[ edit | edit source ]

This is a community maintained wiki. If you spot a mistake, please help with fixing it.

These triggers only work for scientists.

Scientist-scoped triggers: [Collapse]

Version Name Parameters Examples Description Notes

Added

level = <int>

Level to check. has_scientist_level = { Checks if the scientist of the character in scope has_scientist_level   level > 2 matches the skill level condition for a specialization. 1.15

specialization = <specialization_token>   specialization = specialization_nuclear Supports < > = operators.

Specialization. }

Checks if the scientist of the character in scope is is_active_scientist <bool> is_scientist_active = yes 1.15

assigned to a project.

Checks if the scientist of the character in scope is is_scientist_injured <bool> is_scientist_injured = yes 1.15

injured.

Combat ​[ edit | edit source ]

This is a community maintained wiki. If you spot a mistake, please help with fixing it.

These triggers are used within the combatant scope. Some trigger blocks in abilities, combat tactics, and unit leader traits check this, and it's impossible to access elsewhere.

Combatant-scoped triggers: [Collapse]

Version Name Parameters Examples Description Notes

Added

<float> Checks if the current scope has the specified hardness hardness > 0.5 Must use either > or < operators. 1.0

The amount to check for. amount of hardness.

<float> Checks if the current scope has the specified armor armor > 0.5 Must use either > or < operators. 1.0

The amount to check for. amount of armor units.

<float> Checks if the current scope has the specified dig_in dig_in > 0.5 Must use either > or < operators. 1.0

The amount to check for. amount of Dig In bonus.

<float> Checks if the current scope has the specified min_planning min_planning > 0.5 Must use either > or < operators. 1.0

The amount to check for. amount of planning.

<float> Checks if the current scope has a unit with the fastest_unit fastest_unit > 12 Must use either > or < operators. 1.0

The speed in km/h to check for. specified speed.

<float> Checks if the current scope is in a province with temperature temperature > 20 Must use either > or < operators. 1.0

The temperature in celsius to check for. a temperature above the specified amount.

<float> Checks if the current scope has the specified reserves reserves > 10 Must use either > or < operators. 1.0

The amount to check for. amount of reserves waiting.

<string> Checks if the current scope has the specified has_combat_modifier has_combat_modifier = river_crossing 1.0

The modifier to check for. combat modifier.

<string> Checks if the current scope is fighting in the is_fighting_in_terrain is_fighting_in_terrain = desert 1.0

The terrain to check for. specified terrain.

The weather to check for. Checks if the current scope is fighting in the

is_fighting_in_weather OR 1.0 specified weather.

{ <string> <...> <string> } is_fighting_in_weather = { rain_light

The weather to check for in an OR statement. rain_heavy }

Boolean.

<bool> Checks if the current scope has x recon recon_advantage recon_advantage > 0 1.0

Boolean. advantage.

Boolean.

<bool> Checks if the current scope has a full combat frontage_full frontage_full = yes 1.0

Boolean. width.

<bool> Checks if the current scope has flanked their has_flanked_opponent has_flanked_opponent = yes 1.0

Boolean. opponent.

<bool> Checks if the current scope has the maximum has_max_planning has_max_planning = yes 1.0

Boolean. planning bonus.

<bool> Checks if the current scope has any reserves has_reserves has_reserves = yes 1.0

Boolean. waiting.

<bool> Checks if the current scope is performing an is_amphibious_invasion is_amphibious_invasion = yes 1.0

Boolean. amphibious invasion.

Boolean.

Boolean.

<bool> Checks if the current scope is winning their is_winning is_winning = yes 1.0

Boolean. battle.

Boolean.

<bool> Checks if the current scope is fighting with less less_combat_width_than_opponent less_combat_width_than_opponent = yes 1.0

Boolean. combat width than their opponent.

<bool> Checks if the current scope has carrier airwings has_carrier_airwings_on_mission has_carrier_airwings_on_mission = yes 1.0

Boolean. on a mission.

<bool> Checks if the current scope has carrier airwings has_carrier_airwings_in_own_combat has_carrier_airwings_in_own_combat = yes 1.0

Boolean. in their own combat.

Check that ratio of atrillery battalions in the has_artillery_ratio <float> has_artillery_ratio > 0.1 composition of a side of combating troops are ???

over a certain level.

Check if the combatant has at least one of the has_unit_type <unit_type> has_unit_type = amphibious_mechanized ???

provided unit types.

Division scope ​[ edit | edit source ]

This is a community maintained wiki. If you spot a mistake, please help with fixing it.

Can be used in Division scope.

Division-scoped triggers: [Collapse]

Version Name Parameters Examples Description Notes

Added

Battalions are defined within <battalion>

division_has_majority_template division_has_majority_template = Checks if the current scope is majority made /Hearts of Iron IV/common/units/*.txt 1.12

Battalion to check for. light_armor up of the specified battalion. files.

Battalions are defined within <battalion>

division_has_battalion_in_template division_has_battalion_in_template = Checks if the current scope has any /Hearts of Iron IV/common/units/*.txt 1.12

Battalion to check for. light_armor battalions of the type in the template. files.

<float> Checks the current strength of the unit on the unit_strength unit_strength < 0.3 Must use either the < or > operator. 1.12

The amount to check for. scale from 0 to 1.

<float> Checks the current organisation of the unit unit_organization unit_organization < 0.3 Must use either the < or > operator. 1.12

The amount to check for. on the scale from 0 to 1.

Checks if the current division has the supply <bool>

is_unit_template_reserves is_unit_template_reserves = yes priority set to 'Reserves', i.e. the lowest Must use either the < or > operator. 1.12 Boolean.

priority.

<string> Checks if the current division has an officer has_officer_name has_officer_name = FIN_nikke_parmi

The localization key to check. with the provided name key.

MIO scope ​[ edit | edit source ]

This is a community maintained wiki. If you spot a mistake, please help with fixing it.

Can be used in Military-industrial organisation scope.

MIO-scoped triggers: [Collapse]

Version Name Parameters Examples Description Notes

Added

1.13 MIO to check. my_mio_token matches the input token.

<bool> Checks if the currently-scoped MIO is_mio_visible is_mio_visible = yes 1.13

Boolean. is visible.

<bool> Checks if the currently-scoped MIO is_mio_available is_mio_available = yes 1.13

Boolean. is visible.

<bool> Checks if the currently-scoped MIO is_mio_assigned_to_task is_mio_assigned_to_task = yes 1.13

Boolean. is assigned to a task.

Integer.

<token> has_mio_trait = my_trait_token

Trait to check. Checks whether the MIO has the

has_mio_trait OR 1.13 has_mio_trait = { target trait in its list.

trait = <token> token = my_trait_token

Trait to check. }

Trait to check.

OR is_mio_trait_available = my_trait_token

trait = <token>

Trait to check. Checks whether the MIO has the is_mio_trait_available check_mio_parent_completed = <bool> is_mio_trait_available = { target trait in its list and whether it's 1.13

Whether to check if the parent traits are complete.     token = my_trait_token available. True by default.     check_mio_parent_completed = no

} check_mio_mutually_exclusive = <bool>

Whether to check if any mutually exclusive traits are complete. True by default.

<token> is_mio_trait_completed = my_trait_token

Trait to check. Checks whether the MIO has the is_mio_trait_completed OR target trait in its list and whether it's 1.13

is_mio_trait_completed = { trait = <token> completed.

Trait to check. }

<int> Checks the amount of unlocked has_mio_number_of_completed_traits has_mio_number_of_completed_traits < 2 Accepts variables. May use < or >. 1.13

Integer. MIO traits.

<string> Checks if the current scope has the has_mio_flag has_mio_flag = my_flag 1.13

The flag to check. specified flag.

flag = <string>

The flag to check. has_mio_flag = {

value = <int> flag = my_flag Compares the specified flag's last

The flag value to check for. Optional. has_mio_flag     days > 30 If not set, the value comparison is >0 . value  is limited

set date, days since last set, and/or 1.13 date = <date>     date > 1936.6.1 between -32768 and 32767.

value. The flag creation date to check for. Optional.     value > 0

} days = <int>

The duration the flag existed for. Optional.

<token> Checks if the currently-scoped MIO has_mio_policy has_mio_policy = my_policy_token 1.13

Policy to check. has the target policy allowed.

<token> Checks if the currently-scoped MIO has_mio_policy_active has_mio_policy_active = my_policy_token 1.13

Policy to check. has the target policy active.

1.13 Category to check. my_research_category_token has the target research category.

The possible equipment types are defined in script_enum_equipment_bonus_type  (in

<token> Checks if the currently-scoped MIO /Hearts of Iron IV/common/script_enums.txt) and has_mio_equipment_type has_mio_equipment_type = 1.13

Type to check. my_equipment_type_token has the target equipment types. in /Hearts of Iron IV/common/equipment_groups.txt

files.

Contract scope ​[ edit | edit source ]

This is a community maintained wiki. If you spot a mistake, please help with fixing it.

Can be used in purchase contract scope.

Contract-scoped triggers: [Collapse]

Version Name Parameters Examples Description Notes

Added

May use equipment types, equipment <token>

contract_contains_equipment contract_contains_equipment = Checks if the currently-scoped purchase contract archetypes, or types of equipment 1.13

Type to check. infantry_equipment contains an equipment type. archetypes.

<decimal> May use < or >. On the scale from 0 deal_completion deal_completition > 0.6 Checks the deal completion with the target value. 1.13

Progress to compare with. to 1.

Country to check.

Country to check.

Special projects ​[ edit | edit source ]

This is a community maintained wiki. If you spot a mistake, please help with fixing it.

Special project-scoped triggers: [Collapse]

Version Name Parameters Examples Description Notes

Added <string>

The flag to check for.

OR has_project_flag = my_flag

flag = <string>

The flag to check. Check if flag has been set within the special project If not set, the value comparison is >0. has_project_flag = {

has_project_flag value = <int>   flag = my_flag in scope. May checks on the value or date/days value is limited between -32768 and 1.15 The flag value to check for. Optional.   value < 12 since last modified date. 32767. date = <date>   date > 1936.3.25

The flag creation date to check for. Optional.   days > 365

} days = <int>

The duration the flag existed for. Optional.

Meta triggers ​[ edit | edit source ]

This is a community maintained wiki. If you spot a mistake, please help with fixing it.

Meta triggers are a system added with 1.6 with Man the Guns[4] used in the exact same manner as meta effects: in order to tie a trigger block to a dynamic localisation entry. This is usually used in conjunction with scripted localisation for non-numerical checks. This also can serve in order to place a variable check where one is not possible. Meta triggers work in any scope that supports triggers, including combat scope.

The following arguments go inside of a scripted trigger:

text = { ... }  is a trigger block used by the meta trigger. In here, any string that must be made dynamic is marked with the square brackets on each side as [EXAMPLE] . EXAMPLE = "..."  is a string that will serve as the dynamic replacement for [EXAMPLE]  within text = { ... } . This accepts dynamic localisation to a limited degree, such as getting a variable's value with [?var_name]  or some namespaces like [ROOT.GetTag] . debug = yes , if added, will add the final output in the user directory's /Hearts of Iron IV/logs/game.log file for debugging purposes.

For example, the following can be used with has_equipment to make it depend on a variable's value:

meta_trigger = {

}

In this case, GetEquipmentArchetype  is scripted localisation. An example definition of scripted localisation in this case is as such:

defined_text = {

}

Scripted triggers ​[ edit | edit source ]

This is a community maintained wiki. If you spot a mistake, please help with fixing it.

Scripted triggers serve a similar purpose to functions  in that they can be defined in /Hearts of Iron IV/common/scripted_triggers/*.txt and then used elsewhere as a shortened version. Alongside that, the game has certain base game scripted triggers that are checked directly in the game's code determining triggers for functions that cannot be changed.

A scripted trigger is defined simply as

scripted_trigger_name = {

}

This example can be used as a trigger in regular code as scripted_trigger_name = yes  or scripted_trigger_name = no .

For example, this would be the definition in any scripted trigger file:

state_is_in_area = {

}

This can then be used in any other trigger block, such as a national focus' available section:

focus = {

}

If there are several definitions of scripted triggers with the exact same name, the game will make the scripted trigger be the one that was evaluated later, as decided by file names and order in files.

Diplomacy scripted triggers ​[ edit | edit source ]

Diplomacy scripted triggers fall into two mutually exclusive groups: deciding availability and deciding visibility. By default, these are defined in /Hearts of Iron IV/common/scripted_triggers/diplomacy_scripted_triggers.txt and /Hearts of Iron IV/common/scripted_triggers/00_diplo_action_valid_triggers.txt respectively. While it is possible to also use them as regular scripted triggers, they will also modify the prerequisites of being able to do a diplomatic action between 2 countries.

A diplomacy scripted trigger is identified by the naming pattern of DIPLOMACY_<action>*ENABLE_TRIGGER  for availability, such as DIPLOMACY_GUARANTEE_ENABLE_TRIGGER , and is_diplomatic_action_valid*<action>  for visibility, such as is_diplomatic_action_valid_stage_coup . In the base game, diplomacy scripted triggers are used to implement the game rules and add some country-specific exceptions, such as the  United Kingdom being unable to release its colonies with the Man the

Guns DLC. Checking the base game's contents beforehand or copying it to the mod would ensure that the mod won't accidentally break any existing game rules.

Generally, the pattern in localisation keys used for the button usually matches up with the internal ID of a diplomatic action. If the diplomatic action doesn't already have a scripted trigger in the base game, entirety of localisation can be searched for the name of the button to find the name of the action. For example, a the English localisation entry of DIPLOMACY_CALL_ALLY_TITLE:0 "Call to arms"  would suggest that the diplomatic action of calling ally to war is CALL_ALLY .

List of known triggers:

DIPLOMACY_JOIN_ALLY_ENABLE_TRIGGER

DIPLOMACY_CALL_ALLY_ENABLE_TRIGGER

DIPLOMACY_WAR_ENABLE_TRIGGER

DIPLOMACY_JOIN_FACTION_ENABLE_TRIGGER

DIPLOMACY_CREATE_FACTION_ENABLE_TRIGGER

DIPLOMACY_PEACE_PROPOSAL_ENABLE_TRIGGER

DIPLOMACY_IMPROVERELATION_ENABLE_TRIGGER

DIPLOMACY_NONAGGRESSIONPACT_ENABLE_TRIGGER

DIPLOMACY_REVOKE_NONAGGRESSIONPACT_ENABLE_TRIGGER

DIPLOMACY_OFFER_JOIN_FACTION_ENABLE_TRIGGER

DIPLOMACY_REQUEST_FOREIGN_MANPOWER_ENABLE_TRIGGER

DIPLOMACY_CANCEL_FOREIGN_MANPOWER_ENABLE_TRIGGER

DIPLOMACY_CANCEL_LICENSED_PRODUCTION_ENABLE_TRIGGER

DIPLOMACY_REQUEST_ACCESS_TO_LICENCE_PRODUCTION_ENABLE_TRIGGER

DIPLOMACY_SEND_ATTACHE_ENABLE_TRIGGER

DIPLOMACY_EMBARGO_ENABLE_TRIGGER

DIPLOMACY_SEND_EXP_FORCE_ENABLE_TRIGGER

DIPLOMACY_REQUEST_EXP_FORCE_ENABLE_TRIGGER

DIPLOMACY_RETURN_EXP_FORCE_ENABLE_TRIGGER

DIPLOMACY_ASKSTATECONTROL_ENABLE_TRIGGER

DIPLOMACY_GIVESTATECONTROL_ENABLE_TRIGGER

DIPLOMACY_GUARANTEE_ENABLE_TRIGGER

DIPLOMACY_REVOKE_GUARANTEE_ENABLE_TRIGGER

DIPLOMACY_RELEASE_NATION_ENABLE_TRIGGER

DIPLOMACY_MILACC_ENABLE_TRIGGER

DIPLOMACY_OFFER_MILACC_ENABLE_TRIGGER

DIPLOMACY_REVOKE_OFFER_MILACC_ENABLE_TRIGGER

DIPLOMACY_DOCKING_RIGHTS_ENABLE_TRIGGER

DIPLOMACY_OFFER_DOCKING_RIGHTS_ENABLE_TRIGGER

DIPLOMACY_AIR_BASE_ACCESS_ENABLE_TRIGGER

DIPLOMACY_OFFER_AIR_BASE_ACCESS_ENABLE_TRIGGER

DIPLOMACY_REVOKE_OFFER_DOCKING_RIGHTS_ENABLE_TRIGGER

DIPLOMACY_REVOKE_OFFER_AIR_BASE_ACCESS_ENABLE_TRIGGER

DIPLOMACY_LEND_LEASE_ENABLE_TRIGGER

DIPLOMACY_INCOMING_LEND_LEASE_ENABLE_TRIGGER

DIPLOMACY_REQUEST_LICENSED_PRODUCTION_ENABLE_TRIGGER

DIPLOMACY_GENERATE_WARGOAL_ENABLE_TRIGGER

DIPLOMACY_BOOST_PARTY_POPULARITY_ENABLE_TRIGGER

DIPLOMACY_STAGE_COUP_ENABLE_TRIGGER

DIPLOMACY_LEAVE_FACTION_ENABLE_TRIGGER

DIPLOMACY_ASSUME_FACTION_LEADERSHIP_ENABLE_TRIGGER

DIPLOMACY_KICK_FROM_FACTION_ENABLE_TRIGGER

DIPLOMACY_SEND_VOLUNTEERS_ENABLE_TRIGGER

Know tokens for is_diplomatic_action_valid_<action>  are:

declare_war, for the Declare War button generate_wargoal, only affects the war goal menu by greying out the send button guarantee, for the Guarantee Independence button docking_rights, for the request docking rights button offer_docking_right, for the offer docking rights button create_faction, for the create faction button assume_faction_leadership, for the assume leadership of faction button kick_from_faction, for the kick from faction button join_faction, for the ask to join faction button request_access_to_licence_production, for the negotiate license button embargo, for the embargo button send_volunteers, only affects the sent volunteers menu by greying out the send button stage_coup, for the stage coup button boost_party_popularity, for the boost party popularity button

Within a diplomacy scripted trigger, the default scope is the country that does the trigger, while FROM is the target of the action. For availability, the trigger's tooltip is shown to the player when hovering over the diplomatic action; in case of country-specific restrictions, using conditional statements can ensure that the tooltip would only be generated when needed rather than between any pair of nations.

For example, this trigger will prevent BHR from revoking a guarantee on QAT, while still ensuring the game rule works otherwise:

DIPLOMACY_REVOKE_GUARANTEE_ENABLE_TRIGGER = {

}

Resistance initiation triggers ​[ edit | edit source ]

The behaviour of resistance being automatically initiated is decided by the should_initiate_resistance  scripted trigger, by default defined in /Hearts of Iron IV/common/scripted_triggers/00_resistance_initiate_triggers.txt. If it is unfulfilled, the resistance will never initiate unless forced via the the force_enable_resistance effect. Likewise, the resistance will always occur if it's true unless forcefully disabled.

The scripted trigger's behaviour can be overwritten with state-specific scripted triggers, which should be named should_initiate_resistance  with the list of state IDs suffixed at the end, separated by underscores. For example, this ensures that when a state 321 or 123 is controlled by the  United Kingdom, resistance would get initiated if and only if they're national territory of  France; otherwise, the normal behaviour is used:

should_initiate_resistance_123_321 = {

if = {

limit = {

tag = ENG

}

is_core_of = FRA

}

else = {

should_initiate_resistance = yes # If this is not done, resistance would always activate due to overwriting that scripted trigger.

}

}

Useful scripted triggers ​[ edit | edit source ]

These scripted triggers are defined in base game and might be useful to keep in the mod to cut down on the amount of code. As scripted triggers, all of these use a boolean value as argument.

Scripted triggers: [Collapse]

Name Scope Example Description Notes

Checks if ROOT can obtain a wargoal on the current To evaluate as true, the current scope must exist, not share a can_ROOT_get_wargoal_on_THIS Country can_ROOT_get_wargoal_on_THIS = yes

scope. faction with ROOT, and not be a subject of ROOT.

Checks if the current scope is either independent or is_free_or_subject_of_root Country is_free_or_subject_of_root = yes

a subject of ROOT.

Checks if the current scope has the same ideology Needs modifying if there are custom ideologies. Equivalent to has_same_ideology Country has_same_ideology = yes

as ROOT. has_government = ROOT  for base game ideologies.

Checks if the current scope has an ideology that is Communism, Democracy, and Fascism are considered is_enemy_ideology Country is_enemy_ideology = yes

considered enemy to ROOT's. enemy to each other.

Checks if ROOT has at least one division in the has_ROOT_at_least_1_div_in_current_state_scope State has_ROOT_at_least_1_div_in_current_state_scope

= yes current scope.

Checks if the current state is controlled by ROOT or controls_or_subject_of State controls_or_subject_of = yes

a subject of ROOT.

Checks if the current state is controlled by ROOT, a is_controlled_by_ROOT_or_ally State is_controlled_by_ROOT_or_ally = yes subject of ROOT, or a country in the same faction as

ROOT.

Checks if the current scope is owned by ROOT or a owns_or_subject_of State owns_or_subject_of = yes

subject of ROOT.

References and notes ​[ edit | edit source ]

^ a: Triggers are able to set and modify temporary variables. This temporary variable itself may be then used separately to change the game's state, such as in a MTTH block or if the trigger block is used in some effect's limit = { ... }  (most commonly if statements or effect scope limits), though the usage of the variable to change the game's state is not a trigger by itself.

- ↑ [Modding] Achievement for mods Tutorial to write achievements files in your mod

- ↑ How does any_war_score work?

- ↑ forum:1356228/#post-26361808

- ↑ HOI4 Dev Diary - Modding and Traits

Modding Documentation Effects • Triggers • Defines • Modifiers • List of modifiers • Scopes • Localisation • On actions • Data structures (Flags, Event targets, Country tag aliases, Variables, Arrays)

Achievements • AI • AI focuses • Autonomous states • Balances of power • Bookmarks/Scenarios (Game rules) • Buildings • Characters and traits • Cosmetic tags • Countries • Divisions • Decisions • Doctrines • Equipment • Events • Factions • Ideas • Ideologies • Military industrial organizations • Scripting

National focuses • Resources • Scripted GUI • Technologies and doctrines • Units

Map Map • States • Supply areas • Strategic regions

Graphical Interface • Graphical assets • Entities • Posteffects • Particles • Fonts

Cosmetic Portraits • Namelists • Music • Sound

Other Console commands • Troubleshooting • Mod structure • Mods • Nudger

Categories: 1.16 Modding

About Hearts of Iron 4 Wiki Mobile view

GAMES COMMUNITY ABOUT SOCIAL MEDIA

Discover Paradox Forums News

Our Brands Paradox Wikis About us Legal Information

Browse Support Careers EU Online Dispute Resolution Report Illegal Content

Play on Paradox technology Join our playtests

Media contact